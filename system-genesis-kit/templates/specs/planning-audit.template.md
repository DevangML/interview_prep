# Planning Audit (Template Companion)

> _Checklist the `reviewer` runs against a re-derived plan. Zero unresolved violations = pass. GENESIS writes to `<output_folder>/specs/planning-audit.md`, deletes this template._

Run each check; for any ✗, **root-cause it** (which flawed assumption produced it) and fix at **(a) instance** (this schedule) + **(b) systemic** (the invariant/pedagogy rule) so the mistake-class can't recur.

- [ ] **CAP-1** every skill has a cadence tag; no continuous skill parked as a late block.
- [ ] **CAP-2** weakest × slowest × hardest-gated skill starts Day 1.
- [ ] **CAP-3** each core track has a post-sprint spaced review + pre-interview re-verify.
- [ ] **CAP-4** a gate-maintenance re-check covers every earlier gate.
- [ ] **CAP-5** long-lead (referral/interview) nurture starts early; pitch exists early.
- [ ] **CAP-6** every stated weakness has a scheduled home.
- [ ] **CAP-7** Experience Bridge used for bridgeable topics; differentiator + demand-gap tracks exist; hard-gated skills ground-up.
- [ ] **CAP-8** ≥ ~1 light-load week-equivalent buffer before the interview window.
- [ ] **CAP-9** no okf/coach-SKILL rule contradicts the weekly plan.
- [ ] **CAP-10** plan wired to daily-log → RAG → rollover; recalibrates to measured velocity.
- [ ] **Layered proficiency** targets set per track (not single scores), weighted by researched interview distribution.
- [ ] **Plan-vs-reality** rebalanced to the researched interview bucket distribution (no over-indexed / missing bucket).
