import type { LearnTopic } from '../types';

/** Security, part two: identity, transport, supply chain and the policy headers. */
export const securityRoadmapPart2Topics: LearnTopic[] = [
  {
    id: 'rd-sec-authentication',
    area: 'Security',
    group: 'Identity',
    title: 'Authentication — sessions, JWTs, and where the token actually goes',
    status: 'covered',
    minutes: 9,
    summary:
      'Authentication proves who you are. The frontend decision that matters is storage: every option trades XSS exposure against CSRF exposure, and interviewers want you to name the trade rather than recite "use JWT".',
    body: [
      '### Two models',
      '- **Server session** — server stores state, client holds an opaque session id in a cookie. Revocation is instant. Needs shared session storage to scale horizontally.',
      '- **JWT** — signed, self-describing, stateless. Scales trivially. **Cannot be revoked before expiry** unless you add a denylist, at which point you are stateful again.',
      '',
      '### Where to put the token — the real question',
      '| Location | XSS | CSRF | Notes |',
      '| --- | --- | --- | --- |',
      '| `localStorage` | **Exposed** — any XSS reads it | Safe | Convenient, commonly used, worst XSS profile |',
      '| JS memory only | Mostly safe | Safe | Lost on refresh; needs silent renewal |',
      '| `HttpOnly` cookie | **Unreadable by script** | Needs SameSite / token | Usually the right default |',
      '',
      'The strong pattern in 2026: a short-lived access token in memory, plus a long-lived refresh token in an `HttpOnly; Secure; SameSite=Strict` cookie scoped to the refresh path only.',
      '',
      '### Access and refresh',
      'Access tokens are short (5–15 min) to bound the damage of a leak. Refresh tokens are long-lived, single-use and **rotated**: each refresh issues a new one and invalidates the old. Reuse of a spent refresh token means it was stolen — revoke the family.',
      '',
      '### OAuth 2.0 / OIDC for SPAs',
      'Authorization Code **with PKCE** is the current answer. The implicit flow is deprecated — it returned tokens in the URL fragment, where they leaked into history and referrers.',
    ],
    keyPoints: [
      'JWTs cannot be revoked before expiry without reintroducing server state.',
      'localStorage means any XSS is a full account takeover.',
      'Access token short and in memory; refresh token HttpOnly, rotated, single-use.',
      'SPAs use Authorization Code + PKCE; implicit flow is deprecated.',
    ],
    interview:
      'When asked where to store a JWT, refuse the one-word answer. Say what each option costs, then pick HttpOnly cookie for the refresh token and memory for the access token, and say why.',
    pitfalls: [
      'Decoding a JWT client-side and trusting its claims for authorization. Signature verification happens server-side.',
      'Treating "stateless" as free — logout and ban both need state.',
    ],
    resources: [
      { label: 'OWASP Authentication Cheat Sheet', url: 'https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html', kind: 'docs' },
      { label: 'OAuth 2.0 for Browser-Based Apps', url: 'https://datatracker.ietf.org/doc/html/draft-ietf-oauth-browser-based-apps', kind: 'spec' },
    ],
    conceptDuel: [
      {
        q: 'Why is a long-lived JWT in localStorage considered a poor default?',
        options: ['It is slow to parse', 'Any XSS reads it and it cannot be revoked', 'It breaks CORS', 'Browsers cap localStorage at 1KB'],
        correct: 1,
        explanation:
          'Two compounding problems: script can read localStorage, so XSS steals it; and a stateless JWT stays valid until expiry, so you cannot cut off the stolen session.',
      },
    ],
  },

  {
    id: 'rd-sec-authorization',
    area: 'Security',
    group: 'Identity',
    title: 'Authorization — RBAC, ABAC, and why the frontend check is only cosmetic',
    status: 'covered',
    minutes: 7,
    summary:
      'Authorization decides what an authenticated principal may do. Frontend permission checks are user experience; the server is the only enforcement point, and broken object-level authorization is the most common real-world API flaw.',
    body: [
      '### Models',
      '- **RBAC** — permissions attach to roles, users get roles. Simple, coarse, ubiquitous.',
      '- **ABAC** — decisions from attributes: `user.dept === doc.dept && doc.status !== "locked"`. Flexible, harder to reason about.',
      '- **ReBAC** — relationship-based (Google Zanzibar style): "can edit because they own the parent folder". What large SaaS converges on.',
      '',
      '### The frontend\'s actual job',
      'Hiding a button the user cannot use is **UX, not security**. The endpoint behind it is still reachable with curl. Every guarded action needs a server-side check on the specific object, not just the route.',
      '',
      '### BOLA — the flaw that keeps winning',
      'Broken Object Level Authorization: `GET /api/invoices/1043` checks that you are logged in, but not that invoice 1043 is *yours*. Changing the id in the URL returns someone else\'s data. It is number one on the OWASP API list because it is invisible to scanners — the request looks completely normal.',
      '',
      '### Route guards in React',
      'A `<ProtectedRoute>` improves the experience and prevents flashes of unauthorized content. Treat it as such. Also guard against the *inverse* leak: do not ship data into the client bundle or props that the user is not permitted to see, even if you never render it.',
    ],
    keyPoints: [
      'Authentication is who; authorization is what-may-they-do.',
      'Client-side checks are cosmetic — every one must be repeated server-side.',
      'BOLA: always authorize the specific object, not merely the route.',
      'Never send data to the client that the user may not see, rendered or not.',
    ],
    interview:
      'Say plainly that your route guard is UX. Then describe the server check, and mention object-level authorization by name — it separates people who have shipped from people who have read.',
    pitfalls: [
      'Putting roles in a client-decoded JWT and trusting them for enforcement.',
      'Filtering a list in the UI while the API returned every row.',
    ],
    resources: [
      { label: 'OWASP API Security Top 10', url: 'https://owasp.org/API-Security/editions/2023/en/0x11-t10/', kind: 'docs' },
    ],
  },

  {
    id: 'rd-sec-https',
    area: 'Security',
    group: 'Transport',
    title: 'HTTPS, TLS 1.3 and HSTS — what encryption does and does not hide',
    status: 'covered',
    minutes: 7,
    summary:
      'TLS gives confidentiality, integrity and server authentication for data in transit. It does not hide which host you contacted, and it says nothing about whether the site is trustworthy.',
    body: [
      '### The handshake, briefly',
      'TLS 1.3 completes in **one round trip** (down from two in 1.2), and supports 0-RTT resumption for repeat visits. It removed the legacy cipher suites that caused most downgrade attacks. This is a measurable page-load win, not only a security one.',
      '',
      '### What TLS protects',
      '- **Confidentiality** — the body and path are encrypted.',
      '- **Integrity** — tampering is detected.',
      '- **Authentication of the server** — the certificate proves host identity.',
      '',
      '### What it does not protect',
      '- **The hostname still leaks** via DNS and the TLS SNI field. Encrypted Client Hello is the fix, and is still rolling out.',
      '- **Traffic size and timing** remain observable.',
      '- **The padlock is not a trust badge.** A phishing site can obtain a valid certificate in minutes. "It has HTTPS so it is safe" is wrong, and interviewers do ask.',
      '',
      '### HSTS',
      '`Strict-Transport-Security: max-age=31536000; includeSubDomains` tells the browser to refuse plaintext for this host, closing the SSL-stripping window on the first redirect. Only honoured over HTTPS. `preload` bakes you into browser binaries — powerful and slow to reverse, so do not set it casually.',
      '',
      '### Mixed content',
      'One `http://` script on an HTTPS page voids the guarantee, so browsers block it outright.',
    ],
    keyPoints: [
      'TLS 1.3 = one round trip, fewer ciphers, meaningfully faster.',
      'The hostname leaks through SNI and DNS even under TLS.',
      'A valid certificate proves host identity, never honesty.',
      'HSTS preload is effectively one-way — treat it as a commitment.',
    ],
    interview:
      'A good discriminating answer: HTTPS proves you are talking to the host named on the certificate, and stops tampering in between. It makes no claim about that host being benign.',
    pitfalls: [
      'Believing the padlock means the site is legitimate.',
      'Setting HSTS on a host that still needs plaintext on a subdomain.',
    ],
    resources: [
      { label: 'MDN — HSTS', url: 'https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Strict-Transport-Security', kind: 'docs' },
      { label: 'RFC 8446 — TLS 1.3', url: 'https://datatracker.ietf.org/doc/html/rfc8446', kind: 'spec' },
    ],
  },

  {
    id: 'rd-sec-permissions-policy',
    area: 'Security',
    group: 'Policy',
    title: 'Feature-Policy → Permissions-Policy — controlling capabilities, including in frames',
    status: 'covered',
    minutes: 6,
    summary:
      'One header governs which powerful browser features your document and its iframes may use. Feature-Policy was the original name; Permissions-Policy is the current one, and knowing that it was renamed is itself the interview point.',
    body: [
      '### The rename',
      '`Feature-Policy` shipped first, with a space-separated syntax. It was **renamed to `Permissions-Policy`** with a structured-fields syntax. Browsers still accept the old header for compatibility, but new work should emit only the new one. If someone asks you to distinguish them, the honest answer is that they are the same mechanism at two points in its life.',
      '',
      '```',
      'Feature-Policy:    geolocation \'none\'; camera \'self\'        // legacy',
      'Permissions-Policy: geolocation=(), camera=(self)            // current',
      '```',
      '',
      'Note the encoding: `()` is an empty allowlist meaning **deny everywhere**; `(self)` is this origin only; `(self "https://partner.example")` adds a third party.',
      '',
      '### Why it matters to a frontend engineer',
      '1. **Blast radius of third-party embeds.** A marketing widget in an iframe inherits nothing you have not granted. Denying `camera`, `microphone`, `geolocation` and `payment` by default is cheap.',
      '2. **Accidental capability use.** Denying a feature you never intended to use turns a supply-chain surprise into a no-op.',
      '3. **The `allow` attribute** on `<iframe>` delegates specific features to that frame: `<iframe allow="camera; fullscreen">`.',
      '',
      '### Related but distinct',
      '`Permissions-Policy` decides whether the feature may be *requested at all*. The **Permissions API** (`navigator.permissions.query`) reports whether the user has granted it. Policy is the outer gate; user consent is the inner one.',
    ],
    keyPoints: [
      'Permissions-Policy is the renamed Feature-Policy; same mechanism, new syntax.',
      '`feature=()` denies everywhere; `(self)` allows only your origin.',
      'iframes get capabilities only via the allow attribute, within what the parent policy permits.',
      'Policy gates whether a capability can be asked for; the Permissions API reports user consent.',
    ],
    interview:
      'If asked to list security headers, include Permissions-Policy and say what it is for — most candidates stop at CSP and X-Frame-Options.',
    pitfalls: [
      'Emitting Feature-Policy syntax under the Permissions-Policy header name. The value is parsed differently and silently ignored.',
    ],
    resources: [
      { label: 'MDN — Permissions-Policy', url: 'https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Permissions-Policy', kind: 'docs' },
    ],
  },

  {
    id: 'rd-sec-sri-deps',
    area: 'Security',
    group: 'Supply chain',
    title: 'Subresource Integrity and dependency security — the code you did not write',
    status: 'covered',
    minutes: 8,
    summary:
      'Most of your shipped bytes came from strangers. SRI pins third-party files to a hash; lockfiles, auditing and provenance handle the packages that get compiled in.',
    body: [
      '### Subresource Integrity',
      'A CDN can be compromised, or simply change a file. SRI makes the browser verify a cryptographic hash before executing:',
      '',
      '```html',
      '<script src="https://cdn.example/lib.js"',
      '        integrity="sha384-oqVuAfXRKap7fdgcCY5uykM6+..."',
      '        crossorigin="anonymous"></script>',
      '```',
      '',
      'Mismatch means the resource is not executed. `crossorigin` is required so the browser can read the body for verification. SRI only works for pinned, versioned URLs — you cannot SRI a `@latest` endpoint, which is a good reason not to use one.',
      '',
      '### npm supply chain',
      '- **Lockfiles** — commit them. `npm ci` installs the exact resolved tree; `npm install` may drift.',
      '- **`npm audit`** — noisy, and a transitive advisory in a build-only tool is not the same risk as one in shipped runtime code. Triage by whether the code reaches production and whether the vulnerable path is reachable.',
      '- **Install scripts** — `postinstall` runs arbitrary code at install time. `--ignore-scripts` in CI is a cheap, real control.',
      '- **Typosquatting** — `reakt-dom`, `lodahs`. Check the name character by character when adding a dependency.',
      '',
      '### 2026 posture',
      'Provenance attestations (npm packages signed to a public build) and SBOM generation are becoming table stakes for enterprises. Dependabot/Renovate keeps the tree fresh; the discipline is reviewing the diff, not merging bumps blindly.',
    ],
    keyPoints: [
      'SRI pins a hash and needs crossorigin to verify; it requires immutable URLs.',
      'npm ci installs the lockfile exactly; npm install can silently drift.',
      'postinstall scripts execute arbitrary code — disable them in CI.',
      'Triage audit findings by reachability and whether the code ships.',
    ],
    interview:
      'Good signal: distinguish a dev-dependency advisory from one in shipped runtime code, and say that blanket "zero vulnerabilities" policies produce noise fatigue rather than safety.',
    pitfalls: [
      'Adding SRI to a mutable CDN URL, which breaks the page on the next upstream release.',
      'Auto-merging dependency bumps into production without reading the changelog.',
    ],
    resources: [
      { label: 'MDN — Subresource Integrity', url: 'https://developer.mozilla.org/en-US/docs/Web/Security/Subresource_Integrity', kind: 'docs' },
      { label: 'npm — audit', url: 'https://docs.npmjs.com/cli/v10/commands/npm-audit', kind: 'docs' },
    ],
  },

  {
    id: 'rd-sec-ssrf-ssji',
    area: 'Security',
    group: 'Server-adjacent',
    title: 'SSRF and SSJI — the two server-side flaws a frontend engineer causes',
    status: 'covered',
    minutes: 7,
    summary:
      'Both arise from frontend-shaped features: a URL the user supplies that the server fetches, and user data that reaches a server-side evaluator. Interviewers ask because BFF and SSR work put frontend engineers on that code.',
    body: [
      '### SSRF — Server-Side Request Forgery',
      'Your server fetches a URL the user controls: link previews, avatar-by-URL, webhook testers, PDF renderers, image proxies.',
      '',
      'The attacker supplies an **internal** address. The server has network positions the user does not:',
      '- `http://169.254.169.254/latest/meta-data/` — cloud instance metadata, historically leaking credentials',
      '- `http://localhost:6379` — an unauthenticated Redis on the internal network',
      '- `file:///etc/passwd`',
      '',
      '**Controls:** allowlist schemes and hosts, resolve the DNS name and reject private ranges *before* connecting, disable redirects (or re-validate each hop — attackers redirect from a public host to an internal one), and use IMDSv2 which requires a header a naive proxy will not send.',
      '',
      '### SSJI — Server-Side JavaScript Injection',
      'User input reaches a server-side evaluator. On Node this is `eval`, `new Function`, `vm.runInNewContext`, or a template engine configured to execute expressions.',
      '',
      'The frontend-adjacent version: a "custom formula" or "template" feature where a user-authored string is evaluated on the server. It is remote code execution wearing a product requirement.',
      '',
      '**Controls:** do not evaluate user input. If you must have expressions, use a sandboxed, non-Turing-complete expression parser with an explicit function allowlist — never the JavaScript engine itself. `vm` in Node is not a security boundary.',
    ],
    keyPoints: [
      'SSRF weaponizes the server\'s network position; validate after DNS resolution, not before.',
      'Redirect following defeats naive SSRF allowlists.',
      'Node\'s vm module is isolation, not a security sandbox.',
      'A user-authored "formula" feature evaluated server-side is RCE by another name.',
    ],
    interview:
      'Name a concrete SSRF sink you would have built — link preview or avatar-by-URL — and the control. Specificity here reads as experience.',
    pitfalls: [
      'Validating the URL string, then fetching it — DNS can rebind between the two.',
      'Assuming SSRF is "a backend problem" when the feature request came from the frontend.',
    ],
    resources: [
      { label: 'OWASP SSRF Prevention', url: 'https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html', kind: 'docs' },
    ],
  },

  {
    id: 'rd-sec-client-storage',
    area: 'Security',
    group: 'Client state',
    title: 'Client-side storage security and compliance obligations',
    status: 'covered',
    minutes: 7,
    summary:
      'Everything stored in the browser is readable by the user and by any script running in the origin. That fact, plus consent law, decides what may be stored at all.',
    body: [
      '### The single rule',
      'There is no confidential client storage. `localStorage`, `sessionStorage`, IndexedDB and non-HttpOnly cookies are all readable by any script in the origin, and by the user in devtools. Encrypting client-side does not help — the key ships with the code.',
      '',
      '### So what may be stored',
      '**Yes:** UI preferences, drafts, cache of data the user may already see, feature flags.',
      '**No:** anything whose disclosure harms someone else, long-lived credentials, another user\'s PII, payment data.',
      '',
      '### Cookie attributes worth naming',
      '`HttpOnly` (invisible to script) · `Secure` (HTTPS only) · `SameSite` (CSRF posture) · `Domain`/`Path` (scope — over-broad `Domain` shares a cookie with every subdomain, including one a partner controls) · `__Host-` prefix (browser-enforced: Secure, path `/`, no Domain).',
      '',
      '### Compliance, in the terms an interview uses',
      '- **GDPR / India DPDP Act 2023** — non-essential storage needs consent *before* it is written. That includes analytics cookies and most `localStorage` used for tracking. Consent must be as easy to withdraw as to give.',
      '- **Purpose limitation and minimisation** — collect only what the stated purpose needs.',
      '- **Right to erasure** — you must be able to actually delete a person\'s data, which constrains architecture.',
      '- **WCAG 2.2 AA** — the accessibility standard referenced by procurement and by the European Accessibility Act; increasingly a contractual requirement rather than a nice-to-have.',
      '- **PCI DSS** — the reason card fields belong in a payment-provider iframe and never in your DOM.',
    ],
    keyPoints: [
      'No browser storage is confidential; client-side encryption ships its own key.',
      '__Host- prefixed cookies get browser-enforced scoping.',
      'Consent must precede writing non-essential storage, not follow it.',
      'Card data in an provider-hosted iframe keeps your DOM out of PCI scope.',
    ],
    interview:
      'Asked where to keep a token, cover both axes: the security axis (HttpOnly vs script-readable) and the legal axis (consent and minimisation). Very few candidates mention the second.',
    pitfalls: [
      'Writing analytics identifiers before the consent banner is answered.',
      'Setting Domain=.company.com on a session cookie, sharing it with every subdomain.',
    ],
    resources: [
      { label: 'MDN — Set-Cookie', url: 'https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Set-Cookie', kind: 'docs' },
      { label: 'India DPDP Act 2023', url: 'https://www.meity.gov.in/data-protection-framework', kind: 'docs' },
    ],
  },
];
