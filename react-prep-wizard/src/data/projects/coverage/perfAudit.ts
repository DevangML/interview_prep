import { e, type ProjectCoverage } from './types';

/**
 * Intermediate — zero exemptions. A performance autopsy touches everything by
 * nature: rendering, loading, transport, layout and build all appear in the
 * same flame chart, and the audit made each an explicit measurement.
 */
export const perfAuditCoverage: ProjectCoverage = {
  projectId: 'inter-perf-audit',
  edges: [
    e('react-perf', 'explicit', 'Stages 1-2', 'Every tool is applied, measured, and reverted when the measurement does not justify it'),
    e('web-how-page-loads', 'explicit', 'Stage 3', 'LCP, CLS and INP are measured on a throttled profile before and after every loading change'),
    e('tooling-bundlers', 'explicit', 'Stage 3', 'Route-level dynamic imports are introduced and the entry-bundle delta is read from the analyser, not estimated'),
    e('css-media-container', 'explicit', 'Stage 3', 'srcSet and sizes are driven by the same breakpoints as the layout, then measured on a slow connection'),
    e('css-ratio-logical', 'explicit', 'Stage 3', 'Every image reserves its box with width, height and aspect-ratio, taking CLS from 0.4 to 0'),
    e('react-rendering-model', 'explicit', 'Stage 2', 'The flame chart shows 5,000 Row renders and 0 skipped, which is the render model made literal'),
    e('react-references-copying', 'explicit', 'Stage 1', 'The inline arrow prop is a new reference each render, which is precisely what defeats memo'),
    e('react-immutability', 'explicit', 'Stage 2', 'Structural sharing is what makes a shallow comparison a valid skip signal for a large list'),
    e('react-state', 'explicit', 'Stage 2', 'Selection state is moved out of the parent so a click no longer re-renders every row'),
    e('react-effects', 'implicit', 'Measurement', 'The PerformanceObserver is set up and torn down in an effect, since it is an external subscription'),
    e('react-hooks-rest', 'explicit', 'Stage 2', 'useRef holds scroll position without rendering, and useDeferredValue keeps the filter input responsive'),
    e('react-composition', 'explicit', 'Stage 2', 'Rows are composed so the expensive cell can be swapped for a cheap one and the difference measured'),
    e('react-errors-portals', 'explicit', 'Stage 2', 'A boundary keeps a single broken image row from taking the gallery down mid-measurement'),
    e('react-class-lifecycle', 'explicit', 'Stage 1', 'The legacy row is a PureComponent, which is the same shallow comparison as memo and fails the same way'),
    e('r19-actions', 'implicit', 'Filters', 'A transition keeps the filter input responsive while the heavy list re-renders, measured as INP'),
    e('r19-use-rsc', 'explicit', 'Stage 3', 'Moving the static gallery chrome to a server component is measured as an entry-bundle reduction'),
    e('js-event-loop', 'explicit', 'Stage 2', 'The 380ms long task blocking input is the event loop being observed directly rather than described'),
    e('js-promises', 'explicit', 'Stage 3', 'Image decode and lazy chunk loading are promises whose ordering decides what the user sees first'),
    e('js-arrays-objects', 'explicit', 'Stage 2', 'Sorting and filtering 5,000 items is the allocation the profiler flags, then made lazy'),
    e('js-scope-closures', 'explicit', 'Stage 1', 'Every row closure retains its item, which is why 5,000 handlers is a memory decision as well as a render one'),
    e('js-polyfills', 'implicit', 'Filters', 'The scroll and resize handlers are throttled with hand-written utilities and rAF batching'),
    e('js-dom-events', 'explicit', 'Stage 2', 'Delegation replaces per-row handlers, and passive listeners are what stop scroll jank'),
    e('js-this', 'explicit', 'Stage 1', 'The class row binds in its constructor, and the allocation that bind performs per instance is visible in the heap snapshot'),
    e('js-prototypes', 'implicit', 'Measurement', 'Monomorphic shapes matter: rows built from one constructor optimise differently from ad-hoc object literals'),
    e('js-modules', 'explicit', 'Stage 3', 'Static versus dynamic import is what makes a chunk splittable, and barrel files are shown to defeat it'),
    e('js-equality-matrix', 'explicit', 'Stage 1', 'memo compares with Object.is, so which values are considered equal decides the entire bailout'),
    e('js-types-coercion', 'implicit', 'Filters', 'Filter params from the URL are converted once, since converting per row was itself measurable'),
    e('js-defaulting-operators', 'implicit', 'Rows', 'A zero-rated item must not fall back to a placeholder, which is where || costs correctness rather than speed'),
    e('ts-essentials', 'implicit', 'Rows', 'The row props type is narrowed so the memo comparison has a stable, primitive-heavy shape'),
    e('testing-react', 'implicit', 'Regression', 'A budget test fails the build when the entry bundle or the row render count regresses'),
    e('frontend-system-design', 'explicit', 'Stage 3', 'A performance budget is agreed up front, which is what turns optimisation into a spec instead of a hobby'),
    e('router-core', 'explicit', 'Stage 3', 'Route-level splitting is where the largest bundle win comes from, so the route tree is the unit of optimisation'),
    e('redux-core', 'explicit', 'Stage 2', 'A store subscription that re-renders every row is diagnosed, and the selector is the fix'),
    e('redux-react-toolkit', 'explicit', 'Stage 2', 'createSelector memoisation is measured, including the case where an inline argument defeats it'),
    e('state-alternatives', 'explicit', 'Stage 2', 'Where state lives is a performance decision, and this project measures the difference rather than asserting it'),
    e('tooling-flux', 'explicit', 'Stage 2', 'Fan-out from one store to thousands of subscribers is the cost model the selector layer exists to control'),
    e('web-http', 'explicit', 'Stage 3', 'Cache-Control, preload and priority hints move the LCP image earlier in the critical path'),
    e('web-cors', 'explicit', 'Stage 3', 'crossorigin on the image and font requests decides whether timing data is even visible to the page'),
    e('web-storage', 'explicit', 'Stage 3', 'The Cache API serves repeat visits, and the second-load LCP is measured separately from the first'),
    e('web-security', 'explicit', 'Stage 3', 'A strict CSP is measured for its cost, since nonce-based scripts change how the parser is blocked'),
    e('html-semantics', 'explicit', 'Stage 3', 'The heading and landmark structure is what lets the browser and the reader both skip work'),
    e('html-forms', 'implicit', 'Filters', 'The filter form is native, so it works before hydration completes — a real loading-performance property'),
    e('a11y-core', 'explicit', 'Stage 2', 'Virtualisation breaks screen-reader navigation unless the list size is declared, which is asserted in a test'),
    e('css-grid-tracks', 'implicit', 'Gallery', 'The gallery is an auto-fit grid, and track sizing is what lets the virtualiser predict row height'),
    e('css-grid-placement', 'implicit', 'Gallery', 'Featured items span two tracks, which is the case that breaks naive virtualisation and is handled'),
    e('css-grid-align', 'implicit', 'Gallery', 'Captions align across a row regardless of title length, without a second layout pass'),
    e('css-flex-axes', 'implicit', 'Row', 'Each row is a flex line, and the layout cost of flex versus grid is measured rather than assumed'),
    e('css-flex-sizing', 'implicit', 'Row', 'Intrinsic sizing forces extra layout work, which the profile shows as a measurable cost'),
    e('css-flex-align', 'implicit', 'Toolbar', 'Auto margins avoid an extra spacer element, which is one fewer node per row at 5,000 rows'),
    e('css-box-display', 'explicit', 'Stage 3', 'content-visibility and contain give the browser permission to skip offscreen layout entirely'),
    e('css-positioning', 'implicit', 'Gallery', 'The virtualiser positions rows absolutely inside a sized spacer, which is why the scrollbar stays honest'),
    e('css-cascade', 'explicit', 'Stage 3', 'Unused CSS is measured, and cascade layers let dead rules be removed without fear of specificity regressions'),
    e('css-selectors', 'explicit', 'Stage 3', 'Deep descendant selectors are measured in the recalculate-style pass, which is where selector cost is actually visible'),
    e('css-states', 'implicit', 'Row', 'Hover styling that triggers layout is replaced with a transform-only variant, and the difference is recorded'),
    e('css-units', 'implicit', 'Gallery', 'Row height is expressed in rem so the virtualiser estimate holds when the user changes font size'),
    e('css-tokens-modern', 'implicit', 'Theme', 'Custom properties allow a theme switch without re-parsing a stylesheet, which is measured as a style-recalc cost'),
  ],
  exemptions: [],
};
