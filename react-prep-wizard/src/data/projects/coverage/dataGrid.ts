import { e, type ProjectCoverage } from './types';

export const dataGridCoverage: ProjectCoverage = {
  projectId: 'service-data-grid',
  edges: [
    e('html-semantics', 'explicit', 'Stage 1', 'The grid begins as a real table so what windowing later costs is explicit and repaid with roles'),
    e('js-arrays-objects', 'explicit', 'Stage 1', 'The comparator is multi-key with an index tiebreaker, tested against a dataset engineered for ties'),
    e('js-equality-matrix', 'explicit', 'Stage 1', 'Deciding when two rows are equal for sorting is a stated rule, not whatever the engine happens to do'),
    e('react-perf', 'explicit', 'Stage 2', 'Three memo boundaries are chosen by which inputs actually change, then measured'),
    e('react-rendering-model', 'explicit', 'Stage 3', 'Windowed rows are keyed so scrolling reuses nodes instead of remounting every row'),
    e('react-state', 'explicit', 'Stage 2', 'Sorted rows are derived and never stored, so the two representations cannot disagree'),
    e('react-hooks-rest', 'explicit', 'Stage 2', 'useMemo boundaries and a ref-held viewport measurement keep derivation off the render path'),
    e('router-core', 'explicit', 'Stage 2', 'Sort, filter and page live in search params, so a pasted link reproduces the exact grid'),
    e('a11y-core', 'explicit', 'Stage 3', 'Roving focus replaces five thousand tab stops and the active cell is announced with its column'),
    e('css-grid-tracks', 'explicit', 'Stage 3', 'Column widths are grid tracks driven by custom properties, so a resize is a variable change'),
    e('css-grid-placement', 'explicit', 'Stage 3', 'Header and body share a track definition, which is what keeps them aligned while windowed'),
    e('css-grid-align', 'explicit', 'Stage 3', 'Numeric columns align to the end and text to the start, decided per column rather than globally'),
    e('css-positioning', 'explicit', 'Stage 3', 'The header sticks while the body windows, which requires the containment to be deliberate'),
    e('css-box-display', 'explicit', 'Stage 3', 'The body owns its scroll and containment, or windowing scrolls the page instead of the grid'),
    e('js-dom-events', 'explicit', 'Stage 3', 'Column resize uses pointer capture so the drag survives the pointer leaving the handle'),
    e('js-polyfills', 'explicit', 'Stage 2', 'Scroll and resize work is frame-bound rather than event-bound, and the difference is measured'),
    e('ts-essentials', 'explicit', 'Stage 1', 'A column is generic over the row type, so an accessor returning the wrong field is a compile error'),
    e('react-composition', 'explicit', 'Stage 1', 'Columns are data with render functions, so adding one never edits the grid component'),
    e('react-effects', 'explicit', 'Stage 3', 'A ResizeObserver measures the viewport and is disconnected on unmount'),
    e('testing-react', 'explicit', 'Stage 1', 'Repeated sort toggles must not reorder equal rows, and one tab press must leave the grid'),
    e('rd-react-hooks', 'explicit', 'Stage 2', 'Memo, ref and effect together are what keep a five-thousand-row pipeline off the render path'),
    e('rd-react-components', 'explicit', 'Stage 1', 'Column definitions as data is the roadmap composition question with a concrete answer'),
    e('rd-perf-rendering-media', 'explicit', 'Stage 3', 'Scroll frame time is recorded before and after windowing and committed to the repository'),
    e('rd-perf-web-vitals', 'explicit', 'Stage 3', 'Interaction latency while scrolling is held to a stated target rather than described as smooth'),
    e('rd-react-testing', 'explicit', 'Stage 1', 'Sort stability is a property test, which is what testable derivation buys'),

    e('js-types-coercion', 'implicit', 'Stable sort', 'Comparators normalise nulls, numeric strings and dates, or 10 sorts before 9'),
    e('js-defaulting-operators', 'implicit', 'Stable sort', 'A null cell and an empty cell sort differently, and the choice is stated per column'),
    e('react-immutability', 'implicit', 'Derived pipeline', 'Sorting in place corrupts the original order the stable tiebreaker depends on'),
    e('react-references-copying', 'implicit', 'Derived pipeline', 'Each stage returns a new array over the same row objects, which is what makes memoisation sound'),
    e('js-scope-closures', 'implicit', 'Column resize', 'Every resize closes over its column id and start width, or the wrong column moves'),
    e('js-event-loop', 'implicit', 'Windowed body', 'Scroll events outpace frames, so the window is recomputed on a frame rather than per event'),
    e('js-promises', 'implicit', 'Derived pipeline', 'Loading five thousand rows is asynchronous, and a superseded load must not replace a newer one'),
    e('css-flex-axes', 'implicit', 'Table semantics', 'A header cell is a flex line: label, sort indicator, then resize handle'),
    e('css-flex-sizing', 'implicit', 'Table semantics', 'The label grows and the caret holds its basis, so a long column name cannot push it out'),
    e('css-flex-align', 'implicit', 'Table semantics', 'The sort caret aligns to the text baseline rather than the centre of the header cell'),
    e('css-tokens-modern', 'implicit', 'Column resize', 'Widths are custom properties, so resizing writes one variable instead of styling every cell'),
    e('css-cascade', 'implicit', 'Column resize', 'Density and theme overrides sit in layers so neither escalates specificity over row styling'),
    e('css-selectors', 'implicit', 'Table semantics', 'Sorted and active states are attribute-driven, so JavaScript never toggles a class name'),
    e('css-units', 'implicit', 'Column resize', 'Row height and padding are relative, so the grid survives an increased browser font size'),
    e('css-states', 'implicit', 'Grid keyboard', 'The active cell has a visible focus style that is never removed for keyboard users'),
    e('css-media-container', 'implicit', 'Column resize', 'The grid switches to compact density from its own width, so it works inside a drawer'),
    e('web-storage', 'implicit', 'Column resize', 'Column widths persist keyed by grid id, so users do not re-resize on every visit'),
    e('web-http', 'implicit', 'URL view state', 'The row budget at which server-side paging must take over is stated rather than discovered'),
    e('state-alternatives', 'implicit', 'Derived pipeline', 'Derived state versus stored state is decided explicitly, which is the whole caching argument in miniature'),
    e('rd-react-data-fetching', 'implicit', 'URL view state', 'Client-side paging is chosen deliberately, with the threshold for moving it to the server written down'),
    e('rd-fe-modern-css', 'implicit', 'Column resize', 'Custom properties, layers and container queries carry the styling with no grid framework'),
  ],
  exemptions: [
    {
      reason: 'The grid is functions, generics and hooks. Nothing constructs a prototype chain, rebinds a receiver, or needs a class component or an error boundary.',
      conceptIds: ['js-this', 'js-prototypes', 'react-class-lifecycle', 'react-errors-portals'],
    },
    {
      reason: 'A read-only grid has no form beyond a filter input and no submission, so form semantics and the Actions model are covered where they are the subject.',
      conceptIds: ['html-forms', 'r19-actions', 'r19-use-rsc', 'rd-react-routing-forms', 'rd-react-rsc-compiler'],
    },
    {
      reason: 'State here is derived from props and the URL. A store would add indirection without removing a problem, so Redux, RTK and Flux are taught in the build that needs them.',
      conceptIds: ['redux-core', 'redux-react-toolkit', 'tooling-flux', 'rd-react-state-mgmt'],
    },
    {
      reason: 'Module architecture, bundling, origin policy, untrusted content and system-scale design are application concerns a single grid surface does not honestly exercise.',
      conceptIds: ['js-modules', 'tooling-bundlers', 'web-cors', 'web-security', 'frontend-system-design'],
    },
    {
      reason: 'Resource priority, CDN behaviour and the full request path are measured properly by the performance audit build rather than claimed in passing here.',
      conceptIds: ['rd-perf-high-priority', 'rd-perf-network-cdn', 'web-how-page-loads', 'rd-fe-internet-browser'],
    },
    {
      reason: 'No media appears in a cell, so there is no intrinsic ratio to reserve; Web Components and package-level V8 analysis are untouched by a React grid built from primitives.',
      conceptIds: ['css-ratio-logical', 'rd-fe-html-web-components', 'rd-fe-js-v8-packages'],
    },
    {
      reason:
        'These projects were designed before the frontend system-design track existed, and none of them chooses a transport under a stated workload — the thing the topic actually teaches. Claiming them here would be coverage by adjacency: the build fetches over HTTP, therefore it \'covers\' gRPC. A dedicated system-design project carries these edges instead.',
      conceptIds: [
        'rd-comm-protocols',
        'rd-comm-rest',
        'rd-comm-graphql',
        'rd-comm-grpc',
        'rd-comm-polling',
        'rd-comm-websocket-sse',
        'rd-comm-webhooks-webrtc',
      ],
    },
    {
      reason:
        'Security is taught by attacking and then defending a specific sink, and none of these builds has an untrusted-input surface, a session, or a cross-origin boundary to defend. A project that renders trusted local data cannot honestly claim to teach XSS.',
      conceptIds: [
        'rd-sec-overview',
        'rd-sec-xss',
        'rd-sec-sanitization',
        'rd-sec-csrf',
        'rd-sec-cors',
        'rd-sec-headers',
        'rd-sec-iframe',
        'rd-sec-authentication',
        'rd-sec-authorization',
        'rd-sec-https',
        'rd-sec-permissions-policy',
        'rd-sec-sri-deps',
        'rd-sec-ssrf-ssji',
        'rd-sec-client-storage',
      ],
    },
    {
      reason:
        'Client storage, HTTP caching and telemetry are properties of a deployed system under real traffic. These builds run locally against fixtures, so the invalidation and eviction behaviour that makes the topic hard never occurs.',
      conceptIds: [
        'rd-store-comparison',
        'rd-cache-http',
        'rd-cache-client',
        'rd-obs-telemetry',
        'rd-obs-alerting',
      ],
    },
  ],
};
