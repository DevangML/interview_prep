import { e, type ProjectCoverage } from './types';

/**
 * Advanced — zero exemptions. Writing the framework means writing everything
 * the framework provides, and then building a real application on top of it to
 * prove the renderer holds. Both halves are in scope.
 */
export const reactFromScratchCoverage: ProjectCoverage = {
  projectId: 'adv-react-from-scratch',
  edges: [
    e('react-rendering-model', 'explicit', 'Stages 2-3', 'The fiber tree, the yielding work loop and the alternate-pointer swap are all implemented by hand'),
    e('react-state', 'explicit', 'Stage 4', 'The update queue is built, which shows directly why an updater sees the pending value and a captured variable does not'),
    e('react-effects', 'explicit', 'Stage 4', 'useEffect is implemented, including running cleanup before every re-run and on deletion'),
    e('react-hooks-rest', 'explicit', 'Stage 4', 'useRef, useMemo, useContext and useReducer are each built on the same slot array, so their costs become obvious'),
    e('r19-use-rsc', 'explicit', 'Stage 4', 'With the slot array built, the exception allowing use() to be called conditionally follows from it not being slot-backed'),
    e('react-perf', 'explicit', 'Stage 3', 'A bailout is added to the reconciler, making the shallow prop comparison something you wrote rather than trust'),
    e('js-event-loop', 'explicit', 'Stage 2', 'requestIdleCallback and the frame budget are what make rendering interruptible, and yielding is measured'),
    e('web-how-page-loads', 'explicit', 'Stage 2', 'The commit phase is where the DOM is touched, which is the only point the browser can paint'),
    e('react-immutability', 'explicit', 'Stage 3', 'Elements are immutable descriptions and fibers are mutable instances, and conflating them breaks reconciliation'),
    e('react-references-copying', 'explicit', 'Stage 3', 'Double buffering reuses the previous tree, so identity across renders is the mechanism, not an optimisation'),
    e('js-scope-closures', 'explicit', 'Stage 4', 'The dispatcher closes over the work-in-progress fiber, which is exactly why hooks cannot be called outside a render'),
    e('js-this', 'implicit', 'Class support', 'Class components must be invoked with new and their render called on the instance, unlike a function component'),
    e('js-prototypes', 'implicit', 'Class support', 'Detecting a class component requires reading prototype.isReactComponent, since typeof answers "function" for both'),
    e('react-class-lifecycle', 'implicit', 'Class support', 'Implementing didMount, didUpdate and willUnmount as commit-phase effects shows exactly what a hook replaced'),
    e('js-arrays-objects', 'explicit', 'Stage 3', 'Children are normalised, flattened and keyed, which is where most hand-written renderers quietly get it wrong'),
    e('js-equality-matrix', 'explicit', 'Stage 3', 'The bailout compares with Object.is, so implementing it settles NaN and -0 permanently'),
    e('js-types-coercion', 'explicit', 'Stage 1', 'Deciding what renders — 0, false, null, undefined, an array — is a coercion policy you have to write down'),
    e('js-defaulting-operators', 'explicit', 'Stage 1', 'defaultProps semantics differ from ?? in exactly the null-versus-undefined case, and both are implemented'),
    e('js-dom-events', 'implicit', 'Commit phase', 'Synthetic events are delegated at the root, which is the same delegation the vanilla todo project introduced'),
    e('js-modules', 'implicit', 'Package', 'The renderer, the reconciler and the DOM host config are separate modules, which is how React itself is factored'),
    e('js-promises', 'implicit', 'Suspense', 'A thrown promise is caught by the nearest boundary and retried on resolve, which is the whole Suspense protocol'),
    e('js-polyfills', 'implicit', 'Scheduler', 'The scheduler is a hand-written priority queue with a MessageChannel yield, since rIC is not reliable enough'),
    e('ts-essentials', 'implicit', 'Package', 'The fiber and element types are exhaustive unions, so an unhandled work tag is a compile error'),
    e('testing-react', 'implicit', 'Suite', 'A test proves state follows keys and not positions, which is the specification of reconciliation in one assertion'),
    e('tooling-bundlers', 'implicit', 'JSX', 'The JSX transform is configured to your own pragma, making clear that JSX is a function call and not a template'),
    e('tooling-flux', 'implicit', 'Demo app', 'The demo app is Flux over the custom renderer, which shows the render model is independent of the state model'),
    e('redux-core', 'implicit', 'Demo app', 'A store is written against the custom useSyncExternalStore, which proves the subscription primitive works'),
    e('redux-react-toolkit', 'implicit', 'Demo app', 'The real RTK is then run on the custom renderer, which is the strongest possible compatibility test'),
    e('state-alternatives', 'implicit', 'Demo app', 'Context, a store and local state are each implemented, so their real costs are known rather than assumed'),
    e('react-composition', 'implicit', 'Demo app', 'children, render props and HOCs are each supported, which forces the element model to be correct'),
    e('react-errors-portals', 'implicit', 'Boundaries', 'Error boundaries and portals are implemented, which requires the commit phase to handle a container that is not the parent'),
    e('r19-actions', 'implicit', 'Demo app', 'A minimal useActionState is implemented over the update queue, showing it is a reducer with a pending flag'),
    e('router-core', 'implicit', 'Demo app', 'A tiny router over the History API runs on the custom renderer, proving subtree remounting works'),
    e('frontend-system-design', 'implicit', 'Package', 'The host-config boundary is the design: what belongs to the reconciler and what belongs to the DOM'),
    e('html-semantics', 'implicit', 'Host config', 'The host config must handle real elements, attributes versus properties, and boolean attributes correctly'),
    e('html-forms', 'implicit', 'Host config', 'Controlled inputs are the hardest host-config case: value, defaultValue and the cursor position all interact'),
    e('a11y-core', 'implicit', 'Host config', 'Focus must survive reconciliation, so a keyed reorder that steals focus is a renderer bug you have to fix'),
    e('web-http', 'implicit', 'Demo app', 'The demo fetches real data, so Suspense has something genuinely asynchronous to suspend on'),
    e('web-cors', 'implicit', 'Demo app', 'The demo API is cross-origin, so the data layer running on the custom renderer faces real preflight'),
    e('web-storage', 'implicit', 'Demo app', 'State persists across reload, which requires the renderer to hydrate an initial tree from storage'),
    e('web-security', 'implicit', 'Host config', 'dangerouslySetInnerHTML is implemented, and naming it that is itself the lesson about untrusted markup'),
    e('css-box-display', 'implicit', 'Host config', 'Style handling must respect which properties are unitless, or every layout value is silently wrong'),
    e('css-units', 'implicit', 'Host config', 'The renderer appends px only to unitless numbers on the properties that accept it — a real, published list'),
    e('css-cascade', 'implicit', 'Host config', 'Inline styles applied by the renderer sit above the cascade, which is why the style prop wins so often'),
    e('css-selectors', 'implicit', 'Demo app', 'The demo styles from data attributes the renderer writes, keeping DOM state and visual state in step'),
    e('css-states', 'implicit', 'Demo app', 'Focus, hover and disabled must all survive a keyed reorder, which is how renderer bugs surface visually'),
    e('css-grid-tracks', 'implicit', 'Demo app', 'The demo is a grid-based layout, which stresses the host config with shorthand properties'),
    e('css-grid-placement', 'implicit', 'Demo app', 'Named grid areas exercise attribute-versus-property handling for hyphenated CSS names'),
    e('css-grid-align', 'implicit', 'Demo app', 'Alignment shorthands are applied through the style prop, testing camelCase to kebab-case conversion'),
    e('css-flex-axes', 'implicit', 'Demo app', 'The toolbar is a flex row rendered entirely by the custom renderer'),
    e('css-flex-sizing', 'implicit', 'Demo app', 'flex is a shorthand with a unitless first value, which is the exact case the px-appending rule must not break'),
    e('css-flex-align', 'implicit', 'Demo app', 'Auto margins in the demo prove that layout behaves identically under a hand-written renderer'),
    e('css-positioning', 'implicit', 'Portals', 'A portal renders into a different container, so the stacking context is decided by DOM position and not tree position'),
    e('css-ratio-logical', 'implicit', 'Demo app', 'aspect-ratio takes a unitless value, another case the style handler must not append px to'),
    e('css-media-container', 'implicit', 'Demo app', 'Container queries are pure CSS, which is the cleanest demonstration of what the renderer does not have to do'),
    e('css-tokens-modern', 'implicit', 'Host config', 'Custom properties must bypass the camelCase transform entirely, since --my-token is already correct'),

    // ── Extended roadmap tracks ──
    e('rd-react-components', 'explicit', 'Stage 1', 'Writing the renderer is the only way to learn what a component actually is'),
    e('rd-react-hooks', 'explicit', 'Stage 3', 'Hooks are re-implemented as a linked list on the fiber, which is what the rules of hooks protect'),
    e('rd-react-rsc-compiler', 'explicit', 'Stage 4', 'The compiler and server components are rebuilt in miniature to show what they buy'),
  ],
  exemptions: [
    {
      reason:
        'The extended roadmap tracks were added to the curriculum after this build was designed. These are the ones it does not genuinely exercise, and claiming them because the topic exists is the inflation this manifest is meant to prevent.',
      conceptIds: [
        'rd-react-state-mgmt',
        'rd-react-data-fetching',
        'rd-react-routing-forms',
        'rd-react-testing',
        'rd-perf-high-priority',
        'rd-perf-rendering-media',
        'rd-perf-web-vitals',
        'rd-perf-network-cdn',
        'rd-fe-internet-browser',
        'rd-fe-html-web-components',
        'rd-fe-modern-css',
        'rd-fe-js-v8-packages',
      ],
    },
  ],
};
