import { e, type ProjectCoverage } from './types';

export const formWizardCoverage: ProjectCoverage = {
  projectId: 'service-form-wizard',
  edges: [
    e('html-forms', 'explicit', 'Stage 1', 'The form works with JavaScript disabled before a single client-side rule is added'),
    e('html-semantics', 'explicit', 'Stage 1', 'Fieldsets, legends and associated labels give the flow structure with stylesheets off'),
    e('react-state', 'explicit', 'Stage 2', 'One reducer owns the whole draft, so persistence and restoration have a single shape to handle'),
    e('react-hooks-rest', 'explicit', 'Stage 2', 'useReducer holds the draft and a ref holds the debounce handle so writes never race'),
    e('router-core', 'explicit', 'Stage 2', 'The step is a route parameter, so the back button steps back rather than leaving the flow'),
    e('r19-actions', 'explicit', 'Stage 3', 'Submission is an Action, so pending and error state come from the framework rather than a hand-rolled boolean'),
    e('js-promises', 'explicit', 'Stage 3', 'A rejected submission preserves the draft and maps errors instead of clearing the form'),
    e('web-http', 'explicit', 'Stage 3', 'Status codes decide the difference between a field error, a retryable failure and a hard stop'),
    e('react-effects', 'explicit', 'Stage 2', 'The draft is written on change with a debounce whose timer is cleared on unmount'),
    e('a11y-core', 'explicit', 'Stage 3', 'Focus moves to the step heading on navigation and to the first invalid field on rejection'),
    e('css-states', 'explicit', 'Stage 3', 'Validity is styled from attributes, so the visual state cannot drift from the accessible one'),
    e('ts-essentials', 'explicit', 'Stage 1', 'Each step\'s fields and its error map are typed, so a renamed field breaks the compile not the form'),
    e('web-storage', 'explicit', 'Stage 2', 'The draft persists, but fields marked sensitive are excluded and the reason is written down'),
    e('redux-core', 'explicit', 'Stage 2', 'Draft edits are a pure reducer, which is what makes restoring and clearing a single operation'),
    e('react-composition', 'explicit', 'Stage 2', 'Steps share one submission and one draft, so adding a step touches a registry not the flow'),
    e('testing-react', 'explicit', 'Stage 3', 'A test refreshes mid-flow and asserts the data survived; another asserts focus lands on the rejected field'),
    e('rd-react-routing-forms', 'explicit', 'Stage 2', 'Routing and forms are the same problem here, which is exactly how the roadmap pairs them'),
    e('rd-react-hooks', 'explicit', 'Stage 2', 'Reducer, effect and ref together are what make a resumable draft safe'),
    e('rd-react-components', 'explicit', 'Stage 2', 'A step is a component registered into a flow, which is the roadmap composition question'),
    e('rd-react-state-mgmt', 'explicit', 'Stage 2', 'Draft, step and submission are three kinds of state with a stated owner each'),
    e('rd-react-testing', 'explicit', 'Stage 3', 'The suite asserts resumption and error routing rather than internal calls'),

    e('js-defaulting-operators', 'explicit', 'Stage 2', 'An untouched field, a cleared field and an absent field are three different facts the draft must keep apart'),
    e('js-types-coercion', 'implicit', 'Draft store', 'Restored draft values arrive as strings and are parsed once, at the boundary'),
    e('react-immutability', 'implicit', 'Draft store', 'Mutating the draft makes the persisted copy and the rendered copy disagree'),
    e('react-references-copying', 'implicit', 'Draft store', 'A shallow copy that shares a nested step object still lets a render corrupt the saved draft'),
    e('js-arrays-objects', 'implicit', 'Draft store', 'Repeatable field groups are transformed immutably rather than pushed into in place'),
    e('js-scope-closures', 'implicit', 'Draft store', 'Each debounced save closes over its own handle, or the wrong pending write gets cancelled'),
    e('js-equality-matrix', 'implicit', 'Draft store', 'Deciding whether a field actually changed needs a stated comparison, not a truthiness check'),
    e('web-security', 'explicit', 'Stage 2', 'Sensitive fields are excluded from persistence by an allowlist, because a denylist silently rots as fields are added'),
    e('css-grid-tracks', 'implicit', 'Step router', 'Progress rail, form column and summary are grid tracks that survive a step with twice the fields'),
    e('css-grid-placement', 'implicit', 'Step router', 'Named areas mean the summary can move above the form on a narrow screen without reordering the DOM'),
    e('css-grid-align', 'implicit', 'Step router', 'Labels and controls align across rows so the column reads as one vertical rhythm'),
    e('css-box-display', 'implicit', 'Step router', 'The form column owns its scroll so a long step does not scroll the progress rail away'),
    e('css-flex-axes', 'implicit', 'Token theme', 'A field row is a flex line: label, control, then hint'),
    e('css-flex-sizing', 'implicit', 'Token theme', 'The control holds a minimum basis so a long label cannot shrink it below usable width'),
    e('css-flex-align', 'implicit', 'Token theme', 'The error icon aligns to the first line of a wrapped message'),
    e('css-positioning', 'implicit', 'Error summary', 'The summary anchors to the top of the step and remains reachable when the form scrolls'),
    e('css-tokens-modern', 'implicit', 'Token theme', 'Field, label, hint and error styling all read one set of custom properties'),
    e('css-cascade', 'implicit', 'Token theme', 'Invalid styling sits in a layer so a theme override never has to escalate specificity'),
    e('css-selectors', 'implicit', 'Token theme', 'Attribute selectors express validity, so JavaScript never toggles a class'),
    e('css-units', 'implicit', 'Token theme', 'Field sizing is relative, so the form is usable at an increased browser font size'),
    e('css-media-container', 'implicit', 'Token theme', 'The step responds to its own column width, so it works embedded in a panel'),
    e('state-alternatives', 'implicit', 'Draft store', 'The threshold at which a form library would earn its bytes is stated after building without one'),
    e('rd-fe-modern-css', 'implicit', 'Token theme', 'Custom properties, layers and container queries carry the styling with no framework'),
  ],
  exemptions: [
    {
      reason: 'The build is reducers, hooks and native elements. Nothing constructs a prototype chain, rebinds a receiver, or needs a class component.',
      conceptIds: ['js-this', 'js-prototypes', 'react-class-lifecycle'],
    },
    {
      reason: 'A four-step form does not exercise the DOM event layer beyond what native inputs already handle, nor does it hit event-loop subtleties past a single debounce.',
      conceptIds: ['js-dom-events', 'js-event-loop', 'js-polyfills'],
    },
    {
      reason: 'There is no list large enough to profile, no reconciliation puzzle and no error boundary story; claiming rendering internals here would be padding.',
      conceptIds: ['react-perf', 'react-rendering-model', 'react-errors-portals'],
    },
    {
      reason: 'Redux Toolkit, Flux history and server components are taught where each is the actual subject; a local draft reducer gains nothing by adopting them.',
      conceptIds: ['redux-react-toolkit', 'tooling-flux', 'r19-use-rsc', 'rd-react-rsc-compiler'],
    },
    {
      reason: 'Module architecture, bundling, cross-origin policy, data-fetching patterns and system design are application-scale concerns a single flow does not exercise.',
      conceptIds: ['js-modules', 'tooling-bundlers', 'web-cors', 'frontend-system-design', 'rd-react-data-fetching'],
    },
    {
      reason: 'Performance and the request path are the subject of the audit build, which measures them properly rather than asserting them in passing.',
      conceptIds: ['rd-perf-high-priority', 'rd-perf-rendering-media', 'rd-perf-web-vitals', 'rd-perf-network-cdn', 'web-how-page-loads', 'rd-fe-internet-browser'],
    },
    {
      reason: 'No media appears in the flow, so there is no intrinsic ratio to reserve; Web Components and package-level V8 analysis are untouched by a native-first form.',
      conceptIds: ['css-ratio-logical', 'rd-fe-html-web-components', 'rd-fe-js-v8-packages'],
    },
  ],
};
