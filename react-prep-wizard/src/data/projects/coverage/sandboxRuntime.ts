import { e, type ProjectCoverage } from './types';

/**
 * Advanced — zero exemptions. A plugin host must implement every capability it
 * intends to lend out, which means it implements the platform twice: once for
 * itself, and once as the safe surface it exposes to untrusted code.
 */
export const sandboxRuntimeCoverage: ProjectCoverage = {
  projectId: 'adv-sandbox-runtime',
  edges: [
    e('web-security', 'explicit', 'Stages 1-4', 'The threat model is written first, then CSP, sandbox flags and URL-scheme validation are each defeated and fixed'),
    e('web-cors', 'explicit', 'Stage 2', 'The null-origin frame makes the same-origin policy the enforcement mechanism rather than a footnote'),
    e('web-storage', 'explicit', 'Stage 2', 'A null-origin frame shares no cookies, localStorage or IndexedDB, which is demonstrated rather than asserted'),
    e('react-references-copying', 'explicit', 'Stage 2', 'structuredClone is the only transport precisely because it cannot carry a function, so a capability leak fails loudly'),
    e('ts-essentials', 'explicit', 'Stage 4', 'Every inbound message is schema-parsed, since a type annotation is erased and proves nothing at a trust boundary'),
    e('js-prototypes', 'explicit', 'Stage 1', 'Freezing prototypes is shown to be no defence, because the plugin reaches the same intrinsics through any handed object'),
    e('js-scope-closures', 'explicit', 'Stage 3', 'A capability is a closure over a grant record, which is what makes it unforgeable and revocable'),
    e('js-this', 'explicit', 'Stage 3', 'Host methods are never handed over; only bound, argument-checked thunks cross, or the receiver becomes the capability'),
    e('js-event-loop', 'explicit', 'Stage 4', 'The 32ms watchdog is enforced from the host, since a worker spinning cannot police its own event loop'),
    e('js-promises', 'explicit', 'Stage 3', 'Every RPC call is a promise with a timeout, because a plugin that never replies must not leave a pending host request'),
    e('js-modules', 'explicit', 'Stage 2', 'The plugin SDK is a module boundary, and what it does not export is the actual security surface'),
    e('js-arrays-objects', 'explicit', 'Stage 4', 'The described UI tree is walked and rebuilt immutably, so the host never renders the plugin object itself'),
    e('js-dom-events', 'explicit', 'Stage 4', 'Plugin interactions arrive as messages, and the host synthesises the real DOM events after validating them'),
    e('js-polyfills', 'explicit', 'Stage 4', 'The RPC layer is a hand-written correlation-id dispatcher with timeout, cancel and backpressure'),
    e('js-types-coercion', 'explicit', 'Stage 4', 'Every value from a plugin is untrusted JSON, converted once at the parse boundary with an explicit rejection path'),
    e('js-equality-matrix', 'explicit', 'Stage 3', 'Capability token comparison must be exact and constant-time-safe, so loose equality is a security bug'),
    e('js-defaulting-operators', 'explicit', 'Stage 3', 'A manifest field of 0 or false is a real constraint, so ?? is the only correct default operator in the grant parser'),
    e('react-errors-portals', 'explicit', 'Stage 4', 'Every plugin surface is boundary-wrapped and rendered through a portal the host owns'),
    e('react-class-lifecycle', 'explicit', 'Stage 4', 'The plugin boundary is a class, since a plugin render error must be caught rather than propagated'),
    e('react-rendering-model', 'explicit', 'Stage 4', 'Plugin node ids become keys, so a re-described tree preserves host state for the nodes that persisted'),
    e('react-state', 'explicit', 'Stage 3', 'Grant state, plugin lifecycle and UI state are separate machines, because revocation must be immediate and total'),
    e('react-effects', 'explicit', 'Stage 2', 'Worker creation, port transfer, termination and frame removal are effects whose teardown is a security requirement'),
    e('react-hooks-rest', 'explicit', 'Stage 3', 'Context carries the capability registry, and useRef holds worker handles that must never trigger a render'),
    e('react-immutability', 'explicit', 'Stage 3', 'The grant registry is replaced rather than mutated, so an audit log of every permission change exists by construction'),
    e('react-composition', 'explicit', 'Stage 4', 'Host components are registered by name, and a plugin composes them without ever holding one'),
    e('react-perf', 'explicit', 'Stage 4', 'Per-plugin frame budgets are measured, and a plugin over budget is terminated rather than allowed to drop frames'),
    e('r19-actions', 'explicit', 'Stage 4', 'A plugin-described form maps to a host Action, so the plugin never touches the submission or its credentials'),
    e('r19-use-rsc', 'implicit', 'Host', 'The host shell and plugin directory are server components, keeping the trusted surface out of the client bundle'),
    e('redux-core', 'explicit', 'Stage 3', 'The grant and audit log is a reducer, since every permission change must be replayable for a security review'),
    e('redux-react-toolkit', 'explicit', 'Stage 3', 'createSlice manages host state while plugin state is deliberately kept outside it'),
    e('tooling-flux', 'explicit', 'Stage 3', 'One dispatcher validates everything a plugin asks for, which is why the boundary is auditable at all'),
    e('state-alternatives', 'implicit', 'Architecture', 'Host state, plugin state and server state are three systems with a documented, enforced boundary'),
    e('router-core', 'implicit', 'Host', 'A plugin may request navigation but never own the URL, so NAVIGATE is a capability the host resolves'),
    e('frontend-system-design', 'explicit', 'Stage 1', 'The threat model, the capability catalogue and the degradation policy are the first three documents'),
    e('testing-react', 'explicit', 'Stage 3', 'Every attack in the threat model becomes a test, including one that must fail to read the host session'),
    e('tooling-bundlers', 'explicit', 'Stage 2', 'Plugin bundles are built separately with SRI hashes, and the host build must never inline plugin code'),
    e('web-http', 'explicit', 'Stage 3', 'A plugin fetch is proxied through the host, which enforces an allowlist and strips credentials'),
    e('web-how-page-loads', 'explicit', 'Stage 2', 'Plugin frames load after first paint, so an untrusted plugin can never delay the host critical path'),
    e('html-semantics', 'explicit', 'Stage 4', 'A described node maps to a real element, so plugin UI is accessible rather than a div canvas'),
    e('html-forms', 'explicit', 'Stage 4', 'Plugin-described forms are real forms the host owns, with native validation from the described constraints'),
    e('a11y-core', 'explicit', 'Stage 4', 'Heading levels and focus order are host-computed, since a plugin cannot know its surrounding context'),
    e('css-cascade', 'explicit', 'Stage 4', 'Cascade layers guarantee plugin styles can never outrank the host, regardless of specificity'),
    e('css-box-display', 'explicit', 'Stage 4', 'Each plugin surface is a containment context, so a plugin cannot force layout on the host tree'),
    e('css-positioning', 'explicit', 'Stage 4', 'A plugin may not create a full-screen overlay, so the stacking policy is host-owned and enforced'),
    e('css-selectors', 'explicit', 'Stage 4', 'Plugin styling is scoped by attribute and shadow boundary, so a selector cannot reach host DOM'),
    e('css-tokens-modern', 'explicit', 'Stage 4', 'Plugins receive token names, never colours, which is what stops one from impersonating host chrome'),
    e('css-states', 'explicit', 'Stage 4', 'Loading, sandboxed, degraded and terminated are host-rendered states a plugin cannot fake'),
    e('css-grid-tracks', 'implicit', 'Host', 'The plugin surface grid gives each plugin a bounded track it cannot grow beyond'),
    e('css-grid-placement', 'implicit', 'Host', 'Plugins request a slot by name and the host decides placement, so layout is never delegated'),
    e('css-grid-align', 'implicit', 'Host', 'Plugin cards align on both axes regardless of what each plugin renders inside its slot'),
    e('css-flex-axes', 'implicit', 'Host', 'The plugin chrome header is a flex row carrying the identity badge and the terminate control'),
    e('css-flex-sizing', 'implicit', 'Host', 'The plugin body grows within its bounded slot while host chrome holds its basis'),
    e('css-flex-align', 'implicit', 'Host', 'Auto margins keep the revoke control at the trailing edge of every plugin header'),
    e('css-units', 'implicit', 'Host', 'Slot sizes are in rem, and a plugin cannot specify pixels that would ignore the user font size'),
    e('css-ratio-logical', 'implicit', 'Host', 'Plugin slots reserve their box with aspect-ratio, so a slow plugin cannot shift the host layout'),
    e('css-media-container', 'implicit', 'Host', 'Plugins restyle by container query, since a plugin must never read the host viewport'),

    // ── Extended roadmap tracks ──
    e('rd-fe-js-v8-packages', 'explicit', 'Stage 2', 'Parsing, transforming and executing untrusted modules is V8-level work by definition'),
    e('rd-fe-internet-browser', 'explicit', 'Stage 1', 'Origins, iframes and the security boundary are the browser model as a load-bearing constraint'),
  ],
  exemptions: [
    {
      reason:
        'The extended roadmap tracks were added to the curriculum after this build was designed. These are the ones it does not genuinely exercise, and claiming them because the topic exists is the inflation this manifest is meant to prevent.',
      conceptIds: [
        'rd-react-components',
        'rd-react-hooks',
        'rd-react-state-mgmt',
        'rd-react-data-fetching',
        'rd-react-routing-forms',
        'rd-react-testing',
        'rd-react-rsc-compiler',
        'rd-perf-high-priority',
        'rd-perf-rendering-media',
        'rd-perf-web-vitals',
        'rd-perf-network-cdn',
        'rd-fe-html-web-components',
        'rd-fe-modern-css',
      ],
    },
    {
      reason:
        'These projects were designed before the frontend system-design track existed, and none of them chooses a transport under a stated workload — the thing the topic actually teaches. Claiming them here would be coverage by adjacency: the build fetches over HTTP, therefore it \'covers\' gRPC. A dedicated system-design project carries these edges instead.',
      conceptIds: [
        'rd-comm-protocols',
        'rd-comm-rest',
        'rd-comm-graphql',
        'rd-comm-grpc',
        'rd-comm-polling',
        'rd-comm-websocket-sse',
        'rd-comm-webhooks-webrtc',
      ],
    },
    {
      reason:
        'Security is taught by attacking and then defending a specific sink, and none of these builds has an untrusted-input surface, a session, or a cross-origin boundary to defend. A project that renders trusted local data cannot honestly claim to teach XSS.',
      conceptIds: [
        'rd-sec-overview',
        'rd-sec-xss',
        'rd-sec-sanitization',
        'rd-sec-csrf',
        'rd-sec-cors',
        'rd-sec-headers',
        'rd-sec-iframe',
        'rd-sec-authentication',
        'rd-sec-authorization',
        'rd-sec-https',
        'rd-sec-permissions-policy',
        'rd-sec-sri-deps',
        'rd-sec-ssrf-ssji',
        'rd-sec-client-storage',
      ],
    },
    {
      reason:
        'Client storage, HTTP caching and telemetry are properties of a deployed system under real traffic. These builds run locally against fixtures, so the invalidation and eviction behaviour that makes the topic hard never occurs.',
      conceptIds: [
        'rd-store-comparison',
        'rd-cache-http',
        'rd-cache-client',
        'rd-obs-telemetry',
        'rd-obs-alerting',
      ],
    },
  ],
};
