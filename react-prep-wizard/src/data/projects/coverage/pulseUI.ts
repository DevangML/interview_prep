import { e, type ProjectCoverage } from './types';

/**
 * Advanced — zero exemptions. An SDUI host renders arbitrary server-described
 * UI, so it must implement the whole platform surface: every primitive it can
 * be asked to render is a concept it has to get right.
 */
export const pulseUICoverage: ProjectCoverage = {
  projectId: 'project-pulseui',
  edges: [
    e('r19-use-rsc', 'explicit', 'Stage 3', 'Layout payloads stream from the BFF straight into Suspense boundaries as a Flight description'),
    e('react-errors-portals', 'explicit', 'Stage 3', 'Every federated slot is boundary-wrapped, so a rogue remote degrades one card instead of the host'),
    e('tooling-bundlers', 'explicit', 'Stage 3', 'Module Federation 2.0 resolves remotes at runtime with shared singletons and SRI-verified chunks'),
    e('js-modules', 'explicit', 'Stage 3', 'A remote is a dynamic import resolved at runtime, which is where module identity and dedupe become load-bearing'),
    e('ts-essentials', 'explicit', 'Stage 3', 'The node schema is a recursive discriminated union, and Zod parsing is what makes the type true at runtime'),
    e('web-security', 'explicit', 'Stage 3', 'Unregistered component types, javascript: URLs and unverified remotes are each rejected at the schema boundary'),
    e('web-cors', 'explicit', 'Stage 2', 'Remotes load cross-origin, so crossorigin="anonymous" and the header set decide whether errors are even readable'),
    e('css-grid-placement', 'explicit', 'Stage 4', 'Subgrid lets server-composed cards align their internal rows against the parent track list'),
    e('css-grid-align', 'explicit', 'Stage 4', 'Headers, bodies and CTAs align across dynamic columns the client did not choose'),
    e('css-grid-tracks', 'explicit', 'Stage 4', 'The page track list is auto-fit minmax, since the server decides how many cards arrive'),
    e('css-media-container', 'explicit', 'Stage 4', 'Container queries are mandatory: a server-driven card cannot know the slot width it will land in'),
    e('css-ratio-logical', 'explicit', 'Stage 2', 'Remote slots reserve their box before the remote loads, which is what removes the CLS spike'),
    e('react-composition', 'explicit', 'Stage 3', 'The component registry maps a node type to a component, which is composition driven by data rather than JSX'),
    e('react-rendering-model', 'explicit', 'Stage 3', 'Node ids become keys, so a reordered server layout preserves the state of the cards that stayed'),
    e('react-state', 'explicit', 'Stage 3', 'Host state and remote state are separated, because a remote unmount must not take host state with it'),
    e('react-effects', 'explicit', 'Stage 2', 'Remote teardown is explicit: unmount the root, remove listeners, revoke the URL, or the detached tree leaks'),
    e('react-hooks-rest', 'explicit', 'Stage 3', 'Context carries the action dispatcher to arbitrary depth, since the tree shape is not known at build time'),
    e('react-immutability', 'explicit', 'Stage 3', 'Layout trees are replaced rather than patched, so a diff can be computed against the previous payload'),
    e('react-references-copying', 'explicit', 'Stage 3', 'Node objects crossing the boundary are cloned, so a remote cannot mutate the host layout description'),
    e('react-perf', 'explicit', 'Stage 4', 'Impression tracking and remote hydration are measured, since SDUI trades bundle size for payload size'),
    e('react-class-lifecycle', 'explicit', 'Stage 3', 'The slot boundary is a class, and getDerivedStateFromError is what turns a remote crash into a fallback card'),
    e('r19-actions', 'explicit', 'Stage 3', 'Server-described forms map to Actions, so a server can ship a working form without shipping its handler'),
    e('redux-core', 'explicit', 'Stage 3', 'The command bus is a dispatcher over pure handlers, which is what makes server-driven actions auditable'),
    e('redux-react-toolkit', 'explicit', 'Stage 3', 'Host state uses createSlice, and remotes receive a scoped view rather than the whole store'),
    e('tooling-flux', 'explicit', 'Stage 3', 'One dispatcher, many isolated remotes — Flux is the only shape that survives untrusted subscribers'),
    e('state-alternatives', 'explicit', 'Stage 3', 'Server state, host state and remote-local state get three different mechanisms, and the split is documented'),
    e('router-core', 'explicit', 'Stage 3', 'NAVIGATE actions must delegate to the host router, since a remote cannot own the URL'),
    e('frontend-system-design', 'explicit', 'Stage 1', 'The contract between server and client is the design: what a node is, what happens to an unknown one'),
    e('testing-react', 'explicit', 'Stage 3', 'Contract tests assert that an unknown node type renders a fallback rather than throwing'),
    e('web-http', 'explicit', 'Stage 4', 'Cache-Control with stale-while-revalidate on the layout endpoint is what makes edge-cached SDUI viable'),
    e('web-storage', 'explicit', 'Stage 4', 'The last layout is cached so a returning user sees structure before the network answers'),
    e('web-how-page-loads', 'explicit', 'Stage 2', 'Streaming HTML plus deferred remotes is a critical-path design, measured as TTFB against LCP'),
    e('js-promises', 'explicit', 'Stage 2', 'Remote loading is a promise chain with timeout and fallback, since a hung remote must not hang the slot'),
    e('js-event-loop', 'explicit', 'Stage 2', 'Streamed chunks arrive across many tasks, so the host must render progressively rather than await the whole tree'),
    e('js-dom-events', 'explicit', 'Stage 3', 'The cross-remote event bus uses structured CustomEvents, with capture and composedPath across shadow boundaries'),
    e('js-scope-closures', 'explicit', 'Stage 3', 'Each slot closes over its own registry and abort handle, which is what makes two remotes independent'),
    e('js-this', 'explicit', 'Stage 3', 'The action dispatcher is handed to remotes as a bound function, or its receiver is lost at the boundary'),
    e('js-prototypes', 'explicit', 'Stage 3', 'Structured clone drops prototypes, so a class instance crossing the boundary arrives as a plain object'),
    e('js-arrays-objects', 'explicit', 'Stage 3', 'The layout AST is walked and transformed immutably, which is what makes the previous tree diffable'),
    e('js-polyfills', 'explicit', 'Stage 4', 'The impression tracker is a hand-written IntersectionObserver batcher with a sendBeacon flush'),
    e('js-types-coercion', 'explicit', 'Stage 3', 'Every server value arrives as JSON, so numbers, dates and booleans are converted once at the parse boundary'),
    e('js-equality-matrix', 'explicit', 'Stage 3', 'Deciding whether a node changed between payloads requires a stated sameness rule, not reference equality'),
    e('js-defaulting-operators', 'explicit', 'Stage 3', 'Optional node fields default with ??, since a server-sent 0 or empty string is meaningful data'),
    e('html-semantics', 'explicit', 'Stage 3', 'A node type maps to a real element, so server-driven UI does not degrade into nested divs'),
    e('html-forms', 'explicit', 'Stage 3', 'Server-described forms are real forms with native validation attributes carried in the payload'),
    e('a11y-core', 'explicit', 'Stage 3', 'Heading levels and landmarks must be computed from the tree, since the server does not know the surrounding context'),
    e('css-flex-axes', 'explicit', 'Stage 4', 'The Carousel primitive is a flex row with scroll snapping, one of the four primitives the host must render'),
    e('css-flex-sizing', 'explicit', 'Stage 4', 'ActionCard bodies grow so CTAs align, for content lengths the client never sees in advance'),
    e('css-flex-align', 'explicit', 'Stage 4', 'Auto margins pin card actions without the server needing to describe a spacer node'),
    e('css-box-display', 'explicit', 'Stage 4', 'Each slot establishes a containment context so a remote cannot escape its box'),
    e('css-positioning', 'explicit', 'Stage 4', 'Server-described overlays need a host-owned stacking policy, or one remote covers another'),
    e('css-cascade', 'explicit', 'Stage 4', 'Cascade layers keep remote styles from outranking host styles regardless of how specific the remote gets'),
    e('css-selectors', 'explicit', 'Stage 4', 'Styling hangs off data attributes derived from the node type, so unknown types still style predictably'),
    e('css-states', 'explicit', 'Stage 4', 'Loading, degraded and interactive states are host-owned, since a failed remote cannot style itself'),
    e('css-units', 'explicit', 'Stage 4', 'The spacing scale is in rem and the server sends scale steps rather than pixels'),
    e('css-tokens-modern', 'explicit', 'Stage 4', 'The server sends token names, not colours, which is what stops a remote from repainting the host brand'),

    // ── Extended roadmap tracks ──
    e('rd-react-rsc-compiler', 'explicit', 'Stage 2', 'Server components and the compiler are the substance of a server-driven UI orchestrator'),
    e('rd-react-state-mgmt', 'explicit', 'Stage 3', 'Federated state across micro-frontends is the hardest form of the roadmap state topic'),
  ],
  exemptions: [
    {
      reason:
        'The extended roadmap tracks were added to the curriculum after this build was designed. These are the ones it does not genuinely exercise, and claiming them because the topic exists is the inflation this manifest is meant to prevent.',
      conceptIds: [
        'rd-react-components',
        'rd-react-hooks',
        'rd-react-data-fetching',
        'rd-react-routing-forms',
        'rd-react-testing',
        'rd-perf-high-priority',
        'rd-perf-rendering-media',
        'rd-perf-web-vitals',
        'rd-perf-network-cdn',
        'rd-fe-internet-browser',
        'rd-fe-html-web-components',
        'rd-fe-modern-css',
        'rd-fe-js-v8-packages',
      ],
    },
    {
      reason:
        'These projects were designed before the frontend system-design track existed, and none of them chooses a transport under a stated workload — the thing the topic actually teaches. Claiming them here would be coverage by adjacency: the build fetches over HTTP, therefore it \'covers\' gRPC. A dedicated system-design project carries these edges instead.',
      conceptIds: [
        'rd-comm-protocols',
        'rd-comm-rest',
        'rd-comm-graphql',
        'rd-comm-grpc',
        'rd-comm-polling',
        'rd-comm-websocket-sse',
        'rd-comm-webhooks-webrtc',
      ],
    },
    {
      reason:
        'Security is taught by attacking and then defending a specific sink, and none of these builds has an untrusted-input surface, a session, or a cross-origin boundary to defend. A project that renders trusted local data cannot honestly claim to teach XSS.',
      conceptIds: [
        'rd-sec-overview',
        'rd-sec-xss',
        'rd-sec-sanitization',
        'rd-sec-csrf',
        'rd-sec-cors',
        'rd-sec-headers',
        'rd-sec-iframe',
        'rd-sec-authentication',
        'rd-sec-authorization',
        'rd-sec-https',
        'rd-sec-permissions-policy',
        'rd-sec-sri-deps',
        'rd-sec-ssrf-ssji',
        'rd-sec-client-storage',
      ],
    },
    {
      reason:
        'Client storage, HTTP caching and telemetry are properties of a deployed system under real traffic. These builds run locally against fixtures, so the invalidation and eviction behaviour that makes the topic hard never occurs.',
      conceptIds: [
        'rd-store-comparison',
        'rd-cache-http',
        'rd-cache-client',
        'rd-obs-telemetry',
        'rd-obs-alerting',
      ],
    },
  ],
};
