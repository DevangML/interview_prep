import { e, type ProjectCoverage } from './types';

export const infiniteFeedCoverage: ProjectCoverage = {
  projectId: 'service-infinite-feed',
  edges: [
    e('js-dom-events', 'explicit', 'Stage 1', 'The sentinel is an IntersectionObserver with a stated rootMargin, disconnected on unmount and guarded against re-entry'),
    e('react-effects', 'explicit', 'Stage 3', 'Scroll restoration runs in useLayoutEffect, because restoring after paint is a visible jump'),
    e('react-state', 'explicit', 'Stage 1', 'The in-flight guard is a ref the observer callback can read, and why a state variable fails there is stated'),
    e('react-hooks-rest', 'explicit', 'Stage 1', 'Refs hold the cursor and loading flag so the observer callback never closes over a stale value'),
    e('js-promises', 'explicit', 'Stage 1', 'Pages must append in request order, so a late page two cannot land after page three'),
    e('js-event-loop', 'explicit', 'Stage 1', 'Observer callbacks and fetch resolution interleave, which is what allows a duplicate request to slip through'),
    e('web-http', 'explicit', 'Stage 1', 'Cursor paging is compared against offset paging on a moving feed, with duplicate-and-skip demonstrated'),
    e('state-alternatives', 'explicit', 'Stage 3', 'Returning to the feed renders from cache, and refetching is shown not to be restoration'),
    e('router-core', 'explicit', 'Stage 3', 'Opening an item and pressing back is the acceptance test for the entire build'),
    e('css-ratio-logical', 'explicit', 'Stage 2', 'Media boxes reserve their space from an aspect ratio, which removes most of the layout-shift score'),
    e('react-rendering-model', 'explicit', 'Stage 1', 'Stable ids keep rendered cards mounted across an append so images are not re-decoded'),
    e('react-perf', 'explicit', 'Stage 2', 'The cost of an unreserved box is measured rather than argued, before and after the fix'),
    e('css-box-display', 'explicit', 'Stage 2', 'The list owns its containment so offscreen cards cost less paint as the feed grows'),
    e('js-arrays-objects', 'explicit', 'Stage 1', 'Pages concatenate into a new array, so a cached page is never edited in place'),
    e('ts-essentials', 'explicit', 'Stage 1', 'Feed state is one union, so an end-of-feed marker cannot render beside a loading spinner'),
    e('html-semantics', 'explicit', 'Stage 2', 'Cards are list items in a labelled region, so the structure survives with stylesheets disabled'),
    e('a11y-core', 'explicit', 'Stage 3', 'Page arrival is announced politely and the end state is reachable without scrolling by mouse'),
    e('testing-react', 'explicit', 'Stage 3', 'A test fires the sentinel repeatedly and proves the same cursor is never requested twice'),
    e('rd-react-data-fetching', 'explicit', 'Stage 1', 'Cursor paging, caching and the four terminal states are the roadmap data-fetching topic directly'),
    e('rd-react-hooks', 'explicit', 'Stage 1', 'Refs, effects and cleanup are what make an observer safe inside a component lifecycle'),
    e('rd-perf-web-vitals', 'explicit', 'Stage 2', 'Cumulative Layout Shift is recorded before and after the reservation fix and committed'),
    e('rd-perf-rendering-media', 'explicit', 'Stage 2', 'Image decode, reserved boxes and paint cost are the rendering-performance topic in its most common form'),
    e('rd-react-testing', 'explicit', 'Stage 3', 'The suite asserts user-visible guarantees rather than counting calls'),

    e('js-scope-closures', 'implicit', 'Feed list', 'Every load closes over its own cursor, or page two appends twice under the wrong key'),
    e('js-types-coercion', 'implicit', 'Scroll restore', 'The stored offset comes back as a string and must be parsed once, at the boundary'),
    e('js-defaulting-operators', 'implicit', 'Feed list', 'A null cursor means the end of the feed; an undefined one means it has not started'),
    e('react-immutability', 'implicit', 'Page cache', 'Mutating the cached pages makes the restored feed differ from the one the user left'),
    e('react-references-copying', 'implicit', 'Page cache', 'A shallow copy that shares card objects still lets a render corrupt the cache'),
    e('web-security', 'implicit', 'Media card', 'Feed content is user generated, so media URLs get a scheme allowlist and text is never set as HTML'),
    e('web-storage', 'implicit', 'Scroll restore', 'Offset and cursor persist to sessionStorage keyed by feed, so a reload is not a reset'),
    e('js-polyfills', 'implicit', 'Scroll restore', 'The scroll write is throttled, because persisting on every scroll event is a guaranteed jank source'),
    e('css-flex-axes', 'implicit', 'Media card', 'Avatar, text column and trailing timestamp lay out along one axis'),
    e('css-flex-sizing', 'implicit', 'Media card', 'The name grows and the timestamp holds its basis, so a long display name cannot push it off'),
    e('css-flex-align', 'implicit', 'Media card', 'The avatar aligns to the first line of a wrapped name rather than the centre of the card'),
    e('css-grid-tracks', 'implicit', 'Feed list', 'The page is a centred grid track with a maximum measure, because a full-width feed is unreadable'),
    e('css-grid-placement', 'implicit', 'Feed list', 'The sticky header and the list occupy named areas so the layout does not depend on source order'),
    e('css-grid-align', 'implicit', 'Feed list', 'Column alignment keeps the skeleton and the real card occupying the same box'),
    e('css-positioning', 'implicit', 'Skeletons', 'The retry toast anchors above the list, and sticky chrome behaves differently inside a containing block'),
    e('css-states', 'implicit', 'Media card', 'Card focus and hover are styled for keyboard use, since every card is a link'),
    e('css-tokens-modern', 'implicit', 'Token theme', 'Skeleton and card share exact heights because both read the same token'),
    e('css-cascade', 'implicit', 'Token theme', 'Theme overrides live in a layer so neither side escalates specificity'),
    e('css-selectors', 'implicit', 'Token theme', 'State attributes drive styling rather than class names toggled from JavaScript'),
    e('css-units', 'implicit', 'Token theme', 'Card spacing is relative, so the feed stays readable at increased browser font size'),
    e('css-media-container', 'implicit', 'Media card', 'The card responds to its own column width, so the same card works in a sidebar'),
    e('rd-fe-modern-css', 'implicit', 'Token theme', 'Custom properties, layers and container queries carry the styling without a framework'),
  ],
  exemptions: [
    {
      reason: 'A feed of cards has no class components, no prototype work and no dynamic receiver; the build is functions, refs and effects throughout.',
      conceptIds: ['js-this', 'js-prototypes', 'react-class-lifecycle'],
    },
    {
      reason: 'There is no store, no server runtime and no Flux history in a ten-hour list build. Each is covered where it is the actual subject of the project.',
      conceptIds: ['redux-core', 'redux-react-toolkit', 'tooling-flux', 'r19-actions', 'r19-use-rsc', 'rd-react-state-mgmt', 'rd-react-rsc-compiler'],
    },
    {
      reason: 'The feed has no forms, no composition puzzle worth claiming and no error-boundary story beyond a retry; asserting them would inflate the graph.',
      conceptIds: ['html-forms', 'react-composition', 'react-errors-portals', 'rd-react-routing-forms', 'rd-react-components'],
    },
    {
      reason: 'Bundling, module architecture, origin policy and system-level design are application-scale concerns that a single scrolling surface does not exercise.',
      conceptIds: ['js-modules', 'tooling-bundlers', 'web-cors', 'frontend-system-design'],
    },
    {
      reason: 'Resource priority, CDN behaviour and the full request path are the subject of the performance audit build, which measures them properly rather than in passing.',
      conceptIds: ['rd-perf-high-priority', 'rd-perf-network-cdn', 'web-how-page-loads', 'rd-fe-internet-browser'],
    },
    {
      reason: 'Web Components and package-level V8 analysis are untouched by a React feed built from primitives, and claiming them because the topic exists is the padding this file avoids.',
      conceptIds: ['rd-fe-html-web-components', 'rd-fe-js-v8-packages'],
    },
    {
      reason: 'Equality semantics never become load-bearing here: cards are keyed by a server id, so no comparison rule has to be invented or defended.',
      conceptIds: ['js-equality-matrix'],
    },
    {
      reason:
        'These projects were designed before the frontend system-design track existed, and none of them chooses a transport under a stated workload — the thing the topic actually teaches. Claiming them here would be coverage by adjacency: the build fetches over HTTP, therefore it \'covers\' gRPC. A dedicated system-design project carries these edges instead.',
      conceptIds: [
        'rd-comm-protocols',
        'rd-comm-rest',
        'rd-comm-graphql',
        'rd-comm-grpc',
        'rd-comm-polling',
        'rd-comm-websocket-sse',
        'rd-comm-webhooks-webrtc',
      ],
    },
    {
      reason:
        'Security is taught by attacking and then defending a specific sink, and none of these builds has an untrusted-input surface, a session, or a cross-origin boundary to defend. A project that renders trusted local data cannot honestly claim to teach XSS.',
      conceptIds: [
        'rd-sec-overview',
        'rd-sec-xss',
        'rd-sec-sanitization',
        'rd-sec-csrf',
        'rd-sec-cors',
        'rd-sec-headers',
        'rd-sec-iframe',
        'rd-sec-authentication',
        'rd-sec-authorization',
        'rd-sec-https',
        'rd-sec-permissions-policy',
        'rd-sec-sri-deps',
        'rd-sec-ssrf-ssji',
        'rd-sec-client-storage',
      ],
    },
    {
      reason:
        'Client storage, HTTP caching and telemetry are properties of a deployed system under real traffic. These builds run locally against fixtures, so the invalidation and eviction behaviour that makes the topic hard never occurs.',
      conceptIds: [
        'rd-store-comparison',
        'rd-cache-http',
        'rd-cache-client',
        'rd-obs-telemetry',
        'rd-obs-alerting',
      ],
    },
  ],
};
