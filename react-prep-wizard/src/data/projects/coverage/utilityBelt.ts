import { e, type ProjectCoverage } from './types';

/**
 * Intermediate — zero exemptions. Widened during the audit: the utilities now
 * ship inside a documented playground application with a live REPL, published
 * types and a test suite, so the library has a real consumer.
 */
export const utilityBeltCoverage: ProjectCoverage = {
  projectId: 'inter-utility-belt',
  edges: [
    e('js-polyfills', 'explicit', 'Stages 1-3', 'Every utility is written naively, broken by the interviewer follow-up, then rewritten to survive it'),
    e('js-this', 'explicit', 'Stage 2', 'myBind is implemented including the new-target case, which forces every receiver rule into the open'),
    e('js-scope-closures', 'explicit', 'Stage 2', 'debounce, throttle and curry are each a closure over accumulated state, with cancel proving the state is reachable'),
    e('js-promises', 'explicit', 'Stage 3', 'all, allSettled, race and any are implemented from the specification algorithm rather than from memory'),
    e('js-event-loop', 'explicit', 'Stage 3', 'Writing the combinators makes the microtask queue concrete: then callbacks drain before the next timer fires'),
    e('react-references-copying', 'explicit', 'Stage 2', 'deepClone must survive cycles, Map, Set, Date and RegExp, then be compared against structuredClone'),
    e('js-arrays-objects', 'explicit', 'Stage 2', 'groupBy, chunk, uniqBy and deepMerge are written without mutating any input'),
    e('js-prototypes', 'explicit', 'Stage 2', 'Distinguishing a plain object from a class instance requires reading the prototype, since typeof answers "object" for both'),
    e('js-equality-matrix', 'explicit', 'Stage 2', 'isEqual must decide NaN, -0, boxed primitives and Dates, which is all four sameness algorithms in one function'),
    e('js-types-coercion', 'explicit', 'Stage 2', 'A generic toNumber has to state its policy for "", null, [] and "0x10" rather than inherit the operator default'),
    e('js-defaulting-operators', 'implicit', 'Options', 'Every option object defaults with ?? so a deliberate 0 wait or false flag is not overwritten'),
    e('js-modules', 'implicit', 'Package', 'Named ESM exports with sideEffects: false are what make the package tree-shakeable, and the effect is measured'),
    e('js-dom-events', 'implicit', 'Playground', 'The REPL wires input, keyboard and paste listeners, which is what the timing utilities are demonstrated against'),
    e('ts-essentials', 'implicit', 'Package', 'curry and debounce are typed generically so argument types survive the wrapper — the hardest typing in the curriculum'),
    e('testing-react', 'implicit', 'Suite', 'Every follow-up question becomes a test, including fake timers for the timing utilities'),
    e('tooling-bundlers', 'implicit', 'Package', 'The package is built for ESM and CJS, and the bundle analyser proves which exports were shaken out'),
    e('tooling-flux', 'implicit', 'Playground', 'The playground event bus is a dispatcher, which is where compose and curry are first used for real'),
    e('react-composition', 'implicit', 'Playground', 'Each utility demo is a render-prop component so the same harness measures every one'),
    e('react-state', 'implicit', 'Playground', 'The REPL keeps an input log whose updates must batch, since a debounced run can emit several results at once'),
    e('react-effects', 'implicit', 'Playground', 'Every timing demo must clear its timer on unmount, which is the cancel API being used by its own consumer'),
    e('react-hooks-rest', 'implicit', 'Playground', 'useDebounced and useThrottled wrap the utilities, and useRef holds the mutable timer without rendering'),
    e('react-rendering-model', 'implicit', 'Playground', 'A throttled value re-rendering at a fixed rate makes render frequency directly observable'),
    e('react-immutability', 'implicit', 'Playground', 'The run history is appended immutably so the utilities cannot corrupt the harness that measures them'),
    e('react-perf', 'implicit', 'Playground', 'debounce and throttle are the measured fix for an expensive handler, with before-and-after INP'),
    e('react-errors-portals', 'implicit', 'Playground', 'A boundary catches a REPL expression that throws, so a bad input does not kill the page'),
    e('react-class-lifecycle', 'implicit', 'Playground', 'One demo is a class to show that the cancel-on-unmount obligation predates hooks'),
    e('r19-actions', 'implicit', 'Playground', 'The share-snippet form uses useActionState, which removes the pending flag the REPL otherwise hand-rolls'),
    e('r19-use-rsc', 'implicit', 'Docs', 'The documentation pages are server components, so the docs ship no JavaScript while the REPL does'),
    e('router-core', 'implicit', 'Playground', 'Each utility has its own URL and the REPL input is a search param, so a snippet is shareable'),
    e('redux-core', 'implicit', 'Playground', 'The run history is a hand-written reducer, which is where purity pays off for replaying a session'),
    e('redux-react-toolkit', 'implicit', 'Playground', 'The same history is rebuilt with createSlice to compare boilerplate against the hand-written version'),
    e('state-alternatives', 'implicit', 'Playground', 'The harness argues why a reducer beats context here, and where TanStack Query would be wrong'),
    e('frontend-system-design', 'implicit', 'Package', 'The public API surface is designed first: what is exported, what is stable, what may change'),
    e('html-forms', 'implicit', 'Playground', 'The REPL is a form with a labelled textarea, so Enter and Shift+Enter behave as a user expects'),
    e('html-semantics', 'implicit', 'Docs', 'Each utility page is an article with a heading outline, which is what makes the docs navigable'),
    e('a11y-core', 'implicit', 'Playground', 'Results are announced through a live region, and the REPL is fully operable without a mouse'),
    e('web-http', 'implicit', 'Playground', 'The retry and rate-limit utilities are demonstrated against a real endpoint with real status codes'),
    e('web-cors', 'implicit', 'Playground', 'The fetch wrapper is exercised cross-origin so preflight and credentialed requests are configured, not assumed'),
    e('web-storage', 'implicit', 'Playground', 'The REPL history persists, which exposes what the JSON round-trip does to undefined, NaN and Dates'),
    e('web-security', 'implicit', 'Playground', 'A REPL evaluates user input, so the sandbox boundary and output escaping are both required'),
    e('web-how-page-loads', 'implicit', 'Docs', 'The docs are static and the REPL is lazy, so the critical path carries no evaluator'),
    e('css-grid-tracks', 'implicit', 'Docs', 'The utility index is a grid whose columns follow the viewport rather than a fixed count'),
    e('css-grid-placement', 'implicit', 'Playground', 'Editor, output and timeline are named grid areas, so the layout is legible as a diagram'),
    e('css-grid-align', 'implicit', 'Docs', 'Cards align on both axes so signatures line up down the page and stay scannable'),
    e('css-flex-axes', 'implicit', 'Playground', 'The toolbar is a flex row and each demo card is a flex column'),
    e('css-flex-sizing', 'implicit', 'Playground', 'The editor grows and the timeline holds its height, which is a basis-versus-grow decision'),
    e('css-flex-align', 'implicit', 'Playground', 'Auto margins push the run button to the end of the toolbar for any toolbar content'),
    e('css-box-display', 'implicit', 'Playground', 'The output pane scrolls independently, which requires its own formatting context'),
    e('css-positioning', 'implicit', 'Playground', 'The timing timeline uses absolutely positioned markers against a relative track'),
    e('css-cascade', 'implicit', 'Docs', 'Prose styles must not leak into the REPL, resolved with cascade layers rather than escalating specificity'),
    e('css-selectors', 'implicit', 'Playground', ':has() styles a demo card differently once it has produced an error, with no extra class needed'),
    e('css-states', 'implicit', 'Playground', 'Running, cancelled and errored are three visible states on every demo'),
    e('css-units', 'implicit', 'Playground', 'The editor is sized in ch so a line of code has a predictable width'),
    e('css-ratio-logical', 'implicit', 'Docs', 'Diagrams reserve space with aspect-ratio so the docs do not shift as images load'),
    e('css-media-container', 'implicit', 'Playground', 'The demo card restacks by container width, since it appears both inline in docs and in the full REPL'),
    e('css-tokens-modern', 'implicit', 'Docs', 'Syntax-highlight colours are tokens, so the theme is one file rather than a rule per token type'),

    // ── Extended roadmap tracks ──
    e('rd-fe-js-v8-packages', 'explicit', 'Stage 1', 'Hand-written debounce, throttle and deep-clone are the language-level utilities the roadmap expects you to have built once'),
  ],
  exemptions: [
    {
      reason:
        'The extended roadmap tracks were added to the curriculum after this build was designed. These are the ones it does not genuinely exercise, and claiming them because the topic exists is the inflation this manifest is meant to prevent.',
      conceptIds: [
        'rd-react-components',
        'rd-react-hooks',
        'rd-react-state-mgmt',
        'rd-react-data-fetching',
        'rd-react-routing-forms',
        'rd-react-testing',
        'rd-react-rsc-compiler',
        'rd-perf-high-priority',
        'rd-perf-rendering-media',
        'rd-perf-web-vitals',
        'rd-perf-network-cdn',
        'rd-fe-internet-browser',
        'rd-fe-html-web-components',
        'rd-fe-modern-css',
      ],
    },
  ],
};
