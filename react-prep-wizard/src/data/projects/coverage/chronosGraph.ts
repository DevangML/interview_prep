import { e, type ProjectCoverage } from './types';

/**
 * Advanced — zero exemptions. A local-first knowledge IDE is an entire
 * application: editor, storage engine, sync layer, graph renderer and document
 * chrome, so the concept space is covered by construction.
 */
export const chronosGraphCoverage: ProjectCoverage = {
  projectId: 'project-chronosgraph',
  edges: [
    e('r19-use-rsc', 'explicit', 'Stage 3', 'use() suspends block rendering while an OPFS file stream resolves directly inside JSX'),
    e('state-alternatives', 'explicit', 'Stage 3', 'Offline mutations queue in TanStack Query and merge as CRDT diffs, which no client store does alone'),
    e('css-grid-placement', 'explicit', 'Stage 3', 'Subgrid aligns nested outliner rows so tags, authors and timestamps line up at every indent level'),
    e('react-perf', 'explicit', 'Stage 4', 'WGSL compute shaders move O(N²) force layout to the GPU, measured as frame time on a 10k-node graph'),
    e('web-storage', 'explicit', 'Stage 2', 'OPFS, IndexedDB and localStorage are each used where they belong, with the reasons written down'),
    e('react-immutability', 'implicit', 'CRDT', 'A CRDT is immutable history plus a merge function, so structural sharing is the storage model rather than an optimisation'),
    e('react-references-copying', 'implicit', 'CRDT', 'Merging two replicas requires deep structural comparison, since reference identity means nothing across a sync'),
    e('js-arrays-objects', 'implicit', 'CRDT', 'Operation logs are transformed and compacted with pure array operations that must never mutate history'),
    e('js-equality-matrix', 'implicit', 'CRDT', 'Convergence needs a total order over operations, which is a sameness and comparison rule stated explicitly'),
    e('js-promises', 'implicit', 'Sync', 'Sync is a cancellable async pipeline where a superseded pull must not overwrite a newer local edit'),
    e('js-event-loop', 'implicit', 'Editor', 'Keystrokes, sync and layout ticks compete for the main thread, so the yield points are chosen deliberately'),
    e('react-effects', 'implicit', 'Sync', 'Every subscription — file handle, broadcast channel, observer — has a mandatory teardown or the tab leaks'),
    e('react-state', 'implicit', 'Editor', 'Editor selection, document state and sync status are separate machines, since merging them makes undo impossible'),
    e('react-hooks-rest', 'implicit', 'Editor', 'useSyncExternalStore binds the CRDT document, and useRef holds the editor handle without rendering'),
    e('react-rendering-model', 'implicit', 'Outliner', 'Block ids are keys, so a remote insert does not destroy the local caret position'),
    e('react-composition', 'implicit', 'Blocks', 'Block types are registered and composed recursively, since a document is a tree of unknown depth'),
    e('react-errors-portals', 'implicit', 'Blocks', 'A corrupt block renders a recoverable card instead of taking the document down'),
    e('react-class-lifecycle', 'implicit', 'Blocks', 'The block boundary is a class, which remains the only way to catch a render error'),
    e('r19-actions', 'implicit', 'Commands', 'The command palette runs Actions, so pending and optimistic states come from the framework'),
    e('redux-core', 'implicit', 'Undo', 'Undo is a reducer over an operation log, which is the purest possible argument for pure reducers'),
    e('redux-react-toolkit', 'implicit', 'Undo', 'The log is managed with createSlice and Immer, and devtools replay is used to debug a real merge conflict'),
    e('tooling-flux', 'implicit', 'Commands', 'Every mutation is an action through one dispatcher, which is what makes the operation log complete'),
    e('router-core', 'implicit', 'Shell', 'A document and a graph selection are addressable URLs, so any view of the knowledge base is shareable'),
    e('frontend-system-design', 'explicit', 'Stage 1', 'Local-first is a design decision with a written conflict policy, not a library choice'),
    e('testing-react', 'implicit', 'CRDT', 'Convergence is property-tested: any interleaving of the same operations must produce the same document'),
    e('ts-essentials', 'implicit', 'Blocks', 'The block union and the operation union are exhaustive, so a new block type is a compile error everywhere it matters'),
    e('js-modules', 'implicit', 'Structure', 'The renderer, the CRDT and the storage engine are separate modules with worker boundaries between them'),
    e('js-scope-closures', 'implicit', 'Sync', 'Each sync cycle closes over its own abort handle and vector clock snapshot'),
    e('js-this', 'implicit', 'Storage', 'File system handles are objects with methods that throw when detached from their receiver'),
    e('js-prototypes', 'implicit', 'Storage', 'Structured clone into the worker drops prototypes, so rehydrating a document class is explicit work'),
    e('js-polyfills', 'implicit', 'Editor', 'Debounced persistence, batched rAF layout and a hand-written LRU cache are all required'),
    e('js-types-coercion', 'implicit', 'Import', 'Imported Markdown and JSON arrive untyped, so parsing happens once at the boundary with a stated policy'),
    e('js-defaulting-operators', 'implicit', 'Blocks', 'Optional block fields default with ??, since an empty title and a missing title mean different things'),
    e('js-dom-events', 'implicit', 'Editor', 'beforeinput, composition and paste events are what make a contenteditable outliner behave correctly'),
    e('web-http', 'implicit', 'Sync', 'The sync endpoint uses conditional requests and ETags, since sending the whole document every time does not scale'),
    e('web-cors', 'implicit', 'Sync', 'Sync targets another origin, so credentials and preflight are configured rather than discovered in production'),
    e('web-security', 'implicit', 'Import', 'Imported Markdown renders untrusted HTML, so sanitisation and a URL-scheme allowlist are mandatory'),
    e('web-how-page-loads', 'implicit', 'Shell', 'The local document renders before the network answers, which is the entire local-first performance claim'),
    e('html-semantics', 'implicit', 'Outliner', 'Blocks map to real headings, lists and quotes, so an exported document has a correct outline'),
    e('html-forms', 'implicit', 'Metadata', 'Document properties are a real form with native validation, editable inline'),
    e('a11y-core', 'implicit', 'Outliner', 'A tree view needs correct roles, level, expanded state and keyboard navigation, none of which come free'),
    e('css-grid-tracks', 'implicit', 'Shell', 'Sidebar, outliner and graph are three resizable grid tracks driven by custom properties'),
    e('css-grid-align', 'explicit', 'Outliner', 'Indent guides and block content align on both axes at every nesting depth'),
    e('css-flex-axes', 'implicit', 'Block', 'Each block row is a flex line: handle, content, then trailing metadata'),
    e('css-flex-sizing', 'implicit', 'Block', 'Content grows and metadata holds its basis, so long titles do not push timestamps off screen'),
    e('css-flex-align', 'implicit', 'Block', 'The drag handle aligns to the first text line, not the centre, for blocks of any height'),
    e('css-box-display', 'implicit', 'Shell', 'Each pane is its own scroll and containment context, or the graph canvas escapes its track'),
    e('css-positioning', 'implicit', 'Editor', 'The slash-command menu is positioned against a caret rect, which is a containing-block problem with no anchor element'),
    e('css-cascade', 'implicit', 'Themes', 'User themes layer over base styles without either side escalating specificity'),
    e('css-selectors', 'implicit', 'Outliner', ':has() styles a block that contains children, so the collapse affordance needs no extra class'),
    e('css-states', 'implicit', 'Blocks', 'Selected, editing, syncing and conflicted are four visible states, none of them colour-only'),
    e('css-units', 'implicit', 'Outliner', 'Indent steps are in ch so nesting aligns with the text rather than a fixed pixel step'),
    e('css-ratio-logical', 'implicit', 'Blocks', 'Embeds reserve their box with aspect-ratio so a syncing document never shifts under the caret'),
    e('css-media-container', 'implicit', 'Blocks', 'A block restyles by container width, since the same block renders in the outliner, a preview and the graph'),
    e('css-tokens-modern', 'implicit', 'Themes', 'Graph node colours derive from tokens with color-mix, so a new node type needs no new CSS'),
    e('tooling-bundlers', 'implicit', 'Workers', 'Worker and WASM entry points are separate bundler targets, and the WGSL shader is an asset the build must emit unmangled'),

    // ── Extended roadmap tracks ──
    e('rd-react-state-mgmt', 'explicit', 'Stage 3', 'Offline mutations, a client cache and a CRDT are three state systems that must agree'),
    e('rd-perf-rendering-media', 'explicit', 'Stage 4', 'GPU force layout and frame budget are the rendering-performance topic at its limit'),
  ],
  exemptions: [
    {
      reason:
        'The extended roadmap tracks were added to the curriculum after this build was designed. These are the ones it does not genuinely exercise, and claiming them because the topic exists is the inflation this manifest is meant to prevent.',
      conceptIds: [
        'rd-react-components',
        'rd-react-hooks',
        'rd-react-data-fetching',
        'rd-react-routing-forms',
        'rd-react-testing',
        'rd-react-rsc-compiler',
        'rd-perf-high-priority',
        'rd-perf-web-vitals',
        'rd-perf-network-cdn',
        'rd-fe-internet-browser',
        'rd-fe-html-web-components',
        'rd-fe-modern-css',
        'rd-fe-js-v8-packages',
      ],
    },
  ],
};
