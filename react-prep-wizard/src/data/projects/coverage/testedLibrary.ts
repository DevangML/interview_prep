import { e, type ProjectCoverage } from './types';

/**
 * Intermediate — zero exemptions. Widened during the audit: the library now
 * ships with a documentation site, a token pipeline and a demo application, so
 * routing, transport and state are exercised by real consumers of the package.
 */
export const testedLibraryCoverage: ProjectCoverage = {
  projectId: 'inter-tested-library',
  edges: [
    e('testing-react', 'explicit', 'Stages 1-3', 'Class-name tests are rewritten as role-and-name tests, and the network is mocked at the boundary with MSW'),
    e('ts-essentials', 'explicit', 'Stage 2', 'A polymorphic as prop is typed with generics and ComponentPropsWithoutRef, with no escape hatch to any'),
    e('a11y-core', 'explicit', 'Stage 2', 'Each component implements its WAI-ARIA authoring-practices keyboard contract, asserted in tests'),
    e('react-composition', 'explicit', 'Stage 2', 'Tabs and Combobox are compound components sharing implicit state, and every component exposes prop getters'),
    e('react-hooks-rest', 'explicit', 'Behaviour layer', 'Every behaviour is a custom hook over useRef, useContext and useReducer, with no DOM in the hook'),
    e('react-errors-portals', 'implicit', 'Dialog', 'Dialog and Toast render through portals, and a boundary keeps a failing consumer render out of the library'),
    e('react-effects', 'implicit', 'Dialog', 'Focus capture, scroll locking and outside-click listeners are all effects with mandatory teardown'),
    e('react-rendering-model', 'implicit', 'Combobox', 'Keys on filtered options decide whether highlight state follows the option or its position'),
    e('react-state', 'implicit', 'Combobox', 'Open, highlighted index and input value are correlated, so a reducer replaces three racing useState calls'),
    e('react-immutability', 'implicit', 'DataList', 'Sorting must copy before sort, since Array.prototype.sort mutates and would silently reorder the source'),
    e('react-references-copying', 'implicit', 'Prop getters', 'A prop getter returning a new object each render defeats every memo below it unless identities are stabilised'),
    e('react-perf', 'implicit', 'DataList', 'The list is virtualised and the render count is recorded before and after, not assumed'),
    e('react-class-lifecycle', 'implicit', 'Boundary', 'The error boundary is a class, which is the one place the legacy API is still mandatory'),
    e('r19-actions', 'implicit', 'Form components', 'useFormStatus lets the submit button read pending without a prop, which is exactly a library problem'),
    e('r19-use-rsc', 'implicit', 'Docs', 'Components are marked "use client" only where interactivity requires it, so the docs ship almost no JavaScript'),
    e('js-dom-events', 'implicit', 'Behaviour layer', 'Roving focus, type-ahead and outside-click all depend on event phases and composedPath'),
    e('js-scope-closures', 'implicit', 'Behaviour layer', 'Each hook closes over its own timer and last-focused node, which is what makes two instances independent'),
    e('js-this', 'implicit', 'Prop getters', 'A prop getter merged with a consumer handler must preserve the consumer receiver as well as call order'),
    e('js-prototypes', 'implicit', 'Polymorphism', 'Ref forwarding across an as prop requires knowing which element instance the ref will actually receive'),
    e('js-arrays-objects', 'implicit', 'DataList', 'Filter, sort and group are pure transformations, which is what makes them testable in isolation'),
    e('js-promises', 'implicit', 'Combobox', 'Async options need cancellation, or a slow search overwrites the results of a faster newer one'),
    e('js-event-loop', 'implicit', 'Dialog', 'Focus must move after the commit, which is why the microtask and the paint ordering matter here'),
    e('js-polyfills', 'implicit', 'Combobox', 'The type-ahead debounce and the id generator are both written rather than imported'),
    e('js-equality-matrix', 'implicit', 'Combobox', 'Matching the selected option needs a stated sameness rule once values are objects rather than strings'),
    e('js-types-coercion', 'implicit', 'Form components', 'A number input yields a string, so the library must state where conversion happens and not do it twice'),
    e('js-defaulting-operators', 'implicit', 'Props', 'Optional props default with ?? so a deliberate 0 or false from the consumer is never overridden'),
    e('js-modules', 'implicit', 'Package', 'Per-component entry points with sideEffects: false keep a consumer importing one button from pulling in the dialog'),
    e('html-semantics', 'implicit', 'Every component', 'Native elements are used wherever one exists, because ARIA is a fallback and not an upgrade'),
    e('html-forms', 'implicit', 'Form components', 'The field components integrate with native validation and participate in real form submission'),
    e('router-core', 'implicit', 'Docs', 'The docs site is routed, and the Link component must delegate to the router without hardcoding one'),
    e('redux-core', 'implicit', 'Toast queue', 'The toast queue is a reducer, since a queue with dedupe and expiry is exactly what reducers are for'),
    e('redux-react-toolkit', 'implicit', 'Demo app', 'The demo application wires the library into an RTK store to prove the components stay presentational'),
    e('state-alternatives', 'implicit', 'Docs', 'The library ships context-based state and documents why it does not require a store from its consumers'),
    e('tooling-flux', 'implicit', 'Toast queue', 'The queue is a single dispatcher fanning out to subscribers, which is Flux at component scale'),
    e('tooling-bundlers', 'implicit', 'Package', 'The build emits ESM and types, and the analyser proves per-component imports do not pull the whole package'),
    e('frontend-system-design', 'implicit', 'Package', 'The API surface, the versioning policy and the breaking-change rules are designed before the first component'),
    e('web-http', 'implicit', 'DataList', 'The async data component owns real status handling, which is what the MSW tests exercise'),
    e('web-cors', 'implicit', 'DataList', 'The demo API is a separate origin, so the fetch defaults the library ships have to be correct'),
    e('web-storage', 'implicit', 'Docs', 'Theme preference and the last-visited page persist, with a graceful path when storage is unavailable'),
    e('web-security', 'implicit', 'Docs', 'Component props render user content, so every escape hatch that accepts HTML is sanitised and documented'),
    e('web-how-page-loads', 'implicit', 'Docs', 'Font loading and component hydration are measured, since a component library owns a real share of LCP'),
    e('css-tokens-modern', 'implicit', 'Token layer', 'Every colour, space and radius is a custom property, which is what makes theming possible without forking'),
    e('css-cascade', 'implicit', 'Token layer', 'Cascade layers guarantee a consumer stylesheet always wins without either side escalating specificity'),
    e('css-selectors', 'implicit', 'Every component', 'Styling hangs off data attributes and ARIA state, so visual state cannot drift from accessible state'),
    e('css-states', 'implicit', 'Every component', 'hover, focus-visible, active, disabled and invalid are specified for every interactive component'),
    e('css-box-display', 'implicit', 'Dialog', 'The scroll container and stacking behaviour of the dialog body must be deliberate, not inherited'),
    e('css-positioning', 'implicit', 'Combobox', 'The popover is anchored against a relative trigger and must escape an overflow-hidden ancestor'),
    e('css-flex-axes', 'implicit', 'Button', 'Icon-and-label alignment is a one-axis problem solved once and reused by every component'),
    e('css-flex-sizing', 'implicit', 'Dialog', 'The dialog body grows while the header and footer hold their basis, for any content height'),
    e('css-flex-align', 'implicit', 'Toolbar', 'Auto margins separate leading and trailing action groups without a spacer element'),
    e('css-grid-tracks', 'implicit', 'DataList', 'Column widths are grid tracks with minmax, so a long cell does not collapse its neighbours'),
    e('css-grid-placement', 'implicit', 'Field', 'Label, control, hint and error are named grid areas, so every field lays out identically'),
    e('css-grid-align', 'implicit', 'DataList', 'Numeric columns align to the end and headers align to their data on both axes'),
    e('css-units', 'implicit', 'Token layer', 'The space scale is in rem so every component grows with the user font size'),
    e('css-ratio-logical', 'implicit', 'Avatar', 'aspect-ratio and logical properties mean the whole library mirrors under rtl with no extra rules'),
    e('css-media-container', 'implicit', 'Every component', 'Components restyle by container width, since a library cannot know the viewport its consumer will render into'),
  ],
  exemptions: [],
};
