import { e, type ProjectCoverage } from './types';

/**
 * Advanced — zero exemptions. An infinite collaborative canvas needs a full
 * application around the render loop: document chrome, an inspector, a share
 * flow and persistence, all of which the audit made explicit.
 */
export const hyperCanvasCoverage: ProjectCoverage = {
  projectId: 'project-hypercanvas',
  edges: [
    e('r19-actions', 'explicit', 'Stage 3', 'useOptimistic applies a transform locally before the peer confirms, and rolls back on rejection'),
    e('react-state', 'explicit', 'Stage 3', 'Selection, viewport and document state are three machines, because merging them makes undo unimplementable'),
    e('react-perf', 'explicit', 'Stage 2', 'All vector maths and draw calls move to a worker via OffscreenCanvas, measured as INP and long-task count'),
    e('web-how-page-loads', 'explicit', 'Stage 2', 'The frame budget is the design constraint: anything over 8ms on the main thread shows as a dropped frame'),
    e('js-event-loop', 'explicit', 'Stage 2', 'Input, rAF and worker messages are three queues, and the ordering between them is chosen rather than inherited'),
    e('react-references-copying', 'explicit', 'Stage 2', 'ArrayBuffers are transferred rather than cloned, which is zero-copy precisely because the sender loses the reference'),
    e('css-tokens-modern', 'explicit', 'Stage 4', 'The inspector palette is token-driven with color-mix, so a new tool needs no new rules'),
    e('css-media-container', 'explicit', 'Stage 4', 'The property inspector restyles by container width, since it docks left, right or as a sheet'),
    e('js-arrays-objects', 'implicit', 'Geometry', 'Hit testing and transforms are pure array maths over typed arrays, with no allocation inside the loop'),
    e('js-scope-closures', 'implicit', 'Tools', 'Each tool is a closure over its own gesture state, which is what allows tools to be swapped mid-drag'),
    e('js-this', 'implicit', 'Tools', 'Tool handlers are dispatched from a table, so a method detached from its tool loses its receiver'),
    e('js-prototypes', 'implicit', 'Shapes', 'Shape classes share methods on a prototype, and the monomorphic shape is what keeps the hot loop optimised'),
    e('js-promises', 'implicit', 'Assets', 'Image decode and font load are promises that must resolve before a shape can be measured'),
    e('js-polyfills', 'implicit', 'Input', 'Pointer coalescing, rAF batching and a hand-written throttle are what make the pen feel direct'),
    e('js-dom-events', 'implicit', 'Input', 'Pointer events with capture, plus getCoalescedEvents, are the difference between a smooth and a jagged stroke'),
    e('js-modules', 'implicit', 'Workers', 'The render worker is a real module, so every dependency is a static import the bundler can see'),
    e('js-equality-matrix', 'implicit', 'Geometry', 'Floating-point comparison needs an epsilon rule, because -0 and NaN both appear in real transform maths'),
    e('js-types-coercion', 'implicit', 'Inspector', 'Every inspector field is a string that must convert to a number with a stated policy for empty and invalid'),
    e('js-defaulting-operators', 'implicit', 'Inspector', 'A shape at x: 0 or opacity: 0 must not inherit a default, which is where || is actively wrong'),
    e('react-rendering-model', 'implicit', 'Inspector', 'React owns the chrome and the worker owns the canvas, so the render model applies to only half the app'),
    e('react-effects', 'implicit', 'Workers', 'Worker creation, port transfer and termination are effects whose teardown is not optional'),
    e('react-hooks-rest', 'implicit', 'Canvas', 'useSyncExternalStore subscribes to the document store, and useRef holds the canvas and worker handles'),
    e('react-immutability', 'implicit', 'Undo', 'The document history is immutable, which is what makes undo a pointer move rather than a recomputation'),
    e('react-composition', 'implicit', 'Inspector', 'Property editors are registered per shape type and composed, since the inspector shape is data-driven'),
    e('react-errors-portals', 'implicit', 'Canvas', 'A worker crash renders a recoverable canvas fallback rather than an empty white rectangle'),
    e('react-class-lifecycle', 'implicit', 'Canvas', 'The canvas boundary is a class, since a GPU context loss surfaces as a render error'),
    e('r19-use-rsc', 'implicit', 'Shell', 'The document list and share pages are server components, so the editor bundle carries no page chrome'),
    e('redux-core', 'implicit', 'Undo', 'Every edit is an action against a pure reducer, which is what makes history replayable and collaborative'),
    e('redux-react-toolkit', 'implicit', 'Undo', 'createSlice plus Immer keeps the reducer readable while the history stays structurally shared'),
    e('tooling-flux', 'implicit', 'Undo', 'One dispatcher for local and remote edits is what makes the two indistinguishable to the document'),
    e('state-alternatives', 'implicit', 'Architecture', 'The document store, React state and server state are three tools with a documented boundary'),
    e('router-core', 'implicit', 'Shell', 'Document id and viewport live in the URL, so a link reopens the same view of the same canvas'),
    e('frontend-system-design', 'explicit', 'Stage 1', 'The latency budget and the conflict policy are designed before any rendering code exists'),
    e('testing-react', 'implicit', 'Geometry', 'Geometry and reducers are property-tested, since a visual regression cannot tell you which transform was wrong'),
    e('ts-essentials', 'implicit', 'Shapes', 'The shape union is exhaustive, so adding a shape type is a compile error in every switch that matters'),
    e('tooling-bundlers', 'implicit', 'Workers', 'Worker and WASM entry points are separate build targets with their own chunking rules'),
    e('web-http', 'implicit', 'Persistence', 'Document saves are conditional requests, since a full upload per stroke is not viable'),
    e('web-cors', 'implicit', 'Assets', 'A cross-origin image taints the canvas and blocks readback, which is diagnosed rather than worked around'),
    e('web-storage', 'implicit', 'Persistence', 'The document autosaves to IndexedDB so a crash costs seconds, not the session'),
    e('web-security', 'implicit', 'Import', 'Imported SVG is untrusted markup, so it is parsed and rebuilt rather than injected'),
    e('html-semantics', 'implicit', 'Shell', 'The toolbar is a real toolbar with buttons, so the editor is operable before the canvas has painted'),
    e('html-forms', 'implicit', 'Inspector', 'Inspector fields are real inputs with native validation and units, not click-and-drag-only controls'),
    e('a11y-core', 'implicit', 'Canvas', 'A canvas is invisible to assistive tech, so a parallel accessible shape list and keyboard nudging are required'),
    e('css-grid-tracks', 'implicit', 'Shell', 'Toolbar, canvas and inspector are grid tracks whose widths are resizable custom properties'),
    e('css-grid-placement', 'implicit', 'Inspector', 'Property rows are placed on named lines so every editor type aligns identically'),
    e('css-grid-align', 'implicit', 'Inspector', 'Subgrid aligns labels and inputs across property groups of differing heights'),
    e('css-flex-axes', 'implicit', 'Toolbar', 'The toolbar is a flex row that becomes a column when docked vertically'),
    e('css-flex-sizing', 'implicit', 'Inspector', 'The property list grows while the shape preview holds its basis'),
    e('css-flex-align', 'implicit', 'Toolbar', 'Auto margins separate tool groups without spacer elements'),
    e('css-box-display', 'implicit', 'Canvas', 'The canvas needs its own containment context, or a large document forces layout on the whole page'),
    e('css-positioning', 'implicit', 'Overlays', 'Selection handles are absolutely positioned in a layer that must track the canvas transform exactly'),
    e('css-cascade', 'implicit', 'Themes', 'Cascade layers keep the inspector theme from leaking into embedded user content'),
    e('css-selectors', 'implicit', 'Inspector', 'Tool state drives styling through data attributes, so the DOM records the active tool'),
    e('css-states', 'implicit', 'Toolbar', 'Active tool, disabled tool and pending sync are three visible states independent of colour'),
    e('css-units', 'implicit', 'Inspector', 'The inspector is sized in rem while canvas coordinates stay in document units, and the two never mix'),
    e('css-ratio-logical', 'implicit', 'Shell', 'The canvas viewport reserves its box with aspect-ratio so a resize never reflows the chrome'),

    // ── Extended roadmap tracks ──
    e('rd-perf-rendering-media', 'explicit', 'Stage 3', 'Canvas paint cost, layer promotion and frame budget are the rendering topic in its purest form'),
  ],
  exemptions: [
    {
      reason:
        'The extended roadmap tracks were added to the curriculum after this build was designed. These are the ones it does not genuinely exercise, and claiming them because the topic exists is the inflation this manifest is meant to prevent.',
      conceptIds: [
        'rd-react-components',
        'rd-react-hooks',
        'rd-react-state-mgmt',
        'rd-react-data-fetching',
        'rd-react-routing-forms',
        'rd-react-testing',
        'rd-react-rsc-compiler',
        'rd-perf-high-priority',
        'rd-perf-web-vitals',
        'rd-perf-network-cdn',
        'rd-fe-internet-browser',
        'rd-fe-html-web-components',
        'rd-fe-modern-css',
        'rd-fe-js-v8-packages',
      ],
    },
  ],
};
