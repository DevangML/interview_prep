import{j as a}from"./vendor-editor-C_J04lKg.js";import{c as V,a as J,T as K,G as Z,e as B}from"./index-Cfg65pu3.js";import{r as G}from"./vendor-react-X2J0k6x4.js";import{u as Q,X as ee,S as te,C as ae,B as X,T as se}from"./useLibrary-Bz2ABjRX.js";import{C as ne}from"./circle-check-DbIZs4Tl.js";/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */const ie=[["path",{d:"M15 3h6v6",key:"1q9fwt"}],["path",{d:"M10 14 21 3",key:"gplh6r"}],["path",{d:"M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6",key:"a6xqqp"}]],oe=V("external-link",ie);/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */const re=[["path",{d:"m15.5 7.5 2.3 2.3a1 1 0 0 0 1.4 0l2.1-2.1a1 1 0 0 0 0-1.4L19 4",key:"g0fldk"}],["path",{d:"m21 2-9.6 9.6",key:"1j0ho8"}],["circle",{cx:"7.5",cy:"15.5",r:"5.5",key:"yqb3hr"}]],le=V("key",re),ce=[{id:"web-how-page-loads",area:"Web Platform",group:"How a page becomes pixels",title:"URL to pixels — the critical rendering path",status:"missing",minutes:8,summary:"Everything a front-end engineer optimises happens somewhere on the path from typing a URL to seeing pixels. Knowing the stages by name is what lets you say *where* a slowdown lives instead of guessing.",body:["The browser resolves the hostname through DNS, opens a TCP connection, negotiates TLS, then sends an HTTP request. Each of those is a round trip, which is why a cold connection to a distant origin costs far more than the bytes suggest. This is the reason CDNs exist: they move the origin closer, not make it faster.","The HTML arrives as a stream and the parser begins building the DOM immediately — it does not wait for the whole document. When the parser meets a `<script>` without `defer` or `async`, it **stops**: script can call `document.write`, so the parser cannot safely continue. This single fact explains why scripts belong at the end of the body or carry `defer`.","Stylesheets build the CSSOM. CSS is render-blocking by design — the browser will not paint content it might have to restyle a millisecond later, because flashing unstyled text is worse than waiting. DOM and CSSOM combine into the render tree, which contains only what is actually displayed (`display: none` subtrees are absent; `visibility: hidden` ones are present but invisible).","Then three steps that you will hear named constantly in performance work: **layout** (also called reflow) computes geometry — where every box sits and how large it is; **paint** fills in pixels for each box; **composite** assembles the painted layers onto the screen. Changing a geometric property forces all three. Changing only `transform` or `opacity` can often skip straight to composite, which is precisely why animation advice says to prefer them.",'Two events end the story. `DOMContentLoaded` fires when the HTML is parsed and deferred scripts have run — the DOM is usable. `load` fires when every sub-resource, including images, has finished. React apps mount on the first; perception of "loaded" by a user usually happens somewhere between the two.'],keyPoints:["A synchronous `<script>` blocks HTML parsing; `defer` runs after parsing in order, `async` runs whenever it arrives, out of order.","CSS blocks rendering, not parsing. JavaScript blocks parsing.","layout → paint → composite. `transform`/`opacity` can skip to composite; `width`/`top`/`margin` cannot.","`DOMContentLoaded` = DOM ready. `load` = every image and asset done."],interview:'"What happens when you type a URL and press enter?" is the most-asked open question in front-end interviews, and it is really a test of how many layers you can name without hand-waving. Say DNS → TCP → TLS → HTTP → parse → DOM+CSSOM → render tree → layout → paint → composite, then offer to go deeper on any stage.',pitfalls:['Saying "the browser renders top to bottom" — it streams, and it repaints many times.',"Believing `async` preserves order. It does not; only `defer` does."],resources:[{label:"MDN — Critical rendering path",url:"https://developer.mozilla.org/en-US/docs/Web/Performance/Critical_rendering_path",kind:"docs",note:"The canonical description, short enough to read in one sitting."},{label:"web.dev — How browsers work",url:"https://web.dev/articles/howbrowserswork",kind:"article",note:"Long, but the diagrams are the ones everyone quotes."},{label:"MDN — Script loading: defer vs async",url:"https://developer.mozilla.org/en-US/docs/Web/HTML/Element/script#defer",kind:"docs"}]},{id:"web-http",area:"Web Platform",group:"Network",title:"HTTP, status codes, methods and idempotency",status:"partial",minutes:7,summary:"REST questions are really HTTP questions. Interviewers probe whether you know which methods are safe to retry, and what a status code obliges a client to do.",body:["A method is **safe** if it does not change server state (`GET`, `HEAD`, `OPTIONS`). It is **idempotent** if doing it twice has the same effect as doing it once (`GET`, `PUT`, `DELETE`, `HEAD` — and notably **not** `POST`). This is not trivia: it decides whether your retry logic is allowed to retry. Retrying a failed `POST` may create two orders; retrying a failed `PUT` may not.","Status codes group by first digit: 2xx success, 3xx redirect, 4xx you were wrong, 5xx we were wrong. The ones that come up: **201** created (with a `Location` header), **204** no content, **301** permanent vs **302**/**307** temporary, **304** not modified (the caching one), **400** malformed, **401** unauthenticated vs **403** authenticated-but-forbidden, **404** absent, **409** conflict, **422** semantically invalid, **429** rate limited (respect `Retry-After`), **500** generic server fault, **503** unavailable.",'The 401/403 distinction is asked constantly and answered wrongly. **401 means "I do not know who you are"** — sending credentials might fix it. **403 means "I know exactly who you are and the answer is still no"** — sending credentials again will not help.',"Headers worth knowing by name: `Content-Type`, `Accept`, `Authorization`, `Cache-Control`, `ETag`/`If-None-Match`, `Last-Modified`/`If-Modified-Since`, `Set-Cookie`, and the CORS family (`Access-Control-Allow-Origin` and friends)."],keyPoints:["Idempotent methods are safe to retry automatically; `POST` is not.","401 = unauthenticated. 403 = authenticated but not allowed.",'304 means "use your cache" and carries no body — that is the point.',"429 usually carries `Retry-After`; honouring it is what separates a good client from a rude one."],interview:'Expect "difference between PUT and PATCH" (full replace vs partial update), "is POST idempotent" (no), and "what does 304 mean". In a system-design round the follow-up is always retry strategy — exponential backoff with jitter, and never for non-idempotent methods without an idempotency key.',resources:[{label:"MDN — HTTP response status codes",url:"https://developer.mozilla.org/en-US/docs/Web/HTTP/Status",kind:"docs"},{label:"MDN — HTTP request methods",url:"https://developer.mozilla.org/en-US/docs/Web/HTTP/Methods",kind:"docs"},{label:"MDN — HTTP caching",url:"https://developer.mozilla.org/en-US/docs/Web/HTTP/Caching",kind:"docs",note:"Read the ETag section twice; it is the one people fumble."}]},{id:"web-cors",area:"Web Platform",group:"Network",title:"CORS, the same-origin policy, and preflight",status:"missing",minutes:7,summary:"Every front-end engineer hits a CORS error in week one and half of them never learn what it actually means. It is not a bug in your fetch call; it is the browser refusing to hand you a response you were not authorised to read.",body:["An **origin** is the triple of scheme, host and port. `https://app.com` and `https://api.app.com` are different origins; so are `http://` and `https://` versions of the same host. The same-origin policy stops a page reading data from another origin, which is what prevents a malicious page from silently reading your webmail.",'CORS is the mechanism by which a server *opts out* of that restriction for specific callers. The browser sends an `Origin` header; the server answers with `Access-Control-Allow-Origin`. If they do not match, **the request usually still reached the server and ran** — the browser simply refuses to expose the response to your JavaScript. This is why "the API works in Postman" is never evidence of anything: Postman is not a browser and enforces no such policy.','For anything beyond a simple request the browser sends a **preflight**: an `OPTIONS` request asking permission before the real one. A request is "simple" only if it uses `GET`/`HEAD`/`POST`, carries no custom headers, and uses a basic content type (`text/plain`, `application/x-www-form-urlencoded`, `multipart/form-data`). Sending `Content-Type: application/json` — which every API client does — triggers preflight. That is the single most common cause of a mysterious extra `OPTIONS` in the network tab.','Credentials add another layer. To send cookies cross-origin you need `credentials: "include"` on the client **and** `Access-Control-Allow-Credentials: true` on the server — and in that mode the server may **not** answer with the wildcard `*`; it must name the exact origin.'],keyPoints:["CORS protects the *reader*, not the server. The request typically still executed.","`Content-Type: application/json` makes a request non-simple and triggers an `OPTIONS` preflight.","With credentials, `Access-Control-Allow-Origin: *` is illegal — the origin must be named.",'"It works in Postman" proves nothing about CORS.'],interview:'Asked as "you got a CORS error, what do you do?". The strong answer names where the fix lives: on the **server**, in response headers — or in development, behind a dev-server proxy so the browser sees a same-origin request. Anything you can change in the front-end code alone is a workaround, not a fix.',pitfalls:["Trying to fix CORS by changing fetch options. You cannot; the server decides.","Confusing CORS with CSRF. CORS relaxes a restriction; CSRF exploits one."],resources:[{label:"MDN — CORS",url:"https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS",kind:"docs",note:"The preflight flowchart is worth committing to memory."},{label:"MDN — Same-origin policy",url:"https://developer.mozilla.org/en-US/docs/Web/Security/Same-origin_policy",kind:"docs"}]},{id:"web-storage",area:"Web Platform",group:"Storage",title:"Cookies, localStorage, sessionStorage, IndexedDB",status:"missing",minutes:6,summary:"Four storage mechanisms with different lifetimes, sizes, and security properties. Choosing wrongly is how tokens end up readable by any script on the page.",body:["**Cookies** are sent to the server on every matching request, which makes them the only option for something the server must see — a session id. They are small (~4KB), and their security comes from flags: `HttpOnly` makes them invisible to JavaScript (defeating XSS token theft), `Secure` restricts them to HTTPS, and `SameSite` (`Lax`, `Strict`, `None`) controls whether they ride along on cross-site requests — the main CSRF defence.","**localStorage** is roughly 5–10MB, synchronous, string-only, and persists until explicitly cleared. Synchronous matters: a large read blocks the main thread. It is **readable by any JavaScript on the page**, so storing a bearer token there means any successful XSS is a full account takeover.","**sessionStorage** has the same API and limits but is scoped to a single tab and cleared when that tab closes. Useful for wizard progress that should not leak between tabs.","**IndexedDB** is the real database: asynchronous, transactional, indexed, structured-clone capable (so `Date`, `Blob`, `Map` survive), and large. Its raw API is unpleasant enough that most people reach for a wrapper such as `idb`. It is what you use for offline data or anything past a few megabytes.","A newer entry worth naming: the **Cache API**, used with service workers to store whole HTTP responses. It is what makes offline-first PWAs possible."],keyPoints:["Only cookies are automatically sent to the server.","`HttpOnly` + `Secure` + `SameSite` are the three flags that matter.","localStorage is synchronous and script-readable — never put a token you cannot afford to lose in it.","IndexedDB survives structured clone: Dates and Blobs come back as Dates and Blobs."],interview:'"Where do you store a JWT?" is a trap with a correct answer: an `HttpOnly`, `Secure`, `SameSite` cookie, because localStorage exposes it to XSS. If the interviewer pushes back with "but then CSRF" — that is the right conversation, and `SameSite` plus a CSRF token is the answer.',resources:[{label:"MDN — Web Storage API",url:"https://developer.mozilla.org/en-US/docs/Web/API/Web_Storage_API",kind:"docs"},{label:"MDN — Using HTTP cookies",url:"https://developer.mozilla.org/en-US/docs/Web/HTTP/Cookies",kind:"docs"},{label:"MDN — IndexedDB API",url:"https://developer.mozilla.org/en-US/docs/Web/API/IndexedDB_API",kind:"docs"}]},{id:"web-security",area:"Web Platform",group:"Security",title:"XSS, CSRF, clickjacking and CSP",status:"missing",minutes:8,summary:"The four attacks a front-end engineer is expected to name, and the specific defence for each. Getting XSS and CSRF the wrong way round is a common and costly slip.",body:['**XSS (cross-site scripting)** is attacker JavaScript running in your origin. Stored XSS lives in your database, reflected XSS bounces off a URL parameter, DOM XSS never touches the server at all — it comes from writing untrusted input into a dangerous sink like `innerHTML`. The defence is to **escape on output** and to avoid the sinks. React escapes text by default, which is why XSS in React almost always traces to `dangerouslySetInnerHTML`, an attacker-controlled `href="javascript:..."`, or unsanitised HTML from a CMS.','**CSRF (cross-site request forgery)** is the opposite shape: the attacker cannot read anything, but makes *your* browser send an authenticated request — an image tag pointing at a "delete account" endpoint. It works precisely because cookies are attached automatically. Defences: `SameSite` cookies, a CSRF token the attacker cannot read, and never using `GET` for state changes.',"**Clickjacking** frames your page invisibly over a decoy so the victim clicks something they cannot see. Defence: `X-Frame-Options: DENY` or the modern `Content-Security-Policy: frame-ancestors`.","**CSP (Content Security Policy)** is a response header telling the browser which sources of script, style, image and frame are permitted. A good CSP turns a would-be XSS into a blocked request. `unsafe-inline` defeats most of the benefit, which is why nonces or hashes are preferred.",'One more that catches React developers: `target="_blank"` without `rel="noopener"` lets the opened page reach back through `window.opener`. Modern browsers default to `noopener`, but interviewers still ask.'],keyPoints:["XSS = attacker runs script in your origin. CSRF = attacker uses your credentials without reading anything.","React escapes by default; `dangerouslySetInnerHTML` is the deliberate hole.","`SameSite=Lax` blocks most CSRF for free.","CSP with `unsafe-inline` is mostly decorative."],interview:'Almost guaranteed in any interview above junior level: "how does React protect against XSS, and where does that protection stop?" The answer: it escapes interpolated values, and it stops at `dangerouslySetInnerHTML`, `javascript:` URLs, and anything injected outside React.',resources:[{label:"OWASP — XSS prevention cheat sheet",url:"https://cheatsheetseries.owasp.org/cheatsheets/Cross_Site_Scripting_Prevention_Cheat_Sheet.html",kind:"article",note:"The reference everyone cites; skim the sink list."},{label:"OWASP — CSRF prevention cheat sheet",url:"https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html",kind:"article"},{label:"MDN — Content Security Policy",url:"https://developer.mozilla.org/en-US/docs/Web/HTTP/CSP",kind:"docs"}]}],de=[{id:"html-semantics",area:"HTML",group:"Structure",title:"Semantic elements and document outline",status:"missing",minutes:6,summary:'Semantic HTML is not stylistic preference. Each element carries an implicit ARIA role, keyboard behaviour and screen-reader affordance that a `div` does not — which is why "just use a div" quietly breaks accessibility.',body:["Landmarks give assistive technology a way to jump around: `<header>`, `<nav>`, `<main>` (exactly one per page), `<aside>`, `<footer>`, `<section>` (needs an accessible name to be a landmark), `<article>` (independently distributable content). A screen-reader user navigates by landmark the way you navigate by scrolling.","Headings `<h1>`–`<h6>` form the document outline, and users of assistive technology navigate by them more than by any other mechanism. Do not skip levels for visual size — that is what CSS is for. One `<h1>` per page is the safe convention.",'Interactive elements come with behaviour you would otherwise have to rebuild by hand: `<button>` is focusable, activates on Enter *and* Space, and announces as a button. A `<div onClick>` has none of that — no focus, no keyboard, no role. Rebuilding it correctly takes `tabindex="0"`, a `role`, and key handlers for both Enter and Space, and people almost always forget Space.','`<a>` versus `<button>`: an anchor **navigates** and belongs in the browser history; a button **acts**. If it changes the URL, use an anchor. If it opens a modal, use a button. Right-click, middle-click and "open in new tab" only work correctly on real anchors with a real `href`.',"Lists (`<ul>`, `<ol>`, `<dl>`) are announced with their item count, which is genuinely useful information a stack of divs cannot convey. Tables need `<caption>`, `<th scope>` and `<thead>`/`<tbody>` to be navigable; a table used for layout is actively hostile to screen readers."],keyPoints:["Every semantic element carries a free implicit ARIA role — that is the whole argument.","`<button>` fires on Enter *and* Space. Hand-rolled div buttons usually forget Space.","Anchors navigate; buttons act.","Exactly one `<main>`; do not skip heading levels for visual reasons."],interview:'"Why use semantic HTML?" is a screening question — a vague answer about SEO marks you as inexperienced. Name the implicit role, the keyboard behaviour, and the landmark navigation. Then mention that it reduces the amount of ARIA you have to write, which leads neatly into the first rule of ARIA.',resources:[{label:"MDN — HTML elements reference",url:"https://developer.mozilla.org/en-US/docs/Web/HTML/Element",kind:"docs"},{label:"HTML spec — sections and outlines",url:"https://html.spec.whatwg.org/multipage/sections.html",kind:"spec"},{label:"web.dev — Semantic HTML for accessibility",url:"https://web.dev/learn/accessibility/structure",kind:"article"}]},{id:"html-forms",area:"HTML",group:"Forms",title:"Forms, labels, validation and submission",status:"partial",minutes:7,summary:"Forms are the most-asked machine-coding topic and the most commonly broken piece of accessibility. The platform gives you validation, labelling and submission behaviour for free, and most hand-rolled React forms throw all three away.",body:['Every input needs an accessible name, and the robust way is a real `<label for="id">` — clicking the label focuses the control, which is a usability win as well as a screen-reader requirement. `aria-label` works but is invisible to sighted users, and placeholder text is **not** a label: it disappears the moment typing starts.',"The `type` attribute changes the mobile keyboard, the validation rules and sometimes the widget: `email`, `tel`, `url`, `number`, `search`, `date`. Attribute-based constraints — `required`, `min`, `max`, `minlength`, `pattern`, `step` — are enforced by the browser for free and expose `:invalid`, `:valid` and `:user-invalid` for styling. `:user-invalid` is the good one: it only matches after the user has actually interacted, so a pristine form is not painted red.",'Submission has behaviour people rebuild unnecessarily: a `<button type="submit">` inside a form submits on Enter from any text input. That is why a button with no `type` inside a form is a classic bug — the default **is** `submit`, so a "Cancel" button silently submits. Always type your buttons.',"In React, an input is **controlled** when its `value` comes from state and changes only through `onChange`; **uncontrolled** when the DOM owns the value and you read it with a ref or from `FormData` on submit. Controlled gives you validation-as-you-type and derived UI; uncontrolled is less code and fewer renders. React 19 pushes toward form actions, where a `<form action={fn}>` receives `FormData` directly."],keyPoints:["A placeholder is not a label.",'A `<button>` inside a form defaults to `type="submit"`. Always set it explicitly.',"`:user-invalid` beats `:invalid` for styling, because it waits for interaction.","Controlled = React state is the source of truth; uncontrolled = the DOM is."],interview:'The machine-coding staple is "build a form with validation". Points are won by using a real `<form>` with `onSubmit` (so Enter works), labelling every input, disabling the submit button while pending, and showing errors that are associated with their field via `aria-describedby`.',code:`<form onSubmit={handleSubmit} noValidate>
  <label htmlFor="email">Email</label>
  <input
    id="email" name="email" type="email" required
    aria-describedby={error ? 'email-error' : undefined}
    aria-invalid={!!error}
  />
  {error && <p id="email-error" role="alert">{error}</p>}
  <button type="submit" disabled={pending}>
    {pending ? 'Sending…' : 'Send'}
  </button>
</form>`,resources:[{label:"MDN — Client-side form validation",url:"https://developer.mozilla.org/en-US/docs/Learn/Forms/Form_validation",kind:"docs"},{label:"React — <form> and form actions",url:"https://react.dev/reference/react-dom/components/form",kind:"docs"},{label:"MDN — FormData",url:"https://developer.mozilla.org/en-US/docs/Web/API/FormData",kind:"docs"}]},{id:"a11y-core",area:"Accessibility",group:"Foundations",title:"ARIA, keyboard navigation and focus management",status:"missing",minutes:8,summary:"Accessibility is a scored dimension in senior front-end interviews and completely absent from most candidates' preparation. The rules are few, specific, and easy to state.",body:['**The first rule of ARIA is not to use ARIA.** If a native element with the required semantics exists, use it. `role="button"` on a div is strictly worse than a `<button>`, because you must then reimplement focus, Enter, Space, and the disabled state by hand. ARIA adds semantics; it never adds behaviour.','The three ARIA families: **roles** say what a thing is (`role="dialog"`), **properties** describe it (`aria-label`, `aria-describedby`, `aria-haspopup`), and **states** say what it is doing right now (`aria-expanded`, `aria-checked`, `aria-disabled`, `aria-busy`). States must be kept in sync with reality — a stale `aria-expanded` is worse than none.','Keyboard support is the part that gets tested by simply unplugging the mouse. Everything interactive must be reachable by Tab in a sensible order, must show a visible focus ring (never `outline: none` without a replacement), and composite widgets have expected key behaviour: arrows move within a menu or tab list, Escape closes, Enter/Space activate. `tabindex="0"` puts an element in the natural order; `tabindex="-1"` makes it focusable only programmatically; positive values break the order and should be avoided.',"**Focus management** is what separates a working modal from a broken one: on open, move focus into the dialog; trap Tab inside while it is open; on close, return focus to the element that opened it. Without the last step the user is dumped at the top of the document. The `<dialog>` element and `showModal()` now do most of this natively.",'Live regions announce changes that happen away from focus: `aria-live="polite"` waits for a pause, `assertive` interrupts. A result count that updates as you filter should be polite; an error that blocks submission may be assertive. `role="alert"` is an assertive live region with one attribute.'],keyPoints:["First rule of ARIA: do not use ARIA. Use the native element.","ARIA changes semantics, never behaviour — you still write the key handlers.","Modal contract: focus in, trap, and return focus on close.","Never remove a focus indicator without providing a better one."],interview:'"Make this accessible" applied to a custom dropdown or modal is a standard senior exercise. Say the contract out loud as you build: role, accessible name, keyboard model, focus management, live announcements. Naming the contract scores even when time runs out before you finish.',pitfalls:["`outline: none` in a reset with no replacement — an instant fail on a keyboard pass.","Putting `aria-label` on a `<div>` and expecting it to be announced. Without a role, it usually is not."],resources:[{label:"W3C — ARIA Authoring Practices (APG)",url:"https://www.w3.org/WAI/ARIA/apg/patterns/",kind:"spec",note:"The keyboard model for every widget you will be asked to build."},{label:"MDN — ARIA basics",url:"https://developer.mozilla.org/en-US/docs/Learn/Accessibility/WAI-ARIA_basics",kind:"docs"},{label:"web.dev — Learn Accessibility",url:"https://web.dev/learn/accessibility",kind:"article",note:"A full free course; the focus-management module is the one to read."}]}],he=[{id:"css-cascade",area:"CSS",group:"The cascade",title:"Cascade, specificity, inheritance and `!important`",status:"partial",minutes:8,summary:'When two rules fight, four things decide the winner — in a fixed order. Most "CSS is unpredictable" complaints are this order not being known.',body:["The cascade resolves conflicts in this order: **origin and importance** first, then **specificity**, then **source order**. Author styles beat user-agent defaults; `!important` flips the origin ordering, which is why an `!important` in a user stylesheet beats an `!important` in yours.","Specificity is counted as three numbers — **(ids, classes, elements)**. Inline `style` sits above all of them, and `!important` above that. `#nav .item a` is (1,1,1); `.item.active` is (0,2,0) and therefore loses to any id. The universal selector `*` and combinators (`>`, `+`, `~`) add nothing. Crucially, specificity is compared **left to right, not summed** — (1,0,0) beats (0,99,0), because eleven classes never add up to an id.","`:is()` and `:not()` take the specificity of their **most specific argument**; `:where()` always counts **zero**, which is what makes it the correct tool for library defaults you want consumers to override without a fight.","**Inheritance** is separate from the cascade. Some properties inherit by default (`color`, `font-*`, `line-height`, `visibility`, `cursor`); most do not (`margin`, `padding`, `border`, `background`, `display`). The keywords `inherit`, `initial`, `unset` (inherit if inheritable, else initial), `revert` (back to the previous origin) and `all: unset` let you control it explicitly.","`@layer` is the modern answer to specificity wars: layers are compared **before** specificity, so a rule in a later layer beats a more specific rule in an earlier one. Declaring `@layer reset, base, components, utilities` once gives you a predictable ordering that no amount of nesting can disturb."],keyPoints:["Order: origin/importance → specificity → source order.","Specificity is (ids, classes, elements), compared left to right — never summed.","`:where()` has zero specificity; `:is()` takes its most specific argument.","`@layer` outranks specificity entirely."],interview:'"Which rule wins?" with two selectors is a standard warm-up. Count out loud as a triple. The senior follow-up is "how do you avoid specificity wars" — answer `@layer`, `:where()` for defaults, and a flat class-based convention rather than deep nesting.',code:`#main .card p  →  (1,1,1)
.card.active   →  (0,2,0)   loses: ids beat any number of classes
:where(.card) p → (0,0,1)   the :where part contributes nothing
@layer base { a { color: red } }
@layer theme { a { color: blue } }   /* blue wins — later layer */`,resources:[{label:"MDN — Cascade, specificity and inheritance",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/Cascade",kind:"docs"},{label:"MDN — Specificity",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/Specificity",kind:"docs"},{label:"MDN — @layer",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/@layer",kind:"docs"},{label:"Specificity calculator",url:"https://specificity.keegan.st/",kind:"practice"}]},{id:"css-selectors",area:"CSS",group:"Selectors",title:"Selectors, combinators, pseudo-classes, pseudo-elements, attributes",status:"partial",minutes:7,summary:"The full selector vocabulary, including the attribute and data-attribute forms that make state-driven styling possible without extra classes.",body:["**Combinators** describe relationships: `A B` (descendant, any depth), `A > B` (direct child), `A + B` (immediately next sibling), `A ~ B` (any later sibling). There is no parent or previous-sibling combinator — `:has()` fills that role.",'**Attribute selectors** match on markup rather than classes: `[disabled]` (present), `[type="text"]` (exact), `[href^="https"]` (starts with), `[href$=".pdf"]` (ends with), `[class*="col-"]` (contains), `[lang|="en"]` (exactly or hyphen-prefixed), and `[data-state="open" i]` for case-insensitive matching.','**Data attributes** are the idiomatic way to express component state in markup — `data-state="loading"`, `data-variant="primary"` — and style it with `[data-state="loading"] { … }`. It keeps state visible in the DOM (helpful in DevTools and in tests) and avoids the class-name juggling that `classnames` exists to manage. In JavaScript they are `el.dataset.state`.',"**Pseudo-classes** describe state: `:hover`, `:focus`, `:focus-visible`, `:focus-within`, `:active`, `:disabled`, `:checked`, `:required`, `:valid`/`:invalid`/`:user-invalid`, `:first-child`, `:last-child`, `:only-child`, `:nth-child(an+b)`, `:nth-of-type`, `:empty`, `:target`. `:nth-child(2n+1)` is odd rows; `:nth-last-child` counts from the end.",'**Pseudo-elements** create sub-elements: `::before`, `::after` (require `content`, even `content: ""`), `::first-line`, `::first-letter`, `::marker` (list bullets), `::selection`, `::placeholder`, `::backdrop`. One colon is the legacy form; two is correct.',"**`:has()`** is the parent selector CSS lacked for twenty years: `.card:has(img)` styles the card *because of* its contents, and `label:has(input:checked)` styles a label from its input's state. It is now supported everywhere and quietly removes a great deal of JavaScript."],keyPoints:["Four combinators: descendant, child `>`, adjacent `+`, general sibling `~`.","`::before`/`::after` render nothing without a `content` property.","`data-*` + attribute selectors express component state without class juggling.","`:has()` selects a parent by its children."],interview:'Expect "select every second row", "style a label when its checkbox is checked" (`:has()` or `:checked + label`), and "difference between `:focus` and `:focus-visible`" — the latter matches only when the browser judges a focus ring appropriate, i.e. keyboard rather than mouse.',code:`.row:nth-child(2n)          { background: #f8fafc; }
[data-state="loading"] .spin { animation: spin 1s linear infinite; }
.card:has(> img)             { padding-block-start: 0; }
label:has(input:checked)     { font-weight: 700; }
a[href$=".pdf"]::after       { content: " (PDF)"; }`,resources:[{label:"MDN — CSS selectors",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Selectors",kind:"docs"},{label:"MDN — :has()",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/:has",kind:"docs"},{label:"MDN — Using data attributes",url:"https://developer.mozilla.org/en-US/docs/Learn/HTML/Howto/Use_data_attributes",kind:"docs"},{label:"CSS Diner",url:"https://flukeout.github.io/",kind:"practice",note:"Thirty-two levels of selector drilling. Half an hour well spent."}]},{id:"css-box-display",area:"CSS",group:"Box model",title:"Box model, display types, formatting contexts and `flow-root`",status:"covered",minutes:8,summary:"What a box measures, what `display` really sets, and why a container sometimes refuses to contain its own children.",body:["`box-sizing: content-box` (the default) means `width` sizes the **content only** — padding and border are added on top, so `width: 200px; padding: 1rem; border: 2px` occupies 236px. `border-box` makes `width` the **total**, and padding eats inward. The universal reset exists because border-box is what people mean nearly every time.","`display` sets **two** things: the outer role (how the box behaves among siblings — `block`, `inline`, `inline-block`) and the inner layout (how its children are laid out — `flow`, `flow-root`, `flex`, `grid`). The modern two-value syntax makes it explicit: `display: inline flex` is what the legacy `inline-flex` meant.","A **Block Formatting Context (BFC)** is an independent layout region. Inside one, floats are contained, and **margins do not collapse across its boundary**. `display: flow-root` creates one with no side effects — it is the modern, honest replacement for the clearfix hack and for `overflow: hidden` used purely to contain floats.","**Margin collapsing** is the behaviour that surprises everyone: adjacent vertical margins between siblings collapse to the larger of the two (not the sum), a parent and its first/last child collapse together unless separated by padding, border or a new BFC, and an empty element's own top and bottom margins collapse into one. Horizontal margins never collapse, and margins never collapse inside flex or grid containers — which is one reason `gap` is easier to reason about.","`display: contents` removes the box itself while keeping its children in the parent's layout — useful for a wrapper that must not break a grid, and a known accessibility hazard on elements with semantics, because some browsers historically dropped the role along with the box."],keyPoints:["content-box adds padding and border to `width`; border-box includes them.","`display` sets outer role *and* inner layout.","`flow-root` creates a BFC cleanly — no clearfix, no `overflow: hidden` side effects.","Margins collapse to the larger value, never inside flex or grid."],interview:'"Why is my element 236px when I set 200px" is the box-model classic. "My parent has no height even though it has children" is the float/BFC question — answer `flow-root`. Margin collapsing is asked as "what is the gap between these two paragraphs" and the answer is the larger margin, not the sum.',code:`/* contain floats and stop margin collapse, with no side effects */
.container { display: flow-root; }

/* the reset that makes 200px mean 200px */
*, *::before, *::after { box-sizing: border-box; }`,resources:[{label:"MDN — Introduction to the CSS box model",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_box_model/Introduction_to_the_CSS_box_model",kind:"docs"},{label:"MDN — Block formatting context",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_display/Block_formatting_context",kind:"docs"},{label:"MDN — Mastering margin collapsing",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_box_model/Mastering_margin_collapsing",kind:"docs"}]}],ue=[{id:"css-flex-axes",area:"CSS",group:"Flexbox",title:"Flex axes, direction, reverse, wrap and `order`",status:"covered",minutes:7,summary:"Flexbox is one-dimensional and content-driven. Everything else follows from knowing which axis you are on — which is the thing `flex-direction` silently changes.",body:['A flex container has a **main axis** and a **cross axis**. `flex-direction: row` (the default) puts the main axis horizontally, so `justify-content` distributes along the row and `align-items` works vertically. Set `column` and the two swap meaning entirely. Nearly every "justify-content isn\'t working" question is a main-axis confusion.',"`row-reverse` and `column-reverse` reverse the **main-start and main-end** edges. This has a consequence people miss: `justify-content: flex-start` now packs items to the *right* (in a left-to-right writing mode), and the visual order no longer matches DOM order — which means **keyboard and screen-reader order no longer match visual order**. That is an accessibility defect, not a style choice, and the same warning applies to `order`.","`flex-wrap: wrap` allows a second line; `nowrap` (the default) forces everything onto one, shrinking items to fit. `flex-flow` is the shorthand for direction plus wrap. Once wrapping is on, `align-content` becomes meaningful — it distributes the **lines**, whereas `align-items` positions items *within* a line. On a single-line container `align-content` does nothing, which is a frequent source of confusion.","`order` changes visual position without changing the DOM. It is a legitimate tool for responsive reordering, and it carries the same accessibility caveat as the reverse directions: tab order follows the DOM, so a visually reordered layout can become unnavigable. Use it deliberately and sparingly.","`gap` (with `row-gap`/`column-gap`) spaces items without the margin arithmetic that used to require `:last-child` exceptions. It applies **between** items only, never outside them."],keyPoints:["`justify-content` = main axis. `align-items` = cross axis. `flex-direction` decides which is which.","`align-content` only does something when there is more than one line.","`row-reverse` and `order` break the match between visual and keyboard order.","`gap` applies between items, not around them."],interview:'"Centre a div" wants `display: flex; justify-content: center; align-items: center` — or `place-items: center` in grid. The better follow-up question is "now make it a column" and see whether you know the properties swapped axes.',code:`.bar {
  display: flex;
  flex-flow: row wrap;      /* direction + wrap */
  justify-content: space-between;  /* along the main axis */
  align-items: center;             /* across the cross axis */
  align-content: flex-start;       /* only matters once wrapped */
  gap: 1rem;
}`,resources:[{label:"MDN — Basic concepts of flexbox",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_flexible_box_layout/Basic_concepts_of_flexbox",kind:"docs"},{label:"CSS-Tricks — A complete guide to flexbox",url:"https://css-tricks.com/snippets/css/a-guide-to-flexbox/",kind:"article",note:"The diagram reference everyone keeps open."},{label:"Flexbox Froggy",url:"https://flexboxfroggy.com/",kind:"practice"}]},{id:"css-flex-sizing",area:"CSS",group:"Flexbox",title:"`flex` shorthand, `flex-basis` vs `width`, grow and shrink",status:"covered",minutes:7,summary:"The `flex` shorthand is three properties in a trench coat, and its defaults are not the ones you would guess. This is the highest-yield flexbox topic in interviews.",body:["`flex: <grow> <shrink> <basis>`. The initial value is `flex: 0 1 auto` — do not grow, do shrink, size from content. The common shorthands expand to: `flex: 1` → `1 1 0%`, `flex: auto` → `1 1 auto`, `flex: none` → `0 0 auto`, `flex: initial` → `0 1 auto`.",'The distinction that decides layouts: **`flex: 1` sets basis to `0%`**, so all free space is divided equally and items end up the **same size regardless of content**. **`flex: auto`** starts from content size and then distributes the remainder, so a longer item stays wider. Choosing the wrong one is why "my columns are uneven" happens.',"`flex-basis` is the starting size along the **main axis** before grow and shrink apply, and it **beats `width`** when both are set (on a row). On a column, basis governs height instead. `flex-basis: auto` defers to `width`/`height`; `content` sizes from the content itself.","`flex-shrink` is weighted by basis: an item twice as wide shrinks twice as fast at the same shrink factor. `flex-shrink: 0` is the fix for the classic bug where an icon or avatar gets squashed — and the other half of that bug is `min-width`.","**A flex item will not shrink below its content's minimum size** because `min-width`/`min-height` default to `auto` on flex items. That is why a long unbroken string overflows its container: the fix is `min-width: 0` (or `overflow: hidden`) on the item, and it is one of the most useful facts in day-to-day CSS."],keyPoints:["Default is `flex: 0 1 auto` — shrink but do not grow.","`flex: 1` = equal sizes (basis 0). `flex: auto` = content-aware sizes.","`flex-basis` overrides `width` on the main axis.","`min-width: 0` is the cure for a flex item that refuses to shrink."],interview:'"Difference between `flex: 1` and `flex: auto`" is asked constantly and answered vaguely. Say: basis `0%` versus `auto`, therefore equal versus content-proportional. "Why does my text overflow the flex container" expects `min-width: auto` and the `min-width: 0` fix.',code:`/* equal columns whatever the content */
.col { flex: 1; }          /* 1 1 0%  */

/* content-sized, sharing only the leftover */
.col { flex: auto; }       /* 1 1 auto */

/* the two classic fixes */
.icon  { flex-shrink: 0; }   /* stop the avatar being squashed */
.label { min-width: 0; }     /* let long text truncate instead of overflow */`,resources:[{label:"MDN — Controlling ratios of flex items",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_flexible_box_layout/Controlling_ratios_of_flex_items_along_the_main_axis",kind:"docs"},{label:"MDN — flex",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/flex",kind:"docs"},{label:"CSS-Tricks — Flexbox and min-width: auto",url:"https://css-tricks.com/flexbox-truncated-text/",kind:"article"}]},{id:"css-flex-align",area:"CSS",group:"Flexbox",title:"align-self, auto margins and the alignment family",status:"covered",minutes:6,summary:'Per-item alignment, and the auto-margin trick that solves the "push this one to the end" problem more cleanly than anything else.',body:["`align-items` sets the cross-axis alignment for every item; `align-self` overrides it for one. Values: `stretch` (the default — which is why equal-height cards happen for free), `flex-start`, `flex-end`, `center`, `baseline` (aligns text baselines rather than boxes, which is what you want for labels of different font sizes).","**`margin: auto` absorbs free space in the direction it is applied**, and in flexbox it does so along either axis. `margin-left: auto` on one item pushes it and everything after it to the end — the idiomatic way to put a logout button at the right of a nav bar without a spacer element. `margin: auto` on a single item centres it on both axes.","Auto margins have a priority worth knowing: they consume free space **before** `justify-content` gets a chance, so an item with `margin-left: auto` wins over the container's distribution. That is a feature — it lets one item opt out of the container's scheme.","`justify-content` values: `flex-start`, `flex-end`, `center`, `space-between` (no outer space), `space-around` (half-size outer space), `space-evenly` (equal everywhere). The difference between the last three is asked as a diagram question surprisingly often.",'Baseline alignment deserves a note: `align-items: baseline` lines up the first text baseline of each item, which is usually what a designer means by "aligned" for a row of labels with different sizes — and something `center` gets subtly wrong.'],keyPoints:["`align-items` is the container default; `align-self` overrides one item.","`margin-left: auto` pushes an item to the end; `margin: auto` centres it both ways.","Auto margins eat free space before `justify-content` distributes it.","`stretch` is the default — that is why flex children are equal height."],interview:'"Put the last item on the right without a spacer" — `margin-left: auto`. "Difference between space-between, around and evenly" — describe the outer gaps. Both are quick wins that show fluency.',code:`nav { display: flex; gap: 1rem; }
nav .logout { margin-left: auto; }   /* pushed to the far end */

.card { display: flex; }
.card .badge { align-self: flex-start; }  /* opt out of stretch */`,resources:[{label:"MDN — Aligning items in a flex container",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_flexible_box_layout/Aligning_items_in_a_flex_container",kind:"docs"},{label:"MDN — CSS box alignment",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_box_alignment",kind:"docs",note:"The unified model shared by flex and grid — worth reading once properly."}]}],pe=[{id:"css-grid-tracks",area:"CSS",group:"Grid",title:"Tracks, `fr`, `repeat`, `minmax`, `auto-fit` vs `auto-fill`",status:"covered",minutes:8,summary:"Grid is two-dimensional and parent-driven: the container defines the tracks and items are placed into them. Track sizing is where the power lives.",body:["`grid-template-columns` and `grid-template-rows` define the **explicit** grid. Tracks can be fixed (`200px`), content-based (`auto`, `min-content`, `max-content`, `fit-content(300px)`) or fractional (`1fr`). `fr` distributes **leftover** space after fixed tracks and gaps are subtracted, which is why `1fr 1fr` is reliably equal and `50% 50%` breaks the moment a `gap` exists.","`repeat(3, 1fr)` is shorthand for three equal tracks. `minmax(min, max)` bounds a track — `minmax(200px, 1fr)` never goes below 200px but grows to share space. **`minmax(0, 1fr)`** is the essential defensive form: a bare `1fr` means `minmax(auto, 1fr)`, and `auto` refuses to go below the content's minimum, which is exactly why a long word or a `<pre>` blows a grid column out of its container.","**`auto-fit` versus `auto-fill`** is the most-asked grid question. Both repeat as many tracks as fit. `auto-fill` keeps the empty tracks, so remaining items do not stretch and the grid stays on its rhythm. `auto-fit` **collapses** the empty tracks to zero, so the existing items expand to fill the row. With one item: `auto-fill` leaves it at its minimum with empty columns beside it; `auto-fit` stretches it across the whole row.","That combination is the famous responsive grid with no media query at all: `repeat(auto-fit, minmax(min(100%, 18rem), 1fr))`. The inner `min(100%, 18rem)` prevents overflow on screens narrower than the minimum, which the naive version gets wrong on small phones.","Lines can be named — `[full-start] 1fr [content-start] minmax(0, 60ch) [content-end] 1fr [full-end]` — and items placed by name, which is how full-bleed article layouts are built."],keyPoints:["`fr` divides space left over after fixed tracks and gaps.","`1fr` means `minmax(auto, 1fr)`; use `minmax(0, 1fr)` to allow shrinking.","`auto-fill` keeps empty tracks; `auto-fit` collapses them so items stretch.","`repeat(auto-fit, minmax(min(100%, 18rem), 1fr))` — responsive with zero media queries."],interview:'Guaranteed: "difference between auto-fit and auto-fill". Describe the one-item case — it is unambiguous and shows you have actually used both. "Why does my grid overflow" expects `minmax(0, 1fr)`.',code:`.gallery {
  display: grid;
  gap: 1rem;
  grid-template-columns: repeat(auto-fit, minmax(min(100%, 18rem), 1fr));
}
.safe { grid-template-columns: minmax(0, 1fr) minmax(0, 2fr); } /* can shrink */`,resources:[{label:"MDN — CSS grid layout",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_grid_layout",kind:"docs"},{label:"CSS-Tricks — A complete guide to grid",url:"https://css-tricks.com/snippets/css/complete-guide-grid/",kind:"article"},{label:"Grid Garden",url:"https://cssgridgarden.com/",kind:"practice"}]},{id:"css-grid-placement",area:"CSS",group:"Grid",title:"Placement, spanning, `grid-template-areas` and implicit tracks",status:"covered",minutes:7,summary:"Where items land, how they span, and what happens to the ones you never placed.",body:['Items are placed by **line number**, not by cell: `grid-column: 1 / 3` spans from line 1 to line 3, which is two columns. Negative numbers count from the end, so `grid-column: 1 / -1` means "full width" regardless of how many tracks exist — the single most useful placement idiom. `span N` is relative: `grid-column: span 2`.',"`grid-template-areas` lets you draw the layout as ASCII and assign items by name. It is the most readable form for page-level layout, a `.` marks an empty cell, and every row string must have the same number of columns or the whole declaration is invalid. `grid-area` on the child claims the name.",'Anything that does not fit the explicit grid creates the **implicit** grid. `grid-auto-rows` and `grid-auto-columns` size those generated tracks — `grid-auto-rows: minmax(120px, auto)` is the common "rows at least this tall" pattern. Without it, implicit rows are `auto`-sized.',"`grid-auto-flow` controls how auto-placement fills: `row` (default), `column`, and the `dense` modifier, which back-fills earlier holes with later items. `dense` reorders visually without changing DOM order — the same accessibility caveat as flexbox `order` applies.","Grid items can overlap deliberately by placing two items in the same cell; stacking is then decided by `z-index` and source order. This is how card overlays and hero compositions are built without absolute positioning."],keyPoints:["Placement is by line, not cell: `1 / 3` spans two columns.","`grid-column: 1 / -1` is full width for any number of tracks.","`grid-auto-rows` sizes rows the explicit grid never declared.","`grid-auto-flow: dense` back-fills holes and breaks visual/DOM order."],interview:'"Build a holy-grail layout" is best answered with `grid-template-areas` — it is readable and demonstrably two-dimensional. Mentioning `1 / -1` for the full-width header scores.',code:`.page {
  display: grid;
  grid-template-columns: 200px minmax(0, 1fr) 200px;
  grid-template-areas:
    "head head head"
    "nav  main aside"
    "foot foot foot";
  grid-auto-rows: minmax(4rem, auto);
}
.head { grid-area: head; }`,resources:[{label:"MDN — Grid template areas",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_grid_layout/Grid_template_areas",kind:"docs"},{label:"MDN — Auto-placement in grid layout",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_grid_layout/Auto-placement_in_grid_layout",kind:"docs"}]},{id:"css-grid-align",area:"CSS",group:"Grid",title:"Two-axis alignment: justify/align items, content, self, `place-*`",status:"covered",minutes:6,summary:"Grid gives you both axes at once, and six properties that people mix up because the names are nearly identical.",body:["The naming rule that makes it all learnable: **`justify-*` acts on the inline (row) axis, `align-*` on the block (column) axis** — and in grid, unlike flexbox, this does not swap, because grid has real rows and columns.","`*-items` sets the default for every item **inside its cell**. `*-content` distributes the **whole grid** within the container when the tracks are smaller than the container. `*-self` overrides for a single item. So `align-items: center` centres each item in its row; `align-content: center` centres the entire grid vertically in a taller container.","`place-items` is the shorthand for `align-items` + `justify-items` (block first, then inline). `place-content` and `place-self` follow the same order. `place-items: center` is the shortest reliable centring in CSS — two words on the parent and nothing on the child.","The default for both `*-items` axes in grid is `stretch`, which is why a grid child fills its cell unless it has an intrinsic size. Setting `justify-items: start` makes children shrink to content width.",'These properties come from the **CSS Box Alignment** module and are shared with flexbox — which is why learning them once pays twice. The only difference is which axis is "main" in flexbox, versus fixed row/column in grid.'],keyPoints:["`justify-*` = inline/row axis. `align-*` = block/column axis. In grid it never swaps.","`*-items` positions inside cells; `*-content` positions the grid within the container.","`place-items: center` is the two-word perfect centre.","Grid items stretch by default."],interview:'"Centre something perfectly" — `display: grid; place-items: center`. The follow-up "what is the difference between align-items and align-content" is the real test: items within cells versus the whole grid within the container.',code:`.frame { display: grid; place-items: center; min-height: 100dvh; }

.board {
  display: grid;
  grid-template-columns: repeat(3, 100px);
  justify-content: center;   /* the grid itself, horizontally */
  align-content: center;     /* the grid itself, vertically   */
  justify-items: stretch;    /* each item inside its cell     */
}`,resources:[{label:"MDN — Box alignment in grid layout",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_box_alignment/Box_alignment_in_grid_layout",kind:"docs"},{label:"MDN — place-items",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/place-items",kind:"docs"}]}],me=[{id:"css-positioning",area:"CSS",group:"Positioning",title:"static, relative, absolute, fixed, sticky, containing block, z-index",status:"covered",minutes:8,summary:"Five position values, one containing-block rule that explains all of them, and a stacking model that explains why your z-index is being ignored.",body:["`static` is the default and ignores `inset`. `relative` offsets the box **visually** while its original space is preserved — nothing else moves. `absolute` removes the box from flow entirely and positions it against its **containing block**. `fixed` positions against the viewport. `sticky` behaves as relative until a scroll threshold, then behaves as fixed **within its parent** — and silently does nothing if you forget to give it a threshold (`top: 0`) or if an ancestor has `overflow: hidden`.",'The **containing block** is the whole game for absolute positioning: it is the padding box of the nearest ancestor with a `position` other than `static`. If there is none, it is the initial containing block (roughly the viewport). This is why `position: relative` on a parent is the standard first move — it makes that parent the reference frame. A `transform`, `filter`, `perspective`, `will-change` or `contain` on an ancestor also creates a containing block **even for `fixed`** elements, which is the cause of the classic "my fixed modal is trapped inside a transformed card" bug.',"`inset` is the shorthand for `top`/`right`/`bottom`/`left`. Setting **both** opposite offsets stretches the element rather than moving it: `inset: 0` with `position: absolute` fills the containing block, which is the cleanest overlay in CSS. If both are set and a `width` is also set, `margin: auto` centres in the leftover space.",'**Stacking contexts** are why `z-index` "does not work". A new stacking context is created by a positioned element with a `z-index` other than `auto`, and also by `opacity < 1`, `transform`, `filter`, `mix-blend-mode`, `isolation: isolate`, `will-change`, and grid/flex items with a `z-index`. Inside a context, children are painted relative to each other only — a child with `z-index: 9999` can never escape a parent whose context sits below another. `isolation: isolate` is the deliberate way to create one.'],keyPoints:["Absolute positions against the nearest non-static ancestor's padding box.","A `transform` on an ancestor becomes the containing block even for `fixed`.","`inset: 0` + absolute = fill the parent.","`z-index` is only compared within a stacking context — 9999 cannot escape."],interview:'"Difference between absolute and fixed" is the warm-up; "why is my z-index not working" is the real question, and the answer is stacking contexts. Naming `transform` as a context creator marks experience.',code:`.card { position: relative; }              /* becomes the reference frame */
.card .overlay { position: absolute; inset: 0; }   /* fills the card */

.header { position: sticky; top: 0; }     /* needs a threshold to do anything */
/* and no ancestor may have overflow: hidden, or sticky silently dies */`,resources:[{label:"MDN — position",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/position",kind:"docs"},{label:"MDN — Stacking context",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_positioned_layout/Stacking_context",kind:"docs",note:"The list of what creates one is the part to memorise."},{label:"MDN — Containing block",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_display/Containing_block",kind:"docs"}]},{id:"css-units",area:"CSS",group:"Units",title:"Units: px, rem, em, ch, %, viewport units, clamp and calc",status:"covered",minutes:7,summary:`Each unit answers "relative to what?" differently. Choosing wrongly is what makes a layout break at the user's font size instead of at a breakpoint.`,body:["`px` is absolute and predictable — correct for hairlines, borders and shadows. `rem` is relative to the **root** font size, so it scales when the user changes their browser text size; use it for type and spacing so the layout respects that preference. `em` is relative to the **element's own** font size, which compounds through nesting — useful deliberately (padding that scales with a button's text) and a hazard accidentally.","`%` resolves against different things depending on the property: width against the containing block's width, height against its **height** (which is why `height: 100%` fails unless every ancestor has a height), and — the famous one — **padding and margin percentages resolve against the containing block's _width_, even vertically**. That last rule is what made the old aspect-ratio hack possible.",'Viewport units: `vw`, `vh`, `vmin`, `vmax`. On mobile, `100vh` is famously wrong because browser chrome hides and shows; the newer `dvh` (dynamic), `svh` (small) and `lvh` (large) fix it — `100dvh` is what "full screen height" should now say.',"`ch` is the width of the `0` glyph and is the honest unit for line length: `max-width: 65ch` gives a readable measure regardless of font size. `ex` and `cap` are similar font-relative units used less often.","`clamp(min, preferred, max)` is fluid sizing in one line: `font-size: clamp(1rem, 2.5vw + 0.5rem, 2rem)` scales with the viewport but never escapes its bounds — replacing several media queries. `calc()` mixes units (`calc(100% - 2rem)`), and `min()`/`max()` pick a value at computation time. Include a `rem` term inside a clamp so that text still responds to user zoom."],keyPoints:["Percentage padding and margin resolve against **width**, even top and bottom.","`rem` respects user font-size preference; `em` compounds through nesting.","Use `dvh` rather than `vh` for mobile full-height.","`clamp()` replaces breakpoint-driven font sizing — keep a `rem` term for zoom."],interview:'"Difference between rem and em" is entry-level; the follow-up "which would you use for a component\'s padding and why" is the real one. "Why is 100vh wrong on mobile" is a current favourite with `dvh` as the answer.',code:`:root { font-size: 100%; }             /* respect the user's setting */
h1   { font-size: clamp(1.5rem, 4vw + 1rem, 3rem); }
main { max-width: 65ch; }              /* readable measure */
.hero { min-height: 100dvh; }          /* not 100vh */`,resources:[{label:"MDN — CSS values and units",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Values_and_Units",kind:"docs"},{label:"MDN — clamp()",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/clamp",kind:"docs"},{label:"web.dev — Large, small and dynamic viewport units",url:"https://web.dev/blog/viewport-units",kind:"article"}]},{id:"css-ratio-logical",area:"CSS",group:"Sizing",title:"`aspect-ratio`, the padding hack, writing modes and logical properties",status:"partial",minutes:6,summary:"How to hold a shape, how it used to be done, and how to write CSS that survives a language change without rewriting every margin.",body:["`aspect-ratio: 16 / 9` fixes the ratio of a box: give it a width and the height derives, or a height and the width derives. It is the modern answer for video embeds, image placeholders and square avatars, and it prevents layout shift because the space is reserved before the media loads.","The **padding hack** is what it replaced, and it is still asked about because it explains a rule: since **percentage padding resolves against the containing block's width**, `padding-top: 56.25%` produces a box 56.25% as tall as it is wide — a 16:9 ratio. The content then sits in an absolutely positioned child filling the box. Know it as history *and* as proof you understand percentage resolution.",'**Writing modes** change the direction text flows: `writing-mode: vertical-rl` is used for Japanese and Chinese vertical text and, more practically in interviews, for rotated table headers. It swaps which physical axis is "inline" and which is "block", which is exactly why the alignment properties are named `justify` (inline) and `align` (block) rather than horizontal and vertical.',"**Logical properties** name edges by flow rather than by physical direction: `margin-inline`, `padding-block`, `inset-inline-start`, `border-inline-end`, `inline-size`, `block-size`. In a left-to-right English page `margin-inline-start` is the left margin; in Arabic it is the right one — automatically. `margin-inline: auto` is the modern horizontal centring, and it keeps working when direction changes.","The pairing to remember: `width`/`height` are physical, `inline-size`/`block-size` are logical. Under `writing-mode: vertical-rl`, `inline-size` controls the vertical extent, because inline is now the vertical axis."],keyPoints:["`aspect-ratio: 16 / 9` reserves space and prevents layout shift.","The padding hack works because percentage padding resolves against width.","Writing mode is why the properties are `justify`/`align` rather than horizontal/vertical.","`margin-inline: auto` centres and survives a direction change."],interview:'"Make a responsive 16:9 video box" — answer `aspect-ratio`, then offer the padding hack as the pre-2021 method and explain *why* it worked. That second half is the part that impresses.',code:`.video { aspect-ratio: 16 / 9; width: 100%; }

/* the historical version, and the reason it works */
.legacy { position: relative; padding-top: 56.25%; }   /* % of WIDTH */
.legacy > iframe { position: absolute; inset: 0; width: 100%; height: 100%; }

.page { margin-inline: auto; max-inline-size: 65ch; }  /* direction-agnostic */`,resources:[{label:"MDN — aspect-ratio",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/aspect-ratio",kind:"docs"},{label:"MDN — CSS logical properties",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_logical_properties_and_values",kind:"docs"},{label:"MDN — writing-mode",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/writing-mode",kind:"docs"}]}],fe=[{id:"css-media-container",area:"CSS",group:"Responsive",title:"Media queries, container queries and responsive strategy",status:"covered",minutes:8,summary:"Media queries ask about the viewport; container queries ask about the parent. The second is what finally makes a component genuinely reusable.",body:["Media queries test the **viewport and device**: `@media (min-width: 48rem)`, and the modern range syntax `@media (width >= 48rem)`, which reads better and avoids the off-by-one of `max-width: 47.99rem`. Mobile-first means writing the small layout as the base and adding `min-width` queries — you end up with less CSS and a sensible fallback.","Beyond width, media queries test capability and preference: `(prefers-color-scheme: dark)`, `(prefers-reduced-motion: reduce)` — which you should honour by disabling non-essential animation — `(prefers-contrast: more)`, `(hover: hover)` and `(pointer: coarse)`. That last pair is the correct way to detect touch: do not sniff user agents, ask whether hover exists.","**Container queries** ask about the *element's own container*: mark an ancestor with `container-type: inline-size` (optionally `container-name`), then `@container (min-width: 30rem) { … }`. Now a card renders wide inside a wide slot and narrow inside a sidebar — **with no knowledge of the page layout**. That is the difference between a responsive page and a responsive component, and it is why container queries matter more for design systems than media queries ever did.","Container query units follow: `cqw`, `cqh`, `cqi` (inline), `cqb` (block) — sized against the container rather than the viewport, so `font-size: 5cqi` scales type with the component.","Choose deliberately: media queries for page-level structure (sidebar appears, navigation collapses), container queries for component-level adaptation. Many layouts now need neither, because `flex-wrap`, `auto-fit` and `clamp()` handle the middle ground without any breakpoint at all."],keyPoints:["Media query = viewport. Container query = parent element.","`container-type: inline-size` on the parent is required before `@container` does anything.","Detect touch with `(pointer: coarse)`, never with user-agent sniffing.","Honour `prefers-reduced-motion` — it is an accessibility requirement, not a nicety."],interview:'"How would you make this card responsive inside any container?" is the container-query question, and most candidates still answer with media queries. Naming `container-type` and the `cqi` unit is a clear signal of currency.',code:`.sidebar, .main { container-type: inline-size; }

.card { display: grid; gap: 1rem; }
@container (width >= 30rem) {
  .card { grid-template-columns: 12rem 1fr; }
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after { animation-duration: .01ms !important; transition-duration: .01ms !important; }
}`,resources:[{label:"MDN — Using container queries",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_containment/Container_queries",kind:"docs"},{label:"MDN — Using media queries",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_media_queries/Using_media_queries",kind:"docs"},{label:"web.dev — Responsive design in 2026",url:"https://web.dev/learn/design",kind:"article"}]},{id:"css-states",area:"CSS",group:"Interaction",title:"Focus states, `:focus-visible`, and styling interaction accessibly",status:"covered",minutes:6,summary:"The focus ring is the single most commonly destroyed accessibility feature in front-end code, and `:focus-visible` is the reason nobody needs to destroy it any more.",body:["`:focus` matches whenever an element has focus — including after a mouse click, which is why designers historically asked for `outline: none` and developers complied, breaking keyboard navigation for everyone.","`:focus-visible` matches only when the browser judges a focus indicator appropriate — keyboard navigation yes, mouse click on a button no, but click into a text input yes, because you need to see the caret context. It resolves the design objection completely: style `:focus-visible` and the ring appears exactly when it should.","`:focus-within` matches an element when **anything inside it** has focus. It is how you highlight a whole form field group, or keep a dropdown open while a child input is focused, with no JavaScript.","If you remove the default outline, you owe a replacement with sufficient contrast against **both** the component and the page. `outline` is the right property because — unlike `border` — **it does not take up layout space**, so nothing shifts when it appears. `outline-offset` pushes it away from the edge for legibility.","For interaction states generally: `:hover` must never be the only affordance (touch devices have no hover), `:active` gives press feedback, `:disabled` should look unavailable *and* be genuinely non-interactive, and `aria-disabled` with a visual style is preferred when the control must stay focusable so screen-reader users can find it."],keyPoints:["`:focus-visible` shows the ring for keyboards and hides it for mouse clicks.","`:focus-within` styles a parent when a descendant is focused.","`outline` does not affect layout; `border` does — that is why outline is used for focus.","Hover can never be the only affordance."],interview:'"How do you handle focus styles without them looking bad on click?" — `:focus-visible`. This is a small question that reliably separates people who have thought about accessibility from people who have not.',code:`:focus-visible {
  outline: 2px solid Highlight;   /* system colour, respects forced-colors */
  outline-offset: 2px;
}
.field:focus-within { border-color: var(--accent); }`,resources:[{label:"MDN — :focus-visible",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/:focus-visible",kind:"docs"},{label:"MDN — :focus-within",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/:focus-within",kind:"docs"},{label:"WCAG — Focus visible (2.4.7)",url:"https://www.w3.org/WAI/WCAG21/Understanding/focus-visible.html",kind:"spec"}]},{id:"css-tokens-modern",area:"CSS",group:"Modern CSS",title:"Custom properties, design tokens, `color-mix`, nesting, `:has` layouts",status:"covered",minutes:7,summary:"The 2026 toolkit: variables that cascade, colours computed in CSS, nesting without a preprocessor, and the selectors that removed whole categories of JavaScript.",body:["**Custom properties** (`--brand: #0ea5e9`) differ from preprocessor variables in one decisive way: they are live, they **inherit**, and they can be changed at runtime by a media query, a class, or JavaScript (`el.style.setProperty`). That is what makes theming possible without rebuilding a stylesheet. They are also case-sensitive, and `var(--x, fallback)` supplies a default.","A **token layer** is the convention: primitives (`--blue-500`), then semantic tokens (`--color-accent: var(--blue-500)`), then component tokens (`--button-bg: var(--color-accent)`). Components consume only the last layer, so re-theming touches one place. Dark mode becomes a redefinition of the semantic layer inside `@media (prefers-color-scheme: dark)` — or `light-dark(lightValue, darkValue)` in a single declaration.","`color-mix(in oklab, var(--accent) 20%, transparent)` computes colours in CSS: tints, shades and translucent variants without generating a palette by hand. `oklch()` gives perceptually uniform colours, so a lightness change looks like the same step across hues — which HSL does not.","**Native nesting** is now supported without a preprocessor: `& .child`, `&:hover`, and nested media queries. Use it shallowly — nesting deeply reproduces the specificity problems Sass was criticised for.","Two more worth naming because interviewers are starting to: `text-wrap: balance` for headlines that no longer orphan a word, `field-sizing: content` for inputs that grow with their value, and `content-visibility: auto` to skip rendering off-screen sections — a genuine performance win for long pages, applied in this very app."],keyPoints:["Custom properties inherit and are live at runtime; Sass variables are neither.","Three token layers: primitive → semantic → component.","`color-mix()` and `oklch()` remove hand-built palettes.","Nest shallowly — deep nesting recreates specificity wars."],interview:'"How would you implement dark mode?" — semantic token layer redefined under `prefers-color-scheme`, plus a class or `data-theme` override so the user can choose. Mentioning `light-dark()` shows you have read the 2026 additions.',code:`:root {
  --blue-500: oklch(0.68 0.15 240);
  --color-accent: var(--blue-500);          /* semantic */
  --button-bg: var(--color-accent);          /* component */
  --button-bg-hover: color-mix(in oklab, var(--button-bg) 85%, black);
}
[data-theme="dark"] { --color-accent: oklch(0.78 0.13 240); }
.button { background: var(--button-bg); &:hover { background: var(--button-bg-hover); } }`,resources:[{label:"MDN — Using CSS custom properties",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_cascading_variables/Using_CSS_custom_properties",kind:"docs"},{label:"MDN — color-mix()",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/color_value/color-mix",kind:"docs"},{label:"MDN — CSS nesting",url:"https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_nesting",kind:"docs"},{label:"web.dev — Modern CSS",url:"https://web.dev/learn/css",kind:"article"}]}],ge=[{id:"js-types-coercion",area:"JavaScript",group:"Values",title:"Types, coercion and equality",status:"covered",minutes:7,summary:"Seven primitives, one object type, and a coercion algorithm that produces the famous surprises. Interviewers use coercion questions as a proxy for whether you have read the language or merely used it.",body:["Primitives: `string`, `number`, `bigint`, `boolean`, `undefined`, `symbol`, `null`. Everything else — arrays, functions, dates, regexes — is an object. Primitives are compared by value and objects by reference, and that single distinction generates most of the puzzles.",'`===` compares type first and never coerces. `==` runs the abstract equality algorithm: `null == undefined` is `true` (and neither equals anything else), numbers and strings compare as numbers, booleans become numbers *before* anything else, and an object is converted to a primitive via `valueOf`/`toString`. That is why `[] == false` is `true`: `[]` → `""` → `0`, and `false` → `0`.',"`Object.is` differs from `===` in exactly two places: `Object.is(NaN, NaN)` is `true` and `Object.is(0, -0)` is `false`. React uses `Object.is` for its own comparisons, which is why understanding it is not academic — it is the rule that decides whether your component re-renders.",'Falsy values are a closed set worth memorising: `false`, `0`, `-0`, `0n`, `""`, `null`, `undefined`, `NaN`. **Everything else is truthy** — including `[]`, `{}`, `"0"` and `"false"`. This is why `[] == false` (coercion) and `if ([])` (truthiness) give apparently opposite answers; they are different algorithms.',"`??` differs from `||` by triggering only on `null`/`undefined`, not on all falsy values. `count || 10` turns a legitimate `0` into `10`; `count ?? 10` does not. In React props with numeric defaults this is a real bug, not a style preference."],keyPoints:["Primitives compare by value; objects compare by reference.","`Object.is` = `===` except `NaN` equals itself and `+0 !== -0`. React uses it.","Eight falsy values. Everything else, including `[]` and `{}`, is truthy.",'`??` guards only null/undefined; `||` swallows `0` and `""`.'],interview:'Output-prediction items dominate: `[] == false`, `NaN === NaN`, `0.1 + 0.2 === 0.3`, `typeof null`. Answer the value, then name the rule — "false, because `typeof null` is a historical bug preserved for compatibility" scores far better than "object".',code:`[] == false        // true  — [] → "" → 0, false → 0
[] === false       // false — different types, no coercion
!![]               // true  — every object is truthy
NaN === NaN        // false
Object.is(NaN,NaN) // true
0 || 10            // 10  ← swallows a legitimate zero
0 ?? 10            // 0   ← correct default handling`,resources:[{label:"MDN — Equality comparisons and sameness",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Equality_comparisons_and_sameness",kind:"docs",note:"The table comparing ==, ===, Object.is is the whole topic on one page."},{label:"You Don't Know JS — Types & Grammar",url:"https://github.com/getify/You-Dont-Know-JS/tree/2nd-ed/types-grammar",kind:"book"}]},{id:"js-scope-closures",area:"JavaScript",group:"Execution",title:"Scope, hoisting, TDZ and closures",status:"covered",minutes:8,summary:"Closures are the mechanism behind hooks, event handlers, module privacy and the loop puzzle every interviewer owns. If you understand closures, half of React stops being mysterious.",body:["A **closure** is a function together with the variable environment it was created in. It captures **bindings, not values** — this is the sentence that resolves the classic loop question. `var` is function-scoped, so a `for` loop creates one binding shared by every iteration; by the time the timers fire, that binding holds the final value. `let` creates a fresh binding per iteration, so each closure captures its own.",'**Hoisting** is not "declarations move to the top" so much as "declarations are processed before execution". `var` declarations are initialised to `undefined`; `function` declarations are fully available; `let`/`const`/`class` are declared but **uninitialised**, and reading them before the declaration throws — that window is the **Temporal Dead Zone**.',"Closures are how privacy works without classes: variables in an enclosing function are unreachable from outside, and only the returned functions can touch them. This is the module pattern, and it is why `useState` can hold state that no other component can reach.","The React connection is direct: every render creates new closures over that render's props and state. An effect or callback captures the values **from the render it was created in**. A stale closure — a `setInterval` set up once with `[]` deps, reading a `count` frozen at 0 — is the single most common React bug, and it is a closure bug, not a React bug."],keyPoints:["Closures capture bindings, not values.","`var` is function-scoped; `let`/`const` are block-scoped with a TDZ.","Every React render makes new closures — stale closures are captured old renders.","The functional updater `setCount(c => c + 1)` exists precisely to escape a stale closure."],interview:"The `for (var i…) setTimeout` question appears in almost every JavaScript screen. Give the output, name `var` vs `let` binding, then offer the pre-ES6 fix (an IIFE per iteration) — that last part signals you know why `let` was added.",code:`for (var i = 0; i < 3; i++) setTimeout(() => console.log(i)); // 3 3 3
for (let j = 0; j < 3; j++) setTimeout(() => console.log(j)); // 0 1 2

// Pre-ES6 fix: a new scope per iteration
for (var k = 0; k < 3; k++) {
  (function (captured) { setTimeout(() => console.log(captured)); })(k);
}`,resources:[{label:"MDN — Closures",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Closures",kind:"docs"},{label:"You Don't Know JS — Scope & Closures",url:"https://github.com/getify/You-Dont-Know-JS/tree/2nd-ed/scope-closures",kind:"book",note:"The definitive treatment; chapters 5–7 are the interview material."},{label:"Dan Abramov — A complete guide to useEffect",url:"https://overreacted.io/a-complete-guide-to-useeffect/",kind:"article",note:"Really an essay about closures wearing a React hat. Read it twice."}]},{id:"js-this",area:"JavaScript",group:"Execution",title:"`this`, call/apply/bind and arrow functions",status:"covered",minutes:6,summary:"`this` is decided at the call site, not where the function was written — with one exception that swallows the rule. Four binding rules, in strict precedence order.",body:["The rules, highest precedence first. **1 — `new`**: `this` is the freshly created object. **2 — explicit binding**: `call`, `apply` or `bind` set it directly. **3 — implicit binding**: called as `obj.method()`, `this` is `obj`. **4 — default**: `undefined` in strict mode and modules, the global object in sloppy mode.","Detaching a method loses the receiver: `const f = obj.get; f()` falls to the default rule, which is why `this.n` becomes an error or `undefined`. This is the original reason React class components needed `this.handleClick = this.handleClick.bind(this)` in the constructor.","**Arrow functions have no `this` of their own.** They close over the enclosing lexical scope, which makes them perfect for callbacks inside methods and wrong for object methods and prototype methods. An arrow as an object method sees the module scope, not the object.","`call(thisArg, a, b)` and `apply(thisArg, [a, b])` invoke immediately and differ only in how arguments are passed; `bind` returns a new permanently-bound function and is not undone by a later `call`. Arrow functions ignore all three for `this`."],keyPoints:["`this` is determined by the call site, except in arrows, where it is determined by the definition site.","Precedence: `new` > explicit (`bind`/`call`/`apply`) > implicit (`obj.fn()`) > default.","Never use an arrow for an object or prototype method that needs `this`.","`bind` is permanent; a later `call` cannot override it."],interview:'Usually a snippet: an object with both a shorthand method and an arrow "method", asked what each logs. Answer both, then explain that the arrow captured the enclosing scope. Class fields (`handleClick = () => {}`) are the modern fix and worth mentioning.',code:`const o = {
  n: 1,
  regular() { return this.n; },
  arrow: () => this?.n,
};
o.regular();          // 1
o.arrow();            // undefined — arrow has no own \`this\`
const f = o.regular;
f();                  // undefined (strict) — receiver lost
f.call(o);            // 1`,resources:[{label:"MDN — this",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/this",kind:"docs"},{label:"MDN — Function.prototype.bind",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function/bind",kind:"docs"}]},{id:"js-prototypes",area:"JavaScript",group:"Objects",title:"Prototypes, the prototype chain and classes",status:"missing",minutes:7,summary:"JavaScript inherits through a chain of objects, not through classes. `class` is syntax over that chain — and interviewers ask precisely to see whether you know it is syntax.",body:["Every object has an internal link to another object, its **prototype**, reachable as `Object.getPrototypeOf(obj)` (the legacy accessor is `__proto__`). A property lookup walks that chain until it finds the key or reaches `null`. That walk is the entire inheritance mechanism.","A function's `.prototype` property is **not** the function's own prototype. It is the object that will become the prototype of instances made with `new`. This naming is genuinely confusing and is exactly what the question \"difference between `__proto__` and `prototype`\" is testing.","`class` is syntactic sugar: methods land on `Class.prototype` and are therefore shared by all instances, while fields declared in the body or assigned in the constructor are per-instance. `extends` wires the chain; `super()` must be called before `this` is usable in a derived constructor.",'Practical consequences worth stating: `hasOwnProperty` distinguishes own properties from inherited ones (and `Object.hasOwn(obj, key)` is the modern, safer form). `for…in` walks inherited enumerable keys — usually not what you want; `Object.keys` sticks to own keys. Adding to a built-in prototype (`Array.prototype.foo = …`) is "monkey patching" and is discouraged because it pollutes every array in the program.',"Static methods live on the constructor, not the prototype, so they are inherited by subclasses through the constructor chain — which is why `this.constructor.staticMethod()` resolves to the subclass override."],keyPoints:["`obj.__proto__` is what an object inherits *from*; `Fn.prototype` is what `new Fn()` instances will inherit from.","Class methods are shared on the prototype; class fields are per-instance.","`Object.hasOwn(obj, k)` is the modern replacement for `obj.hasOwnProperty(k)`.","`for…in` includes inherited enumerable keys; `Object.keys` does not."],interview:'"Explain prototypal inheritance" and "is `class` real inheritance?" are both standard. Draw the chain: instance → `Fn.prototype` → `Object.prototype` → `null`. Mentioning that method lookup is a runtime walk explains both the flexibility and the cost.',code:`function Animal(name) { this.name = name; }
Animal.prototype.speak = function () { return this.name + ' makes a noise'; };

const dog = new Animal('Rex');
Object.getPrototypeOf(dog) === Animal.prototype; // true
dog.hasOwnProperty('speak');                      // false — it is inherited
Object.hasOwn(dog, 'name');                       // true  — own property`,resources:[{label:"MDN — Inheritance and the prototype chain",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Inheritance_and_the_prototype_chain",kind:"docs"},{label:"MDN — Classes",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Classes",kind:"docs"}]}],be=[{id:"js-equality-matrix",area:"JavaScript",group:"Values",title:"Sameness: `==`, `===`, `Object.is`, `NaN`, `+0`/`-0`",status:"partial",minutes:7,summary:'JavaScript has four different notions of "the same", and React uses the one most people have never read about. Knowing which is which decides both interview answers and re-render behaviour.',body:["The four algorithms, from loosest to strictest. **Loose equality (`==`)** coerces before comparing. **Strict equality (`===`)** compares type first and never coerces — but treats `NaN` as unequal to itself and `+0` as equal to `-0`. **SameValueZero** is `===` except that `NaN` equals `NaN`; it is what `Array.prototype.includes`, `Map` keys and `Set` membership use. **SameValue** (`Object.is`) is SameValueZero except that `+0` and `-0` are *different*.","That gives a table worth memorising. `NaN === NaN` → false. `[NaN].includes(NaN)` → **true** (SameValueZero). `[NaN].indexOf(NaN)` → **-1** (indexOf uses `===`). `Object.is(NaN, NaN)` → true. `0 === -0` → true. `Object.is(0, -0)` → **false**. `new Set([NaN, NaN]).size` → **1**.",'**React compares with `Object.is`** — for state bail-outs, `memo` props, hook dependencies and context values. This is not academic: it is why setting state to a value that is already there skips the render, and why `-0` and `+0` would be treated as a change. Every "why did my component re-render" question resolves to an `Object.is` comparison of references.','Loose equality has exactly one defensible use: `x == null` is a concise test for "null **or** undefined" and is widely accepted. Everywhere else, `===` avoids the coercion table entirely. The coercion rules worth knowing anyway: `null == undefined` is true and neither equals anything else, a boolean is converted to a number **first**, and an object is converted to a primitive via `Symbol.toPrimitive` → `valueOf` → `toString`.','Detecting `NaN`: use `Number.isNaN(x)`, never the global `isNaN(x)`. The global coerces first, so `isNaN("hello")` is `true` — which is almost never what you meant. Similarly prefer `Number.isInteger` and `Number.isFinite` over their global namesakes.'],keyPoints:["Four sameness algorithms: `==`, `===`, SameValueZero (`includes`, `Set`, `Map`), SameValue (`Object.is`).","`includes` finds `NaN`; `indexOf` cannot.","React uses `Object.is` — that is the rule behind every re-render question.","`Number.isNaN` never coerces; the global `isNaN` does."],interview:'Asked as a rapid-fire volley: `NaN === NaN`, `Object.is(0,-0)`, `[NaN].includes(NaN)`. Answer each, then say the algorithm name — "SameValueZero, which is why `includes` and `Set` behave differently from `indexOf`".',code:`NaN === NaN                 // false
Object.is(NaN, NaN)         // true
[NaN].includes(NaN)         // true   ← SameValueZero
[NaN].indexOf(NaN)          // -1     ← uses ===
0 === -0                    // true
Object.is(0, -0)            // false
new Set([NaN, NaN]).size    // 1
isNaN('hello')              // true   ← coerces first, almost never wanted
Number.isNaN('hello')       // false  ← correct`,resources:[{label:"MDN — Equality comparisons and sameness",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Equality_comparisons_and_sameness",kind:"docs",note:"The comparison table on this page is the entire topic."},{label:"MDN — Object.is",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/is",kind:"docs"},{label:"MDN — Number.isNaN",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Number/isNaN",kind:"docs"}]},{id:"js-defaulting-operators",area:"JavaScript",group:"Values",title:"Handling undefined: `??`, `||`, `?.`, `??=`, default parameters",status:"missing",minutes:6,summary:'Five ways to say "if this is missing, use that" — and they disagree about what counts as missing. Choosing the wrong one is a real bug with `0` and `""`.',body:['`||` falls back on **any falsy** value, so `count || 10` turns a legitimate `0` into `10`, and `name || "Anonymous"` turns a deliberate empty string into "Anonymous". `??` (nullish coalescing) falls back only on `null` and `undefined`, leaving `0`, `""` and `false` intact. For numeric and boolean props, `??` is almost always the correct operator.',"`?.` (optional chaining) short-circuits to `undefined` instead of throwing when the left side is `null` or `undefined`: `user?.address?.city`. It has a call form, `fn?.()`, which invokes only if `fn` exists, and an index form, `arr?.[0]`. Note it stops at the first nullish link and returns `undefined` — it does not skip the rest of a chain silently in some other way.","Do not over-apply it. `a?.b.c` guards only `a`; if `b` is missing it still throws. And using `?.` everywhere hides genuine data-shape bugs behind a silent `undefined` — the value of a crash is that it tells you the contract was violated.","The logical assignment operators are the compact forms: `x ??= v` assigns only when `x` is nullish, `x ||= v` when falsy, `x &&= v` when truthy. They short-circuit, so the right-hand side is not evaluated unless needed.","**Default parameters** trigger on `undefined` only, never on `null`: `function f(a = 1)` called as `f(null)` gives `a === null`. The same rule governs destructuring defaults, which is why `const { b = 5 } = { b: null }` leaves `b` as `null`. One rule, three places — remember it once."],keyPoints:["`||` catches all falsy; `??` catches only null/undefined.","Default parameters and destructuring defaults fire on `undefined` only — never `null`.","`a?.b.c` guards `a` alone; the rest of the chain can still throw.","`??=`, `||=`, `&&=` short-circuit — the right side is not evaluated unless needed."],interview:'A favourite snippet: a prop with a numeric default written with `||`, asked what happens when the value is `0`. Answer, then name `??` as the fix. In React this comes up as "why does my zero disappear".',code:`const count = 0;
count || 10        // 10  ← bug: a real zero was replaced
count ?? 10        // 0   ← correct

function f(a = 1) { return a; }
f(undefined)       // 1
f(null)            // null  ← defaults do not fire on null

const { b = 5 } = { b: null };   // b === null, same rule
user?.profile?.name ?? 'Anonymous';`,resources:[{label:"MDN — Nullish coalescing operator",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Nullish_coalescing",kind:"docs"},{label:"MDN — Optional chaining",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Optional_chaining",kind:"docs"},{label:"MDN — Default parameters",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/Default_parameters",kind:"docs"}]}],ye=[{id:"js-event-loop",area:"JavaScript",group:"Async",title:"The event loop, microtasks and macrotasks",status:"covered",minutes:8,summary:"One thread, two queues, and a strict order. Ordering questions are the most reliable way an interviewer separates people who have read about async from people who have reasoned about it.",body:["JavaScript runs on a single thread with a call stack. When the stack empties, the event loop takes work from queues. There are two, and their priority is not equal: the **microtask queue** (promise callbacks, `queueMicrotask`, `MutationObserver`) is drained **completely** before a single **macrotask** (timers, I/O, UI events, `setTimeout`, `setInterval`, `MessageChannel`) is taken.","That asymmetry produces the canonical output: synchronous code first, then every promise callback, then the first timer — even a `setTimeout(fn, 0)`. A microtask that schedules another microtask is also drained in the same pass, which means an infinite microtask chain can starve the loop and freeze the page, while an infinite timer chain cannot.","`await` is syntax over `.then`: everything after an `await` is a microtask continuation. An `async` function runs synchronously up to its first `await` — a detail that decides ordering answers and surprises people who assume the whole body is deferred.","Rendering fits between macrotasks. The browser aims for one paint per frame (~16.7ms at 60Hz); `requestAnimationFrame` callbacks run just before paint, which is why animation belongs there and not in `setTimeout`. `requestIdleCallback` runs when the browser has spare time — appropriate for analytics, wrong for anything the user is waiting on.","Long synchronous work blocks everything: layout, paint, input. That is why heavy computation belongs in a Web Worker, and why the app you are building runs Babel and Prettier in workers rather than on the main thread."],keyPoints:["Microtasks drain fully before the next macrotask. Promises always beat timers.","An `async` function body runs synchronously until the first `await`.",'`setTimeout(fn, 0)` does not mean "now"; it means "next macrotask, at least 0ms".',"`requestAnimationFrame` runs before paint; `setTimeout` has no such guarantee."],interview:'Expect a snippet mixing `console.log`, `setTimeout`, `Promise.then` and sometimes `await`, asked for exact output order. Walk it aloud: sync → microtasks → macrotasks. Saying "the microtask queue drains completely first" is the sentence being listened for.',code:`console.log('A');
setTimeout(() => console.log('B'), 0);
Promise.resolve().then(() => console.log('C'));
(async () => { console.log('D'); await null; console.log('E'); })();
console.log('F');
// A D F C E B`,resources:[{label:"MDN — The event loop",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Event_loop",kind:"docs"},{label:"Jake Archibald — In the loop (talk)",url:"https://www.youtube.com/watch?v=cCOL7MC4Pl0",kind:"video",note:"The clearest 30 minutes on this subject that exists."},{label:"JS Visualizer 9000",url:"https://www.jsv9000.app/",kind:"practice",note:"Paste a snippet and watch the queues drain. Best way to build intuition."}]},{id:"js-promises",area:"JavaScript",group:"Async",title:"Promises, async/await and cancellation",status:"partial",minutes:8,summary:"Beyond `then` and `await` lie the combinators, error propagation rules, and cancellation — the parts that appear in real code review and in senior interviews.",body:["A promise is a value that will settle exactly once, into fulfilled or rejected, and never changes afterwards. `then` returns a **new** promise, which is what makes chaining work; returning a promise from inside `then` adopts its state rather than nesting it.","The four combinators are frequently confused. `Promise.all` rejects as soon as any input rejects and resolves with an array otherwise — use it when you need everything. `Promise.allSettled` never rejects and reports `{status, value|reason}` for each — use it when partial success is acceptable. `Promise.race` settles with the first to settle, **including rejection**. `Promise.any` resolves with the first *fulfilment*, rejecting only if all fail (with an `AggregateError`).","Error handling: `try/catch` around `await` catches rejections; a `.catch` anywhere in a chain handles everything upstream of it. An unhandled rejection is a real error event, not a silent no-op. `finally` runs regardless and — importantly — passes the value through untouched.",'**Cancellation** is the part most candidates have never used. Promises are not cancellable, so the platform provides `AbortController`: create one, pass `controller.signal` to `fetch`, and call `controller.abort()` to reject the fetch with an `AbortError`. In React this is the correct cleanup for a request started in an effect, and it is the answer to "how do you avoid setting state after unmount".',"Sequential `await` in a loop is a common performance bug: each iteration waits for the last. If the operations are independent, build the array of promises first and `await Promise.all(...)`."],keyPoints:["`Promise.all` fails fast; `allSettled` never rejects; `race` includes rejections; `any` wants the first success.","`AbortController` is how you cancel a fetch — and how you clean up an effect.","An `await` inside a loop serialises independent work.","`finally` passes the value through; it cannot change the result."],interview:'"How do you cancel a fetch?" and "difference between `Promise.all` and `allSettled`" are both common. The strong follow-up answer is showing an effect that aborts on cleanup — it demonstrates React and platform knowledge in one snippet.',code:`useEffect(() => {
  const ctrl = new AbortController();
  fetch(url, { signal: ctrl.signal })
    .then(r => r.json())
    .then(setData)
    .catch(e => { if (e.name !== 'AbortError') setError(e); });
  return () => ctrl.abort();   // cleanup cancels the in-flight request
}, [url]);`,resources:[{label:"MDN — Using promises",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Using_promises",kind:"docs"},{label:"MDN — AbortController",url:"https://developer.mozilla.org/en-US/docs/Web/API/AbortController",kind:"docs"},{label:"MDN — Promise.allSettled",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/allSettled",kind:"docs"}]}],we=[{id:"js-arrays-objects",area:"JavaScript",group:"Data",title:"Array and object transformations, immutability",status:"partial",minutes:7,summary:"React re-renders on reference change, so the difference between a method that mutates and one that returns a copy is not a style question — it decides whether your UI updates.",body:["Mutating array methods: `push`, `pop`, `shift`, `unshift`, `splice`, `sort`, `reverse`, `fill`, `copyWithin`. Non-mutating: `map`, `filter`, `slice`, `concat`, `flat`, `flatMap`, and the spread form. `sort` and `reverse` mutating in place is the trap — `items.sort()` reorders your state array directly, so React sees the same reference and skips the render.","ES2023 added copying counterparts that solve exactly that: `toSorted`, `toReversed`, `toSpliced`, and `with(index, value)`. `[...items].sort()` remains the widely-supported idiom.","Spread is a **shallow** copy: `{...obj}` copies top-level keys, but nested objects are still shared references. Mutating `copy.nested.x` mutates the original. `structuredClone(obj)` is the modern deep copy — it handles `Date`, `Map`, `Set`, `ArrayBuffer` and cycles, all of which `JSON.parse(JSON.stringify(obj))` destroys or throws on.","Know the reducers and searchers by behaviour: `reduce` folds to any shape, `find`/`findIndex` return the first match, `some`/`every` short-circuit, `includes` finds `NaN` where `indexOf` cannot (because it uses SameValueZero).","`Map` versus object: `Map` keeps insertion order, allows any key type including objects, has a real `size`, and is faster for frequent additions and deletions. Objects are better for fixed-shape records and JSON. `Set` gives uniqueness by SameValueZero, which is why `[...new Set(arr)]` is the idiomatic dedupe."],keyPoints:["`sort` and `reverse` mutate. `toSorted` and `toReversed` do not.","Spread is shallow; `structuredClone` is deep and preserves Dates, Maps and cycles.","`includes` finds `NaN`; `indexOf` does not.","React compares references — mutating state in place produces no re-render."],interview:"The shallow-copy question is near-universal: spread an object with a nested object, mutate the nested value, ask what the original shows. Answer 99, then name shallow copy and offer `structuredClone` as the fix.",code:`const a = { x: 1, nested: { y: 2 } };
const b = { ...a };
b.nested.y = 99;
a.nested.y;                 // 99 — shared reference

const c = structuredClone(a);
c.nested.y = 7;
a.nested.y;                 // 99 — genuinely independent`,resources:[{label:"MDN — Array",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array",kind:"docs",note:"Each method page states whether it mutates. Worth checking rather than guessing."},{label:"MDN — structuredClone",url:"https://developer.mozilla.org/en-US/docs/Web/API/structuredClone",kind:"docs"},{label:"MDN — Map vs Object",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map#objects_vs._maps",kind:"docs"}]},{id:"js-modules",area:"JavaScript",group:"Modules",title:"ES modules, CommonJS and dynamic import",status:"partial",minutes:5,summary:"Named on the Mettl competency list under ECMAScript, and the foundation of every bundler question about code splitting and tree shaking.",body:["ES module `import`/`export` is **static**: the dependency graph is known before execution, which is what makes tree shaking and hoisting possible. Bindings are **live views**, not copies — if the exporting module reassigns, importers see the new value. Modules are always strict mode, execute once and are cached, and their top-level `this` is `undefined`.","CommonJS (`require`/`module.exports`) is dynamic: `require` runs at call time, can be conditional, and returns a copy of the exports object at that moment. This dynamism is precisely why CommonJS resists tree shaking.",'`import()` is the dynamic form: it returns a promise, may appear anywhere including inside a condition, and is the mechanism behind `React.lazy` and route-level code splitting. Every "how do you reduce bundle size" answer starts here.',"Default versus named exports: named exports are better for tree shaking and refactoring tools; a default export is a single anonymous value the importer may rename freely, which is convenient and slightly worse for consistency. Circular imports resolve to partially-initialised modules rather than errors, which produces confusing `undefined` values at import time."],keyPoints:["ESM is static and hoisted; CommonJS is dynamic and runs at call time.","ESM bindings are live; CommonJS exports are a snapshot copy.","`import()` is the async form and the basis of code splitting.","Tree shaking requires static analysis, which is why ESM enables it and CJS mostly does not."],interview:'Asked as "how does tree shaking work" or "how would you reduce this bundle". Connect static ESM analysis → dead-code elimination → `import()` for route splitting → measurable chunk sizes. That chain is the complete answer.',resources:[{label:"MDN — JavaScript modules",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Modules",kind:"docs"},{label:"MDN — Dynamic import()",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/import",kind:"docs"}]},{id:"js-dom-events",area:"JavaScript",group:"DOM",title:"DOM APIs, events, bubbling and delegation",status:"partial",minutes:7,summary:"React abstracts the DOM but interviews still test it — and vanilla machine-coding rounds test it directly.",body:["An event travels in three phases: **capture** from the window down to the target, **target**, then **bubble** back up. `addEventListener(type, fn)` listens on the bubble phase by default; passing `{capture: true}` moves it to the way down. Not everything bubbles — `focus`, `blur`, `load` and `scroll` (on elements) do not, which is why `focusin`/`focusout` exist as bubbling alternatives.","**Delegation** exploits bubbling: attach one listener to a container and read `event.target` to find what was actually clicked. It is how you handle a list of a thousand rows with one listener instead of a thousand, and how you handle elements that do not exist yet. `event.target` is what was clicked; `event.currentTarget` is what the handler is attached to — a distinction interviewers like.","`preventDefault()` cancels the browser's default action (following a link, submitting a form). `stopPropagation()` halts further travel through the tree. They are independent, and using the second where you meant the first is a frequent bug that breaks other listeners.","Useful listener options: `{once: true}` auto-removes after the first call, `{passive: true}` promises not to call `preventDefault` so the browser need not block scrolling, and `{signal}` accepts an `AbortController` signal so a group of listeners can be removed in one call.","The observers replace polling and are worth naming: `IntersectionObserver` (visibility — lazy loading, infinite scroll), `ResizeObserver` (element size — container-driven layout), `MutationObserver` (DOM changes). Each is asynchronous and far cheaper than a scroll handler measuring positions."],keyPoints:["Capture goes down, bubble comes up; listeners default to bubble.","`event.target` is what was hit; `currentTarget` is what is listening.","`preventDefault` ≠ `stopPropagation`.","Delegation: one listener on a parent handles unlimited and future children."],interview:'"Implement event delegation" is a standard vanilla task, as is "make an infinite-scroll list" — where the expected answer is `IntersectionObserver`, not a scroll handler doing arithmetic.',code:`list.addEventListener('click', (e) => {
  const row = e.target.closest('[data-id]');   // handles clicks on nested spans too
  if (!row || !list.contains(row)) return;
  select(row.dataset.id);
});`,resources:[{label:"MDN — Event bubbling and capture",url:"https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Building_blocks/Event_bubbling",kind:"docs"},{label:"MDN — IntersectionObserver",url:"https://developer.mozilla.org/en-US/docs/Web/API/Intersection_Observer_API",kind:"docs"},{label:"MDN — EventTarget.addEventListener options",url:"https://developer.mozilla.org/en-US/docs/Web/API/EventTarget/addEventListener#options",kind:"docs"}]},{id:"js-polyfills",area:"JavaScript",group:"Machine coding",title:"Polyfills, debounce, throttle, curry, deep clone",status:"covered",minutes:7,summary:"The standard vanilla machine-coding set. Each is small, each has a trap, and being able to write them from blank is the point.",body:["**debounce** delays invocation until the input has been quiet for a delay — right for search-as-you-type and resize handling. **throttle** guarantees at most one call per interval — right for scroll and mousemove. Confusing the two is the classic error: debounce may never fire during continuous activity, throttle always fires at a steady rate.","Writing `Array.prototype.map` by hand tests whether you remember the callback receives `(value, index, array)`, that the callback's `this` can be set by a second argument, and that holes in sparse arrays are skipped.","`call`, `apply` and `bind` polyfills test whether you understand implicit binding: attach the function as a temporary property of the context object, invoke it, delete it. `bind` additionally must support partial application and behave correctly when the bound function is used with `new`.","Deep clone by hand must handle nested objects and arrays, cycles (keep a `WeakMap` of seen objects), and special types — and the honest interview answer is to write it, then say `structuredClone` does this natively now.","`Promise.all` by hand is a favourite: resolve with results **in input order** (not completion order), reject immediately on the first rejection, and handle the empty-array case by resolving immediately."],keyPoints:["Debounce waits for quiet; throttle enforces a rate.","A hand-written `Promise.all` must preserve input order and handle `[]`.","Deep clone needs a `WeakMap` to survive cycles.","`bind` must still work when the result is called with `new`."],interview:'Very common as the 20-minute coding portion. Narrate the trap as you write — "results must be in input order, so I index into a pre-sized array rather than pushing" — because that sentence is what is being assessed.',code:`function debounce(fn, delay) {
  let t;
  return function (...args) {
    clearTimeout(t);
    t = setTimeout(() => fn.apply(this, args), delay);
  };
}

function throttle(fn, interval) {
  let last = 0;
  return function (...args) {
    const now = Date.now();
    if (now - last >= interval) { last = now; fn.apply(this, args); }
  };
}`,resources:[{label:"GreatFrontEnd — JavaScript coding questions",url:"https://www.greatfrontend.com/questions/js",kind:"practice",note:"The polyfill set with test cases, which is how you should practise them."},{label:"MDN — Function.prototype.apply",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Function/apply",kind:"docs"}]}],ve=[{id:"react-rendering-model",area:"React Core",group:"Mental model",title:"Rendering, reconciliation, keys and the virtual DOM",status:"partial",minutes:8,summary:"Almost every React bug and every React interview question resolves to one sentence: rendering is a pure function of props and state, and React decides what changed by comparing trees.",body:['A render is React **calling your component function** to get a description of the UI — a tree of plain objects, the "virtual DOM". Nothing has touched the real DOM yet. React then **reconciles**: it diffs the new tree against the previous one and produces the minimal list of real DOM mutations, which it applies in the **commit** phase. Render can be interrupted and re-run; commit cannot.',"The virtual DOM is not magic and is not faster than a precise hand-written DOM update. It is faster than naive full re-rendering, and vastly easier to write correctly. Saying that out loud in an interview signals you understand the trade rather than repeating marketing.","The diff uses two heuristics. **Different element type ⇒ discard the subtree and rebuild** — which is why a component defined *inside* another component remounts on every render and loses its state, a subtle and expensive bug. **Same type ⇒ keep the DOM node, update the changed props, recurse.**","**Keys** identify children across renders in a list. With `key={index}`, inserting at the front shifts every index, so React matches the old first row to the new first item and carries its DOM state — a focused input, a checkbox — onto the wrong data. Keys must be stable, unique among siblings, and derived from the data. They are not passed to the component and are not a prop.","A component re-renders for exactly three reasons: its own state changed, its parent re-rendered, or a context it consumes changed. Notably, a *parent* re-rendering re-renders children even when their props are identical — that is what `React.memo` exists to interrupt."],keyPoints:["Render describes; commit mutates. Render may be discarded and re-run.","Different element type discards the whole subtree — never define a component inside a component.","Index keys corrupt row state on insertion or reorder.","Three re-render causes: own state, parent render, consumed context."],interview:'"Why do we need keys?" is asked constantly, and the strong answer describes the failure: without stable keys, state attaches to the wrong row. "Is the virtual DOM faster than the DOM?" is a trap — the honest answer is "no, it is faster than re-rendering everything, and much easier to get right".',pitfalls:["Declaring a child component inside a parent component body — remounts every render.","Using an array index as key in a list that can reorder, filter or insert."],resources:[{label:"React — Preserving and resetting state",url:"https://react.dev/learn/preserving-and-resetting-state",kind:"docs",note:"The clearest explanation of why position and key decide identity."},{label:"React — Render and commit",url:"https://react.dev/learn/render-and-commit",kind:"docs"},{label:"React — Rendering lists",url:"https://react.dev/learn/rendering-lists",kind:"docs"}]},{id:"react-state",area:"React Core",group:"State",title:"State, batching, updaters and derived values",status:"covered",minutes:7,summary:"State updates are asynchronous, batched, and read from a closure — three facts that together explain the most common React confusion of all.",body:["Calling a setter does not change the variable you are holding; it schedules a re-render. The `count` in the current closure keeps its old value for the rest of that render. Three calls to `setCount(count + 1)` therefore all compute the same number, and the count goes up by one. `setCount(c => c + 1)` reads the pending value instead and goes up by three.","**Batching**: React groups multiple updates into a single re-render. Since React 18 this is automatic everywhere, including inside promises, timeouts and native event handlers — before 18 it only happened inside React event handlers, which is why old articles disagree.","State is compared with `Object.is`. Setting it to a value that is already there bails out of the re-render — but mutating an object or array in place keeps the same reference, so React sees no change and does not render. This is why immutable updates are mandatory rather than stylish.",'**Derived state is an anti-pattern.** If a value can be computed from props or other state, compute it during render rather than storing it in a second `useState` synchronised by an effect. Storing it creates two sources of truth that can disagree, plus an extra render. `const fullName = first + " " + last` is better than any effect.',"The lazy initialiser matters: `useState(expensive())` calls `expensive` on **every** render and throws the result away; `useState(() => expensive())` calls it once. Same trap as building a new object literal in props for a memoised child."],keyPoints:["The setter schedules; it does not assign. The current render keeps the old value.","Use the functional updater whenever the next value depends on the previous.","Mutating state in place leaves the reference unchanged, so nothing re-renders.","Never store what you can derive."],interview:'The three-increments question is near-universal. Answer "1", then explain the closure, then give the updater fix. If they follow up with "why is state asynchronous", the answer is batching and interruptible rendering.',code:`const [count, setCount] = useState(0);

// all three read the same captured value → +1 total
const wrong = () => { setCount(count + 1); setCount(count + 1); setCount(count + 1); };

// each reads the pending value → +3 total
const right = () => { setCount(c => c + 1); setCount(c => c + 1); setCount(c => c + 1); };`,resources:[{label:"React — Queueing a series of state updates",url:"https://react.dev/learn/queueing-a-series-of-state-updates",kind:"docs"},{label:"React — You might not need an effect",url:"https://react.dev/learn/you-might-not-need-an-effect",kind:"docs",note:"The single most useful page on the site. Read it before writing any effect."}]},{id:"react-effects",area:"React Core",group:"Hooks",title:"useEffect — dependencies, cleanup and when not to use it",status:"covered",minutes:9,summary:"The most misused hook in React. Most effects in most codebases should not exist, and the official documentation now says so explicitly.",body:["An effect synchronises your component with an **external system** — a subscription, a network request, a DOM measurement, a timer. That is its purpose. It is not a lifecycle hook, and thinking of it as `componentDidMount` is what produces the misuse.",'The dependency array is compared with `Object.is`. No array means "after every commit"; `[]` means "once after mount"; `[a, b]` means "whenever `a` or `b` changes by reference". Because objects and functions get new identities every render, depending on one re-runs the effect every time — the classic infinite loop. Depend on primitives, or memoise the object with `useMemo`/`useCallback`.',"**Cleanup runs before the next effect and on unmount**, not only on unmount. That is why the return function is where you unsubscribe, clear intervals, and abort fetches. React 18's StrictMode deliberately mounts, unmounts and remounts in development to expose effects that lack cleanup — an effect that breaks under StrictMode was already broken.","The cases that should **not** be effects, from the official guidance: transforming data for rendering (compute during render), handling user events (put it in the handler), resetting state when a prop changes (use a `key` instead), and synchronising two pieces of state (derive one from the other). Each removal deletes a render and a class of bugs.","`useLayoutEffect` runs synchronously after DOM mutation and **before paint** — correct for measuring layout and adjusting it without a visible flicker, and wrong for anything else because it blocks painting."],keyPoints:["Effects synchronise with external systems. Rendering data is not an external system.","Cleanup runs between effects, not just at unmount.","Object and function dependencies change identity every render — that is the infinite-loop cause.","`useLayoutEffect` blocks paint; use it only for measurement."],interview:'Expect "what does the dependency array do" (three cases), "why does my effect loop" (object identity), and increasingly "when would you *not* use an effect". That last one separates people who have read the modern docs from people who learned React in 2019.',code:`// Loops forever: a fresh object each render
useEffect(() => { load(filters); }, [{ page, size }]);

// Correct: depend on the primitives
useEffect(() => { load({ page, size }); }, [page, size]);

// Correct cleanup: cancel rather than set state after unmount
useEffect(() => {
  const ctrl = new AbortController();
  fetchUser(id, ctrl.signal).then(setUser).catch(ignoreAbort);
  return () => ctrl.abort();
}, [id]);`,resources:[{label:"React — Synchronising with effects",url:"https://react.dev/learn/synchronizing-with-effects",kind:"docs"},{label:"React — You might not need an effect",url:"https://react.dev/learn/you-might-not-need-an-effect",kind:"docs"},{label:"Dan Abramov — A complete guide to useEffect",url:"https://overreacted.io/a-complete-guide-to-useeffect/",kind:"article",note:"Long, and the best thing written about the hook."}]},{id:"react-hooks-rest",area:"React Core",group:"Hooks",title:"useRef, useContext, useReducer and custom hooks",status:"partial",minutes:8,summary:"The rest of the core hook set, plus the rule that governs all of them and the one place that rule now bends.",body:["**Rules of hooks**: call them at the top level, never inside conditions, loops or nested functions, and only from components or other hooks. The reason is mechanical — React identifies hooks by **call order** into a per-component list, so skipping one shifts every later hook onto the wrong slot. React 19's `use()` is the single documented exception.",'**`useRef`** is a stable mutable box that survives renders and is invisible to rendering: changing `.current` triggers nothing. That makes it right for DOM handles, timer ids, previous values and "did I already do this" flags — and wrong for anything the UI displays, because the UI will not update.',"**`useContext`** reads the nearest provider. Its performance characteristic is the thing to know: **every consumer re-renders when the value's reference changes**, so passing an inline object literal as `value` re-renders all consumers on every provider render. Memoise the value, or split read and write into two contexts. Context is a dependency-injection mechanism, not a state manager — it does not batch, select or prevent re-renders.","**`useReducer`** centralises transitions when the next state depends on the previous in non-trivial ways, or when several values change together. It makes state changes describable as actions, which is easier to test and to reason about than five setters called in sequence. It is `useState` with the update logic hoisted out.","**Custom hooks** share *logic*, never state. Two components calling `useCounter()` get two independent counters — a point interviewers probe. A custom hook is simply a function whose name begins with `use` and which calls other hooks; that naming convention is what lets the linter check the rules."],keyPoints:["Hooks are matched by call order — that is why the rules exist.","A ref change never re-renders. That is the feature.","Context re-renders every consumer on reference change; memoise the value.","Custom hooks share logic, not state."],interview:`"Difference between state and ref", "why can't hooks be conditional", and "two components use the same custom hook — do they share state?" are all standard. The last one is answered "no" with an explanation of per-call-site hook slots.`,code:`// Stable identity for a context value, so consumers do not re-render needlessly
const value = useMemo(() => ({ user, setUser }), [user]);
return <UserContext.Provider value={value}>{children}</UserContext.Provider>;`,resources:[{label:"React — Rules of hooks",url:"https://react.dev/reference/rules/rules-of-hooks",kind:"docs"},{label:"React — Passing data deeply with context",url:"https://react.dev/learn/passing-data-deeply-with-context",kind:"docs"},{label:"React — Reusing logic with custom hooks",url:"https://react.dev/learn/reusing-logic-with-custom-hooks",kind:"docs"}]},{id:"react-class-lifecycle",area:"React Core",group:"Legacy",title:"Class components and the lifecycle vocabulary",status:"missing",minutes:6,summary:'On the Mettl competency list as "Lifecycle Application", and unavoidable in any codebase older than 2019. A hooks-only preparation cannot answer these.',body:["Mounting: `constructor` → `static getDerivedStateFromProps` → `render` → `componentDidMount`. The last is where subscriptions and initial fetches belong, because the DOM now exists and `setState` is safe.","Updating: `getDerivedStateFromProps` → `shouldComponentUpdate` → `render` → `getSnapshotBeforeUpdate` → `componentDidUpdate`. `shouldComponentUpdate` returning `false` skips the render for that subtree — `React.PureComponent` implements it as a shallow prop and state comparison, making it the class-era `React.memo`.","Unmounting: `componentWillUnmount`, where you remove listeners, clear timers and cancel requests. Forgetting it is the classic memory leak, and it is the exact job the effect cleanup function inherited.","The deprecated trio — `componentWillMount`, `componentWillReceiveProps`, `componentWillUpdate` — were unsafe under concurrent rendering because they could run more than once per commit. They survive as `UNSAFE_`-prefixed names.","Mapping to hooks: `componentDidMount` ≈ `useEffect(fn, [])`; `componentDidUpdate` ≈ `useEffect(fn, [deps])`; `componentWillUnmount` ≈ the cleanup return; `shouldComponentUpdate` ≈ `React.memo`. The mapping is approximate, not exact — effects run after paint, `componentDidUpdate` runs after commit but before paint, which is why `useLayoutEffect` is the closer analogue for measurement."],keyPoints:["`componentDidMount` = after first commit, DOM available.","`shouldComponentUpdate` skips the render; `PureComponent` implements it shallowly.","`componentWillUnmount` is the cleanup; missing it leaks.","The `componentWill*` trio is unsafe under concurrent rendering."],interview:'Directly on the Mettl syllabus: "which lifecycle method for an API call" (`componentDidMount`), "what does `shouldComponentUpdate` do", and "map lifecycles to hooks". Say the mapping is approximate and name why — it shows depth rather than memorisation.',resources:[{label:"React — Component (legacy API reference)",url:"https://react.dev/reference/react/Component",kind:"docs",note:"The complete lifecycle reference, still maintained."},{label:"React lifecycle methods diagram",url:"https://projects.wojtekmaj.pl/react-lifecycle-methods-diagram/",kind:"practice",note:"Interactive diagram — the fastest way to memorise the order."}]}],xe=[{id:"react-immutability",area:"React Core",group:"State",title:"Immutability in React — mutating vs copying methods",status:"partial",minutes:8,summary:"React decides whether to re-render by comparing references with `Object.is`. Mutating state in place leaves the reference identical, so the update is invisible. This is the single most consequential JavaScript fact in React.",body:["When you call a setter, React compares the new value with the old using `Object.is`. `items.push(x)` returns a length and leaves `items` pointing at the same array — same reference, so React bails out and nothing renders. The data changed; the identity did not. `setItems([...items, x])` creates a new array, a new reference, and a render.",'The same logic drives `React.memo`, `useMemo`, `useCallback` and hook dependency arrays — all shallow reference comparisons. This is why "I mutated state and the UI did not update" and "I added memo and it still re-renders" are the same problem seen from opposite ends.',"**Array methods that mutate** (never use directly on state): `push`, `pop`, `shift`, `unshift`, `splice`, `sort`, `reverse`, `fill`, `copyWithin`. **Methods that return a new array** (safe): `map`, `filter`, `slice`, `concat`, `flat`, `flatMap`, `toSorted`, `toReversed`, `toSpliced`, `with`. Spread is safe. The trap is `sort` and `reverse`, which look harmless and reorder your state array in place — always copy first: `[...items].sort()`.",'**Objects**: spread for a shallow update, `Object.assign({}, obj, changes)` for the same thing in older code. Nested updates require copying every level on the path to the change — this is the "spread staircase" that makes deep state unpleasant and is a good reason to keep state flat, or to use Immer (`produce`), which lets you write mutating syntax and produces an immutable result.',"Two subtleties. `useState` with an object gives you no automatic merging — unlike the old `this.setState`, you must spread the previous state yourself. And when the new value derives from the old, use the functional updater so you are copying the *pending* value rather than a stale closure: `setItems(prev => [...prev, x])`."],keyPoints:["`sort`, `reverse` and `splice` mutate. `toSorted`, `toReversed`, `toSpliced` and `with` do not.","Same reference ⇒ no re-render. That is `Object.is`, not React being clever.","Nested updates must copy every level on the path to the change.","`useState` never merges objects for you — spread the previous value yourself."],interview:`"Why can't I push to state?" is the entry-level version; "why did my memoised child re-render anyway" is the senior one. Both answers are reference identity. Being able to write the nested update by hand — and then say "or Immer" — is the complete answer.`,pitfalls:["`items.sort()` inside a render or handler — mutates state and produces no update.","Mutating a nested object inside an otherwise-spread copy: `{...state}` is shallow, so `copy.user.name = x` edits the original."],code:`// arrays
setItems(prev => [...prev, item]);                       // add
setItems(prev => prev.filter(i => i.id !== id));         // remove
setItems(prev => prev.map(i =>                            // update one
  i.id === id ? { ...i, done: !i.done } : i));
setItems(prev => [...prev].sort(byName));                 // sort — copy first
setItems(prev => prev.toSorted(byName));                  // ES2023 equivalent

// objects, including nested
setUser(prev => ({ ...prev, name }));                     // shallow
setUser(prev => ({                                        // nested: copy the path
  ...prev,
  address: { ...prev.address, city },
}));`,resources:[{label:"React — Updating arrays in state",url:"https://react.dev/learn/updating-arrays-in-state",kind:"docs",note:"Includes the mutating/non-mutating table. Bookmark it."},{label:"React — Updating objects in state",url:"https://react.dev/learn/updating-objects-in-state",kind:"docs"},{label:"Immer — produce",url:"https://immerjs.github.io/immer/",kind:"docs",note:"Write mutating syntax, get an immutable result. Built into Redux Toolkit."},{label:"MDN — Array (mutation noted per method)",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array",kind:"docs"}]},{id:"react-references-copying",area:"JavaScript",group:"Data",title:"Object references, shallow copies and deep cloning",status:"covered",minutes:6,summary:'Assignment copies the reference, not the object. Every aliasing bug, every failed re-render and every "I changed the copy and the original changed too" traces here.',body:["Primitives are copied by value: `let a = 1; let b = a; b = 2` leaves `a` at 1. Objects are copied by **reference**: `const b = a` gives two names for one object in the heap, so `b.x = 9` is visible through `a`. Function arguments follow the same rule — passing an object passes the reference, so a function can mutate the caller's object.","`{...obj}` and `Object.assign({}, obj)` are **shallow**: the top-level keys are new, but any nested object is still the same shared reference. This is the most common copying bug in React — you spread state, feel safe, then mutate `copy.address.city` and change the original.","`structuredClone(obj)` is the platform deep clone. It handles nested objects, arrays, `Date`, `Map`, `Set`, `RegExp`, `ArrayBuffer` and **circular references**. It cannot clone functions, DOM nodes, or class identity (you get a plain object back), and it throws on anything non-cloneable.","`JSON.parse(JSON.stringify(obj))` is the old idiom and it is lossy: `undefined` values and functions vanish, `Date` becomes a string, `NaN` and `Infinity` become `null`, `Map`/`Set` become `{}`, and a circular reference throws. Know it, and know why `structuredClone` replaced it.","Equality follows the same rule: `{a:1} === {a:1}` is `false` because they are two objects. Comparing structures requires deep equality — `JSON.stringify` comparison for simple data (order-sensitive, so fragile), or a library such as `fast-deep-equal`. This is exactly why React compares references and asks you to create new ones, rather than deep-comparing on every render."],keyPoints:["Assignment copies the reference; two names, one object.","Spread and `Object.assign` are shallow — nested objects stay shared.","`structuredClone` handles Dates, Maps, Sets and cycles; JSON round-tripping destroys them.","`{a:1} === {a:1}` is false — deep equality needs a deep comparison."],interview:'The canonical snippet: spread an object with a nested object, mutate the nested value, ask what the original shows. Answer, name shallow copy, offer `structuredClone`, then connect it to React — "and this is why mutating nested state produces no re-render".',code:`const a = { x: 1, nested: { y: 2 } };
const b = { ...a };
b.nested.y = 99;
a.nested.y;                  // 99 — the nested object was never copied

const c = structuredClone(a);
c.nested.y = 7;
a.nested.y;                  // 99 — genuinely independent

JSON.parse(JSON.stringify({ d: new Date(), u: undefined }));
// { d: "2026-08-25T…" }  ← Date became a string, undefined key vanished`,resources:[{label:"MDN — structuredClone",url:"https://developer.mozilla.org/en-US/docs/Web/API/structuredClone",kind:"docs"},{label:"MDN — Object.assign",url:"https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/assign",kind:"docs"},{label:"MDN — Shallow copy",url:"https://developer.mozilla.org/en-US/docs/Glossary/Shallow_copy",kind:"docs"}]}],ke=[{id:"react-composition",area:"React Advanced",group:"Patterns",title:"Composition: children, render props, HOCs, compound components",status:"missing",minutes:7,summary:'Named on the Mettl competency list (Higher Order Components) and the substance of every "how would you design this component" question.',body:["**Composition over configuration.** A component with fourteen boolean props is a design smell; the same flexibility usually falls out of accepting `children` or named slots. Passing elements as props is also the standard cure for prop drilling — hand the subtree down rather than the data it needs.","**Higher Order Component**: a function that takes a component and returns a new one — `withRouter`, `connect`, `withTheme`. It composes behaviour without inheritance. The costs are a deeper tree, prop-name collisions, and the need to hoist statics and forward refs. Hooks replaced most HOCs, but the pattern is explicitly on the assessment syllabus, so be able to write one.","**Render props**: pass a function as a child (or prop) and call it with internal state — `<Mouse>{pos => <Cat at={pos} />}</Mouse>`. Maximum flexibility, at the cost of nesting. Hooks solved the same problem more cleanly, which is the historical arc worth narrating.","**Compound components**: several components sharing implicit state through context — `<Tabs><Tabs.List/><Tabs.Panel/></Tabs>`. This is how mature UI libraries model widgets, because it gives consumers layout freedom while the parent keeps the state machine.","**Controlled versus uncontrolled** applies to your own components too: accept `value` + `onChange` for controlled use, `defaultValue` for uncontrolled, and support both — that is what every serious library does."],keyPoints:["HOC: `Component => Component`. Remember to forward refs and hoist statics.","Render props and HOCs both solved logic reuse; hooks superseded both.","Passing elements as children is the simplest cure for prop drilling.","Support controlled and uncontrolled modes in reusable components."],interview:'"Design a reusable Modal/Tabs/Autocomplete" is the senior component-design question. Talk about the state machine, what the consumer controls, the accessibility contract, and whether it is controlled — that structure is the answer.',code:`// HOC — still named on the Mettl React competency list
function withLogging(Wrapped) {
  function WithLogging(props) {
    useEffect(() => { track(Wrapped.displayName); }, []);
    return <Wrapped {...props} />;
  }
  WithLogging.displayName = \`withLogging(\${Wrapped.displayName ?? Wrapped.name})\`;
  return WithLogging;
}`,resources:[{label:"React — Passing props to a component",url:"https://react.dev/learn/passing-props-to-a-component",kind:"docs"},{label:"React (legacy docs) — Higher-Order Components",url:"https://legacy.reactjs.org/docs/higher-order-components.html",kind:"docs",note:"Legacy, but this is the page the HOC interview question comes from."},{label:"Kent C. Dodds — Advanced React patterns",url:"https://kentcdodds.com/blog/compound-components-with-react-hooks",kind:"article"}]},{id:"react-perf",area:"React Advanced",group:"Performance",title:"memo, useMemo, useCallback, virtualization, profiling",status:"partial",minutes:8,summary:'Memoisation is the most cargo-culted topic in React. The senior answer is always "measure first" — and knowing exactly when each tool applies.',body:['`React.memo` shallow-compares props and skips re-rendering when they are unchanged. It fails the moment a prop is a fresh object, array or function literal created during the parent\'s render — which is most of the time, and why "I added memo and nothing improved" is so common. `useMemo` caches a computed value; `useCallback(fn, deps)` is exactly `useMemo(() => fn, deps)` and caches the function identity.',"Memoisation is not free: it costs memory and a comparison on every render. Wrapping everything makes an app slower and much harder to read. The correct order is profile → find the actual cost → memoise that. **React 19's compiler automates most of this**, which makes the manual version increasingly a legacy skill — and still an interview topic.","For long lists, memoisation is the wrong tool entirely: **virtualization** (`react-window`, TanStack Virtual) renders only the visible rows. Ten thousand rows become twenty DOM nodes. That is a structural fix, not a micro-optimisation. In CSS-only cases `content-visibility: auto` gets much of the benefit with no library.","**Code splitting** with `React.lazy` + `Suspense` moves work out of the initial bundle; route-level splitting is the highest-leverage version. Combine with `manualChunks` so vendor code caches independently of your application code.",'Profiling: the React DevTools Profiler shows what re-rendered and why (enable "record why each component rendered"), and the browser Performance panel shows where main-thread time went. The Core Web Vitals to name are **LCP** (largest contentful paint), **INP** (interaction to next paint, which replaced FID in 2024) and **CLS** (cumulative layout shift).'],keyPoints:["`memo` is defeated by inline object, array and function props.","`useCallback(fn, deps)` ≡ `useMemo(() => fn, deps)`.","Long lists need virtualization, not memoisation.","LCP, INP, CLS — INP replaced FID."],interview:'"How would you optimise a slow React app?" wants a *process*: profile, identify whether the cost is render, network or main-thread work, then apply the matching fix — memo, virtualization, code splitting, or moving work off the main thread. Jumping straight to `useMemo` is the wrong-shaped answer.',resources:[{label:"React — memo",url:"https://react.dev/reference/react/memo",kind:"docs",note:'The "when not to use it" section is the important half.'},{label:"React — React Compiler",url:"https://react.dev/learn/react-compiler",kind:"docs"},{label:"web.dev — Core Web Vitals",url:"https://web.dev/articles/vitals",kind:"article"},{label:"TanStack Virtual",url:"https://tanstack.com/virtual/latest",kind:"docs"}]},{id:"react-errors-portals",area:"React Advanced",group:"Escape hatches",title:"Error boundaries, portals, refs and imperative escape hatches",status:"missing",minutes:6,summary:"The parts of React that step outside the declarative model — and the reason each one exists.",body:["**Error boundaries** catch errors thrown during rendering, in lifecycles, and in constructors of the tree below them, and render a fallback instead of unmounting the whole app. They must be **class components** — `getDerivedStateFromError` for the fallback state, `componentDidCatch` for logging. They do **not** catch errors in event handlers, in asynchronous code, or in the boundary itself; those need ordinary `try/catch`. Placement is a design decision: one per pane keeps a failure the size of the thing that failed.","**Portals** render children into a different DOM node while keeping them in the React tree — so context still flows and events still bubble *through React*, to the React parent, not the DOM parent. That is precisely what modals, tooltips and toasts need to escape `overflow: hidden` and stacking contexts without losing their place in the component hierarchy.","**Refs** reach the DOM for the things React does not model: focus, text selection, media playback, scroll position, and integrating imperative third-party libraries. `useImperativeHandle` lets a component expose a deliberately narrow imperative API to its parent — `{ focus, reset }` rather than the raw node. In React 19, `ref` is an ordinary prop and `forwardRef` is no longer needed.","`flushSync` forces a synchronous re-render and commit, escaping batching. It exists for the rare case where you must measure the DOM immediately after an update. It hurts performance by definition, which is why it is a last resort rather than a tool."],keyPoints:["Error boundaries must be classes and do not catch event-handler or async errors.","Portals move the DOM node but keep the React tree — context and event bubbling follow React.","`useImperativeHandle` narrows what a parent can do to a child.","React 19: `ref` is a normal prop; `forwardRef` is legacy."],interview:'"How do you handle errors in React?" expects error boundaries **plus** the explicit list of what they miss. "How would you build a modal?" expects a portal, focus management, Escape handling and a return of focus on close.',code:`class PaneBoundary extends React.Component {
  state = { error: null };
  static getDerivedStateFromError(error) { return { error }; }
  componentDidCatch(error, info) { log(error, info.componentStack); }
  render() {
    return this.state.error
      ? <Fallback error={this.state.error} onRetry={() => this.setState({ error: null })} />
      : this.props.children;
  }
}`,resources:[{label:"React — createPortal",url:"https://react.dev/reference/react-dom/createPortal",kind:"docs"},{label:"React — Manipulating the DOM with refs",url:"https://react.dev/learn/manipulating-the-dom-with-refs",kind:"docs"},{label:"React — Component.componentDidCatch",url:"https://react.dev/reference/react/Component#componentdidcatch",kind:"docs"}]}],O=[...ce,...de,...he,...ue,...pe,...me,...fe,...ge,...be,...ye,...we,...ve,...xe,...ke],$=["Web Platform","HTML","Accessibility","CSS","JavaScript","TypeScript","React Core","React Advanced","React 19","Routing","State Management","Data & APIs","Testing","Performance","Tooling","Architecture"],Se=new Map(O.map(t=>[t.id,t]));function Ce(){const t={covered:0,partial:0,missing:0};for(const e of O)t[e.status]++;return{topics:O.length,areas:new Set(O.map(e=>e.area)).size,minutes:O.reduce((e,n)=>e+n.minutes,0),resources:O.reduce((e,n)=>e+n.resources.length,0),...t}}const Te={covered:"bg-emerald-500",partial:"bg-amber-500",missing:"bg-rose-500"};function Ne(t){const e=J.c(73),{activeId:n,read:o,onSelect:P}=t;let w;e[0]===Symbol.for("react.memo_cache_sentinel")?(w={id:"status",label:"drilled",options:[{value:"covered",label:"covered",test:Ie},{value:"partial",label:"thin",test:ze},{value:"missing",label:"not drilled",test:Pe}]},e[0]=w):w=e[0];let r;e[1]!==o?(r={value:"unread",label:"unread",test:i=>!o[i.id]},e[1]=o,e[2]=r):r=e[2];let u;e[3]!==o?(u={value:"done",label:"read",test:i=>!!o[i.id]},e[3]=o,e[4]=u):u=e[4];let p;e[5]!==r||e[6]!==u?(p=[w,{id:"read",label:"state",options:[r,u]}],e[5]=r,e[6]=u,e[7]=p):p=e[7];const j=p;let M;e[8]===Symbol.for("react.memo_cache_sentinel")?(M={id:"gaps",label:"gaps",hint:"On the syllabus for interviews, absent from your drills",test:Oe},e[8]=M):M=e[8];let m;e[9]!==o?(m={id:"unread",label:"unread",hint:"Not yet marked read",test:i=>!o[i.id]},e[9]=o,e[10]=m):m=e[10];let z;e[11]===Symbol.for("react.memo_cache_sentinel")?(z={id:"short",label:"under 7 min",hint:"Fits in a coffee break",test:Ae},e[11]=z):z=e[11];let x;e[12]!==m?(x=[M,m,z],e[12]=m,e[13]=x):x=e[13];const v=x;let k;e[14]!==j||e[15]!==v?(k={items:O,storageKey:"learn:nav",text:De,group:Re,subgroup:Me,facets:j,views:v},e[14]=j,e[15]=v,e[16]=k):k=e[16];const s=Q(k);let f;if(e[17]!==j||e[18]!==v){f={};for(const i of j)for(const l of i.options)f[`${i.id}:${l.value}`]=O.filter(l.test).length;for(const i of v)f[`view:${i.id}`]=O.filter(i.test).length;e[17]=j,e[18]=v,e[19]=f}else f=e[19];const g=f;let S;e[20]!==s.groups?(S=[...s.groups].sort(je),e[20]=s.groups,e[21]=S):S=e[21];const C=S;let b;e[22]===Symbol.for("react.memo_cache_sentinel")?(b=a.jsx(te,{size:13,className:"absolute left-2.5 top-1/2 -translate-y-1/2 text-slate-400 pointer-events-none"}),e[22]=b):b=e[22];let c,y;e[23]!==s?(c=i=>s.setQuery(i.target.value),y=i=>{i.key==="Escape"&&s.setQuery("")},e[23]=s,e[24]=c,e[25]=y):(c=e[24],y=e[25]);let d;e[26]!==s.query||e[27]!==c||e[28]!==y?(d=a.jsxs("div",{className:"relative",children:[b,a.jsx("input",{value:s.query,onChange:c,onKeyDown:y,placeholder:`Search ${O.length} topics…`,"aria-label":"Search the reading library",className:`w-full pl-7 pr-2 py-1.5 text-xs bg-white border border-slate-300 rounded-md outline-none
                       focus:border-sky-600 focus:ring-2 focus:ring-sky-100`})]}),e[26]=s.query,e[27]=c,e[28]=y,e[29]=d):d=e[29];let h;e[30]!==g||e[31]!==s?(h=s.views.map(i=>a.jsxs("button",{onClick:()=>s.toggleView(i.id),title:i.hint,"aria-pressed":s.view===i.id,className:`px-1.5 py-0.5 rounded text-[9px] font-bold uppercase tracking-wide border
                ${s.view===i.id?"bg-sky-700 border-sky-700 text-white":"bg-white border-slate-300 text-slate-600 hover:border-sky-500"}`,children:[i.label,a.jsx("span",{className:"ml-1 opacity-60 tabular-nums",children:g[`view:${i.id}`]})]},i.id)),e[30]=g,e[31]=s,e[32]=h):h=e[32];let T;e[33]!==h?(T=a.jsx("div",{className:"flex flex-wrap gap-1",children:h}),e[33]=h,e[34]=T):T=e[34];let N;e[35]!==g||e[36]!==s?(N=s.facets.map(i=>a.jsxs("div",{className:"flex flex-wrap items-center gap-1",children:[a.jsx("span",{className:"text-[8px] font-bold uppercase tracking-wider text-slate-400 w-9 shrink-0",children:i.label}),i.options.map(l=>a.jsxs("button",{onClick:()=>s.toggleFacet(i.id,l.value),"aria-pressed":s.isSelected(i.id,l.value),className:`px-1.5 py-0.5 rounded-full text-[9px] font-semibold border
                  ${s.isSelected(i.id,l.value)?"bg-slate-900 border-slate-900 text-white":"bg-white border-slate-200 text-slate-600 hover:border-slate-400"}`,children:[l.label,a.jsx("span",{className:"ml-1 opacity-60 tabular-nums",children:g[`${i.id}:${l.value}`]})]},l.value))]},i.id)),e[35]=g,e[36]=s,e[37]=N):N=e[37];const R=s.matched===s.total?`${s.total} topics`:`${s.matched} of ${s.total}`;let D;e[38]!==R?(D=a.jsx("span",{"aria-live":"polite",className:"tabular-nums font-semibold",children:R}),e[38]=R,e[39]=D):D=e[39];let I;e[40]!==s.clear||e[41]!==s.isFiltered?(I=s.isFiltered&&a.jsxs("button",{onClick:s.clear,className:"flex items-center gap-0.5 px-1.5 py-0.5 rounded bg-amber-100 text-amber-800 font-semibold",children:[a.jsx(ee,{size:9})," clear"]}),e[40]=s.clear,e[41]=s.isFiltered,e[42]=I):I=e[42];let _;e[43]!==D||e[44]!==I?(_=a.jsxs("div",{className:"flex items-center gap-1.5 text-[10px] text-slate-500",children:[D,I]}),e[43]=D,e[44]=I,e[45]=_):_=e[45];let A;e[46]!==d||e[47]!==T||e[48]!==N||e[49]!==_?(A=a.jsxs("div",{className:"p-2.5 border-b border-slate-200 bg-slate-50/70 shrink-0 flex flex-col gap-1.5",children:[d,T,N,_]}),e[46]=d,e[47]=T,e[48]=N,e[49]=_,e[50]=A):A=e[50];const W=`flex-1 overflow-y-auto p-2 space-y-1 transition-opacity ${s.stale?"opacity-60":""}`;let U;e[51]!==s.clear||e[52]!==s.query||e[53]!==C.length?(U=C.length===0&&a.jsxs("div",{className:"text-center p-6",children:[a.jsxs("p",{className:"text-xs text-slate-500 mb-2",children:["Nothing matches",s.query.trim()&&a.jsxs(a.Fragment,{children:[" “",a.jsx("strong",{children:s.query.trim()}),"”"]}),"."]}),a.jsx("button",{onClick:s.clear,className:"px-2 py-1 text-xs font-semibold rounded bg-sky-700 text-white",children:"Clear filters"})]}),e[51]=s.clear,e[52]=s.query,e[53]=C.length,e[54]=U):U=e[54];let E;if(e[55]!==n||e[56]!==s||e[57]!==P||e[58]!==C||e[59]!==o){let i;e[61]!==n||e[62]!==s||e[63]!==P||e[64]!==o?(i=l=>a.jsxs("details",{open:!l.collapsed,style:{contentVisibility:"auto",containIntrinsicSize:"auto 2.5rem"},children:[a.jsxs("summary",{onClick:H=>{H.preventDefault(),s.toggleCollapsed(l.key)},className:`flex items-center gap-1.5 p-2 rounded-lg text-[11px] font-bold cursor-pointer list-none
                         text-slate-700 hover:bg-slate-100`,children:[a.jsx(ae,{size:12,className:`shrink-0 text-slate-400 transition-transform ${l.collapsed?"":"rotate-90"}`}),a.jsx("span",{className:"truncate",children:l.label}),a.jsx("span",{className:"ml-auto text-[9px] tabular-nums text-slate-400",children:l.items.length===l.total?l.total:`${l.items.length}/${l.total}`})]}),a.jsx("div",{className:"pl-2 pt-1 space-y-1",children:l.subgroups.map(H=>a.jsxs("div",{children:[a.jsx("p",{className:"px-2 py-0.5 text-[9px] font-bold uppercase tracking-wider text-slate-400 truncate",children:H.label}),H.items.map(q=>a.jsxs("button",{onClick:()=>P(q),"aria-current":n===q.id,className:`w-full text-left p-2 rounded-lg text-[11px] flex items-start gap-2 border transition-colors
                        ${n===q.id?"bg-sky-50 border-sky-300 text-sky-900":"border-transparent hover:bg-slate-50 text-slate-600"}`,children:[a.jsx("span",{className:`mt-1 w-1.5 h-1.5 rounded-full shrink-0 ${Te[q.status]}`,title:`Drill coverage: ${q.status}`}),a.jsxs("span",{className:"flex-1 min-w-0",children:[a.jsx("span",{className:`block truncate ${o[q.id]?"line-through opacity-60":"font-medium"}`,children:q.title}),a.jsxs("span",{className:"flex items-center gap-1 text-[9px] text-slate-400 mt-0.5",children:[a.jsx(X,{size:9})," ",q.minutes," min · ",q.resources.length," links"]})]})]},q.id))]},H.key))})]},l.key),e[61]=n,e[62]=s,e[63]=P,e[64]=o,e[65]=i):i=e[65],E=C.map(i),e[55]=n,e[56]=s,e[57]=P,e[58]=C,e[59]=o,e[60]=E}else E=e[60];let F;e[66]!==W||e[67]!==U||e[68]!==E?(F=a.jsxs("div",{className:W,children:[U,E]}),e[66]=W,e[67]=U,e[68]=E,e[69]=F):F=e[69];let L;return e[70]!==A||e[71]!==F?(L=a.jsxs("div",{className:"h-full flex flex-col rounded-xl overflow-hidden border border-slate-200 bg-white",children:[A,F]}),e[70]=A,e[71]=F,e[72]=L):L=e[72],L}function je(t,e){return $.indexOf(t.key)-$.indexOf(e.key)}function Me(t){return{key:t.group,label:t.group}}function Re(t){return{key:t.area,label:t.area}}function De(t){return`${t.title} ${t.area} ${t.group} ${t.summary} ${t.keyPoints.join(" ")}`}function Ae(t){return t.minutes<7}function Oe(t){return t.status==="missing"}function Pe(t){return t.status==="missing"}function ze(t){return t.status==="partial"}function Ie(t){return t.status==="covered"}const _e={covered:{label:"Drilled",cls:"bg-emerald-100 text-emerald-800 border-emerald-200",note:"The Mastery stream practises this. Reading here is revision."},partial:{label:"Thinly drilled",cls:"bg-amber-100 text-amber-800 border-amber-200",note:"A question or two exists, but there is no real practice. Reading is most of your exposure."},missing:{label:"Not drilled",cls:"bg-rose-100 text-rose-800 border-rose-200",note:"Nothing in the drill syllabus covers this. Reading here is your only exposure — treat it accordingly."}};function qe(t){const e=J.c(61),{topic:n,isRead:o,onToggleRead:P}=t,w=_e[n.status];let r;e[0]!==n.area?(r=a.jsx("span",{className:"px-2 py-1 rounded bg-sky-50 text-sky-700",children:n.area}),e[0]=n.area,e[1]=r):r=e[1];let u;e[2]!==n.group?(u=a.jsx("span",{className:"text-slate-400",children:n.group}),e[2]=n.group,e[3]=u):u=e[3];let p;e[4]!==n.minutes?(p=a.jsxs("span",{className:"text-slate-400",children:["· ",n.minutes," min read"]}),e[4]=n.minutes,e[5]=p):p=e[5];const j=`px-2 py-1 rounded border ${w.cls}`;let M;e[6]!==w.label||e[7]!==j?(M=a.jsx("span",{className:j,children:w.label}),e[6]=w.label,e[7]=j,e[8]=M):M=e[8];let m;e[9]!==r||e[10]!==u||e[11]!==p||e[12]!==M?(m=a.jsxs("div",{className:"flex items-center gap-2 flex-wrap text-[10px] font-bold uppercase tracking-wider",children:[r,u,p,M]}),e[9]=r,e[10]=u,e[11]=p,e[12]=M,e[13]=m):m=e[13];let z;e[14]===Symbol.for("react.memo_cache_sentinel")?(z={textWrap:"balance"},e[14]=z):z=e[14];let x;e[15]!==n.title?(x=a.jsx("h1",{className:"text-3xl font-extrabold tracking-tight text-slate-900",style:z,children:n.title}),e[15]=n.title,e[16]=x):x=e[16];let v;e[17]!==n.summary?(v=a.jsx("p",{className:"text-[15px] leading-relaxed text-slate-700 bg-slate-50 border border-slate-200 rounded-xl p-4",children:n.summary}),e[17]=n.summary,e[18]=v):v=e[18];let k;e[19]!==w.note?(k=a.jsx("p",{className:"text-[11px] text-slate-500 italic",children:w.note}),e[19]=w.note,e[20]=k):k=e[20];let s;e[21]!==k||e[22]!==m||e[23]!==x||e[24]!==v?(s=a.jsxs("header",{className:"space-y-3",children:[m,x,v,k]}),e[21]=k,e[22]=m,e[23]=x,e[24]=v,e[25]=s):s=e[25];let f;e[26]!==n.body?(f=n.body.map(Fe),e[26]=n.body,e[27]=f):f=e[27];let g;e[28]!==f?(g=a.jsx("section",{className:"space-y-4",children:f}),e[28]=f,e[29]=g):g=e[29];let S;e[30]!==n.code?(S=n.code&&a.jsx("pre",{className:"text-[12.5px] leading-relaxed bg-slate-900 text-slate-100 rounded-xl p-4 overflow-x-auto font-mono",children:a.jsx("code",{children:n.code})}),e[30]=n.code,e[31]=S):S=e[31];let C;e[32]===Symbol.for("react.memo_cache_sentinel")?(C=a.jsxs("h2",{className:"flex items-center gap-2 px-4 py-2 bg-slate-50 text-[11px] font-bold uppercase tracking-wider text-slate-600",children:[a.jsx(le,{size:13,className:"text-amber-500"})," Worth memorising"]}),e[32]=C):C=e[32];let b;e[33]!==n.keyPoints?(b=n.keyPoints.map(Ee),e[33]=n.keyPoints,e[34]=b):b=e[34];let c;e[35]!==b?(c=a.jsxs("section",{className:"rounded-xl border border-slate-200 overflow-hidden",children:[C,a.jsx("ul",{className:"p-4 space-y-2",children:b})]}),e[35]=b,e[36]=c):c=e[36];let y;e[37]===Symbol.for("react.memo_cache_sentinel")?(y=a.jsxs("h2",{className:"flex items-center gap-2 text-[11px] font-bold uppercase tracking-wider text-sky-800 mb-2",children:[a.jsx(se,{size:13})," How it is actually asked"]}),e[37]=y):y=e[37];let d;e[38]!==n.interview?(d=a.jsxs("section",{className:"rounded-xl border border-sky-200 bg-sky-50/50 p-4",children:[y,a.jsx("p",{className:"text-[14px] leading-relaxed text-sky-950",children:n.interview})]}),e[38]=n.interview,e[39]=d):d=e[39];let h;e[40]!==n.pitfalls?(h=n.pitfalls&&n.pitfalls.length>0&&a.jsxs("section",{className:"rounded-xl border border-rose-200 bg-rose-50/50 p-4",children:[a.jsxs("h2",{className:"flex items-center gap-2 text-[11px] font-bold uppercase tracking-wider text-rose-800 mb-2",children:[a.jsx(K,{size:13})," Where people get it wrong"]}),a.jsx("ul",{className:"space-y-1.5",children:n.pitfalls.map(Ue)})]}),e[40]=n.pitfalls,e[41]=h):h=e[41];let T;e[42]===Symbol.for("react.memo_cache_sentinel")?(T=a.jsxs("h2",{className:"flex items-center gap-2 px-4 py-2 bg-slate-50 text-[11px] font-bold uppercase tracking-wider text-slate-600",children:[a.jsx(X,{size:13,className:"text-sky-500"})," Read further"]}),e[42]=T):T=e[42];let N;e[43]!==n.resources?(N=n.resources.map(We),e[43]=n.resources,e[44]=N):N=e[44];let R;e[45]!==N?(R=a.jsxs("section",{className:"rounded-xl border border-slate-200 overflow-hidden",children:[T,a.jsx("ul",{className:"divide-y divide-slate-100",children:N})]}),e[45]=N,e[46]=R):R=e[46];const D=`px-4 py-2 rounded-xl text-sm font-bold flex items-center gap-2 transition-colors
            ${o?"bg-emerald-600 text-white":"bg-slate-900 text-white hover:bg-slate-800"}`;let I;e[47]===Symbol.for("react.memo_cache_sentinel")?(I=a.jsx(ne,{size:15}),e[47]=I):I=e[47];const _=o?"Read ✓ — click to unmark":"Mark as read";let A;e[48]!==P||e[49]!==D||e[50]!==_?(A=a.jsx("footer",{className:"pt-2 pb-8",children:a.jsxs("button",{onClick:P,className:D,children:[I,_]})}),e[48]=P,e[49]=D,e[50]=_,e[51]=A):A=e[51];let W;return e[52]!==s||e[53]!==g||e[54]!==S||e[55]!==c||e[56]!==d||e[57]!==h||e[58]!==R||e[59]!==A?(W=a.jsxs("article",{className:"max-w-[68ch] mx-auto px-6 py-6 space-y-6",children:[s,g,S,c,d,h,R,A]}),e[52]=s,e[53]=g,e[54]=S,e[55]=c,e[56]=d,e[57]=h,e[58]=R,e[59]=A,e[60]=W):W=e[60],W}function We(t){return a.jsx("li",{children:a.jsxs("a",{href:t.url,target:"_blank",rel:"noreferrer noopener",className:"flex items-start gap-2 px-4 py-3 hover:bg-sky-50/60 group",children:[a.jsx(oe,{size:13,className:"mt-0.5 shrink-0 text-slate-400 group-hover:text-sky-600"}),a.jsxs("span",{className:"flex-1 min-w-0",children:[a.jsx("span",{className:"block text-[14px] font-semibold text-sky-800 group-hover:underline",children:t.label}),t.note&&a.jsx("span",{className:"block text-[12px] text-slate-500 mt-0.5",children:t.note})]}),a.jsx("span",{className:"text-[9px] font-bold uppercase tracking-wider text-slate-400 shrink-0 mt-1",children:t.kind})]})},t.url)}function Ue(t,e){return a.jsxs("li",{className:"text-[14px] leading-relaxed text-rose-950 flex gap-2",children:[a.jsx("span",{className:"select-none",children:"✗"}),a.jsx("span",{children:t})]},e)}function Ee(t,e){return a.jsxs("li",{className:"text-[14px] leading-relaxed text-slate-700 flex gap-2",children:[a.jsx("span",{className:"text-amber-500 select-none",children:"▪"}),a.jsx("span",{children:t})]},e)}function Fe(t,e){return a.jsx("p",{className:"text-[15px] leading-[1.75] text-slate-800",style:{textWrap:"pretty"},children:t},e)}const Y="learn:read";function He(){try{return JSON.parse(localStorage.getItem(Y)||"{}")}catch{return{}}}function Ve(){var N;const t=J.c(32),[e,n]=G.useState(((N=O[0])==null?void 0:N.id)??""),[o,P]=G.useState(He);let w;t[0]!==e?(w=Se.get(e)??O[0],t[0]=e,t[1]=w):w=t[1];const r=w;let u;t[2]===Symbol.for("react.memo_cache_sentinel")?(u=Ce(),t[2]=u):u=t[2];const p=u;let j;t[3]!==o?(j=Object.values(o).filter(Boolean),t[3]=o,t[4]=j):j=t[4];const M=j.length;let m;t[5]===Symbol.for("react.memo_cache_sentinel")?(m=R=>{var D;n(R.id),(D=document.getElementById("learn-reader"))==null||D.scrollTo({top:0})},t[5]=m):m=t[5];const z=m;let x;t[6]!==o||t[7]!==r.id?(x=()=>{const R={...o,[r.id]:!o[r.id]};P(R);try{localStorage.setItem(Y,JSON.stringify(R))}catch{}},t[6]=o,t[7]=r.id,t[8]=x):x=t[8];const v=x;let k,s,f,g;t[9]===Symbol.for("react.memo_cache_sentinel")?(k=a.jsx(Z,{size:16,className:"text-sky-400"}),s=a.jsx("span",{className:"font-bold tracking-tight",children:"The Library"}),f=a.jsx("span",{className:"text-slate-400 hidden md:inline",children:"Read first · then drill · then defend"}),g=a.jsx("span",{className:"flex-1"}),t[9]=k,t[10]=s,t[11]=f,t[12]=g):(k=t[9],s=t[10],f=t[11],g=t[12]);let S;t[13]===Symbol.for("react.memo_cache_sentinel")?(S=a.jsxs("span",{className:"text-slate-400 tabular-nums hidden sm:inline",children:[p.topics," topics · ",p.areas," areas · ",Math.round(p.minutes/60),"h reading · ",p.resources," links"]}),t[13]=S):S=t[13];let C;t[14]===Symbol.for("react.memo_cache_sentinel")?(C=a.jsxs("span",{className:"px-2 py-1 rounded-lg bg-rose-500/10 border border-rose-500/30 text-rose-300 font-mono flex items-center gap-1.5",children:[a.jsx(K,{size:12})," ",p.missing," not drilled"]}),t[14]=C):C=t[14];let b;t[15]!==M?(b=a.jsxs("div",{className:"bg-slate-950 text-white px-4 py-2 flex items-center gap-3 shrink-0 text-xs border-b border-slate-800",children:[k,s,f,g,S,C,a.jsxs("span",{className:"px-2 py-1 rounded-lg bg-emerald-500/10 border border-emerald-500/30 text-emerald-300 font-mono",children:[M,"/",p.topics," read"]})]}),t[15]=M,t[16]=b):b=t[16];let c;t[17]!==e||t[18]!==o?(c=a.jsx(B,{name:"The topic navigator",children:a.jsx(Ne,{activeId:e,read:o,onSelect:z})}),t[17]=e,t[18]=o,t[19]=c):c=t[19];let y;t[20]!==o||t[21]!==v||t[22]!==r?(y=r&&a.jsx(qe,{topic:r,isRead:!!o[r.id],onToggleRead:v}),t[20]=o,t[21]=v,t[22]=r,t[23]=y):y=t[23];let d;t[24]!==y?(d=a.jsx(B,{name:"The reader",children:a.jsx("div",{id:"learn-reader",className:"h-full overflow-y-auto rounded-xl border border-slate-200 bg-white",children:y})}),t[24]=y,t[25]=d):d=t[25];let h;t[26]!==c||t[27]!==d?(h=a.jsxs("main",{className:"grid grid-cols-1 lg:grid-cols-[20rem_1fr] gap-2 p-2 flex-1 min-h-0",children:[c,d]}),t[26]=c,t[27]=d,t[28]=h):h=t[28];let T;return t[29]!==b||t[30]!==h?(T=a.jsxs("div",{className:"flex flex-col flex-1 min-h-0 bg-slate-100",children:[b,h]}),t[29]=b,t[30]=h,t[31]=T):T=t[31],T}export{Ve as default};
